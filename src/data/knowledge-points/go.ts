import type { KnowledgeSourceId } from "./sources.ts";
import type { GraphKnowledgePoint } from "./types.ts";

const goSourceRefs: KnowledgeSourceId[] = [
  "go-docs",
  "go-spec",
  "go-effective-go",
  "go-modules-reference",
  "go-std-lib",
  "go-diagnostics",
  "go-concurrency-pipelines",
];

type GoKnowledgePointInput = Pick<
  GraphKnowledgePoint,
  | "id"
  | "zh"
  | "en"
  | "area"
  | "difficulty"
  | "summary"
  | "concept"
  | "explanation"
  | "typicalProblems"
  | "commonIssues"
  | "practiceAdvice"
  | "applicationScenarios"
  | "prerequisites"
  | "related"
  | "order"
> & {
  commands?: string[];
  focus: string;
  mechanism: string;
  pitfalls: string;
  scenarios: string[];
  sourceRefs?: KnowledgeSourceId[];
};

function goPoint({
  commands,
  focus,
  mechanism,
  pitfalls,
  scenarios,
  sourceRefs = goSourceRefs,
  ...point
}: GoKnowledgePointInput): GraphKnowledgePoint {
  return {
    ...point,
    sourceRefs,
    explanation: point.explanation ?? [
      `概念定义：${point.zh}（${point.en}）聚焦${point.concept}Go 以简洁语法、静态类型、编译工具链和轻量并发组织服务端与基础设施程序；理解它时先抓住${focus}。`,
      `工作机制：${mechanism}`,
      `适用场景：${point.zh}常用于${scenarios.join("、")}。学习时把它放回 Go 工程链路中观察，重点看输入、状态变化、输出结果和资源生命周期。`,
      `特殊场景：在并发、网络、I/O、泛型、模块版本或运行时调优中，${point.zh}会和 ${point.related.slice(0, 3).join("、")} 串联出现。此时重点看边界条件、顺序约束、资源消耗和可观测信号。`,
      `常见误区与注意点：实践中容易把${point.zh}当成孤立语法处理，结果遗漏${pitfalls}。落地时用 gofmt、go test、go vet、race detector 和 pprof 验证行为。`,
      "参考来源：本讲解参考 Go 官方文档、语言规范、Effective Go、模块参考、标准库文档、诊断文档和 Go Blog 并发模式，优先采用官方定义、语义约束和工程实践中的稳定结论。",
    ],
    typicalProblems: point.typicalProblems ?? [
      `${point.zh}解决什么工程问题`,
      `${point.zh}核心机制是什么`,
      `${point.zh}常见边界和排查方式有哪些`,
    ],
    applicationScenarios: point.applicationScenarios ?? scenarios,
    commonCommands: commands,
  };
}

export const goKnowledgePoints = [
  /* <!-- KG_REVIEWED: Go 语言概览 | 2026-06-05 | source_count=16 --> */
  goPoint({
    id: "go-overview",
    zh: "Go 语言概览",
    en: "Go Overview",
    area: "foundation",
    difficulty: "easy",
    summary: "Go 用简洁语法、静态类型、标准工具链和轻量并发支撑可维护的工程交付。",
    concept: "用一套工程化语言、工具链、模块系统和运行时构建后端服务、命令行工具与基础设施程序。",
    focus: "语法简洁、静态类型、包组织、模块依赖、goroutine、channel、GC、诊断工具和工程约定",
    mechanism: "Go 源码按 package 组织，模块用 go.mod 声明依赖和版本，go 命令完成格式化、测试、构建、下载依赖和缓存复用；编译器生成目标程序，运行时负责 goroutine 调度、栈增长、垃圾回收、网络轮询和部分诊断数据采集。",
    scenarios: ["后端 API 与 RPC 服务", "云原生组件", "命令行工具", "基础设施程序", "并发 I/O 密集型服务"],
    pitfalls: "包边界、错误处理、context 取消、goroutine 泄漏、数据竞争、GC 压力和私有模块配置",
    sourceRefs: [
      "go-docs",
      "go-spec",
      "go-command",
      "go-effective-go",
      "go-code-review-comments",
      "go-modules-reference",
      "go-std-lib",
      "go-diagnostics",
      "go-gc-guide",
      "go-memory-model",
      "go-race-detector",
      "go-blog-pprof",
      "go-concurrency-pipelines",
      "draveness-go",
      "draveness-go-scheduler",
      "uber-go-guide",
    ],
    explanation: [
      "概念定位：Go 语言概览解决的是“怎样用一门小而完整的工程语言稳定交付后端与基础设施软件”的问题。\n\nGo（Golang）是一门静态类型、编译型、带垃圾回收和内建并发模型的通用编程语言。它在真实系统里常出现在 HTTP/RPC 服务、云原生控制面、Kubernetes 生态组件、代理网关、CLI、DevOps 自动化、数据管道和并发 I/O 密集型服务中。\n\nGo 的核心价值由四部分组成：语言规格提供稳定语义，`go` 命令提供统一工具链，module 系统提供可复现依赖，runtime 提供 goroutine 调度、栈增长、GC、网络轮询和诊断能力。新手先把 Go 看成“写服务和工具的一体化工程环境”；经验工程师继续关注并发生命周期、内存分配、构建可复现性、GC 延迟、数据竞争和线上证据。",
      "准确定义：Go 是一门以包（package）为组织单元、以模块（module）为版本边界、以 goroutine/channel/context 为并发协作基础、以标准库和诊断工具为工程底座的语言生态。\n\n这个定义包含几条稳定判断线：\n\n- 语言层：`package`、类型系统、接口、方法、组合、显式错误返回和 `defer` 构成日常编码模型。\n- 工具层：`go fmt`、`go test`、`go build`、`go mod tidy`、`go vet`、race detector、pprof 和 trace 构成质量与诊断闭环。\n- 依赖层：`go.mod` 声明模块路径、Go 版本和依赖要求，`go.sum` 记录校验数据，MVS 决定最终构建列表。\n- 运行时层：runtime 管理 goroutine、M/P/G 调度、可增长栈、垃圾回收、计时器、网络轮询和运行时统计。\n- 工程层：清晰包边界、小接口、显式错误处理、上下文取消、自动化测试和性能剖析决定项目可维护性。",
      "心智模型：把 Go 看成“语言本体 + 工程脚手架 + 运行时调度室”。\n\n语言本体让代码保持直接：类型在编译期约束，错误沿返回值传递，接口描述调用方真正需要的行为，组合优先于继承。工程脚手架让团队获得一致流程：格式化、测试、构建、依赖、文档和诊断都由官方工具串起来。运行时调度室把大量 goroutine 映射到少量 OS 线程，结合网络轮询和 GC 支撑高并发服务。\n\n这个模型能解释 Go 的学习路径：先掌握包、类型、错误和测试，再掌握 goroutine、channel、context 和同步原语，最后用 pprof、trace、race detector 和 GC 指标理解生产现象。",
      "主流程机制：一个 Go 服务从源码到线上请求处理，通常经历这些阶段。\n\n1. 模块初始化：`go mod init` 写入模块路径，源码按 `package` 拆分边界，导入路径把包图连接起来。\n2. 依赖解析：`go` 命令读取 `go.mod`、`go.sum`、`GOPROXY`、`GOSUMDB`、`GOPRIVATE` 和构建标签，生成可构建的模块与包集合。\n3. 质量检查：`gofmt` 固定格式，`go test ./...` 执行单元测试和包测试，`go vet` 捕获高风险模式，`go test -race` 在并发路径上查数据竞争。\n4. 编译链接：编译器检查类型、逃逸、内联等信息并生成目标代码，链接器写入二进制；部署时通常只需要二进制、配置和证书等外部资源。\n5. 运行调度：runtime 创建 goroutine，调度器把 G 放到 P 上运行，由 M 承载 OS 线程执行；阻塞系统调用、网络轮询、抢占和 GC 会改变调度状态。\n6. 请求处理：标准库或框架从 listener 接收连接，goroutine 处理请求，`context.Context` 传播超时、取消和请求级元数据。\n7. 观测诊断：日志、指标、pprof、trace、runtime stats、GC 日志和 race 检测结果帮助定位 CPU、内存、锁、阻塞和 goroutine 泄漏。\n\n```bash\ngo version\ngo env GOMOD GOPROXY GOSUMDB GOPRIVATE GOTOOLCHAIN\ngo mod init example.com/app\ngo test ./...\ngo test -race ./...\ngo test -run=^$ -bench=. -benchmem ./...\ngo tool pprof http://127.0.0.1:6060/debug/pprof/profile\n```",
      "核心能力速查：Go 项目可以按“写法、边界、并发、运行、证据”五条线建立知识地图。\n\n```text\n写法: package, import, exported name, struct, method, interface, error, defer\n边界: module path, go.mod, go.sum, semantic version, internal package, cmd package\n并发: goroutine, channel, select, context, sync.Mutex, sync.WaitGroup, atomic\n运行: runtime scheduler, stack growth, GC, network poller, timer, cgo, syscalls\n证据: go test, race detector, pprof, trace, GODEBUG, runtime/metrics, logs\n```\n\n这张地图适合排查真实问题：构建失败看模块和环境变量；请求超时看 context、下游、网络和 goroutine 阻塞；CPU 高看 profile 热点；内存涨看 heap profile、对象分配和 GC；并发错乱看 race detector、锁范围和 happens-before 关系。",
      "并发与运行时：Go 的并发优势来自 goroutine 的低创建成本、可增长栈、调度器和标准库网络轮询，工程质量来自明确的生命周期管理。\n\n关键细节包括：\n\n- goroutine 初始栈较小并可增长，数量上升仍会消耗内存、调度队列、定时器、fd、锁和下游连接。\n- channel 适合表达所有权转移、事件通知和流水线背压；共享状态更适合用 `sync.Mutex`、`sync.RWMutex`、`atomic` 或封装良好的并发类型。\n- `context.Context` 负责跨 API 边界传递取消、超时和请求级值，服务端请求、数据库调用、RPC 调用和后台任务都需要定义退出路径。\n- Go 内存模型用 happens-before 描述可见性，channel send/receive、锁解锁/加锁、原子操作等同步事件决定并发读写能否被可靠观察。\n- 网络 I/O 通常由 runtime netpoller 与 goroutine 调度协作完成；阻塞系统调用、cgo 调用和长时间 CPU 循环会改变调度表现。\n\n生产判断的重点是“每个 goroutine 为什么存在、谁负责取消、资源何时释放、共享状态由什么同步原语保护”。",
      "内存、GC 与性能取舍：Go 的 GC 降低手工内存管理成本，同时把分配率、堆大小和扫描工作变成服务延迟与成本因素。\n\nGo GC 指南把 GC 成本和活跃堆、分配速度、目标堆增长关联起来；工程上要重点观察 allocation rate、heap live、heap goal、GC CPU fraction、STW 时间和 p99 延迟。高分配热点常来自频繁字符串拼接、临时 slice/map、JSON 编解码、大对象复制、接口装箱、闭包捕获和日志字段构造。\n\n优化顺序建议是：\n\n1. 先用 `pprof` 和 `-benchmem` 找到真实分配热点。\n2. 再减少请求路径临时对象，复用 buffer，预分配 slice/map，控制 JSON 和日志成本。\n3. 对高吞吐服务观察 `GOGC`、内存上限、容器限制、p99 延迟和 OOM 风险的联动。\n4. 对低延迟服务关注锁竞争、阻塞 profile、系统调用、网络等待和 GC 暂停共同形成的尾延迟。\n\nGo 的性能工程强调证据驱动：先 profile，再改代码，再压测验证收益和副作用。",
      "工程场景：Go 的优势在系统软件和服务端工程里最明显。\n\n- 后端 API/RPC：`net/http`、context、连接池、超时、限流、日志和 metrics 组成稳定服务骨架。\n- 云原生控制面：controller、operator、scheduler、agent 依赖 goroutine、channel、work queue 和强类型 API 管理复杂状态。\n- CLI 与运维工具：单二进制分发、交叉编译、标准库文件/网络能力和清晰错误返回适合自动化。\n- 代理与网关：大量连接、网络轮询、TLS、HTTP/2、HTTP/3 生态和 profile 工具适合流量路径诊断。\n- 数据处理与后台任务：pipeline、worker pool、context 取消、backpressure 和可观测指标决定吞吐与稳定性。\n\n选择 Go 时，团队通常看重工具链统一、部署轻、并发 I/O 成本低、维护成本可控和线上诊断路径成熟。",
      "边界与故障模式：Go 项目的线上问题常集中在生命周期、并发、内存、依赖和环境差异。\n\n- goroutine 泄漏：请求取消后后台 goroutine 仍在 channel、锁、ticker、网络读写或下游调用上等待。\n- 数据竞争：多个 goroutine 读写共享变量，测试环境偶发，线上表现为状态错乱、缓存污染或崩溃。\n- context 断链：调用方超时，底层数据库/RPC/队列操作继续执行，占用连接池和下游资源。\n- GC 压力：高分配导致 CPU 被 GC 消耗、heap 增长、尾延迟抖动，容器内存限制下更容易触发 OOM。\n- 构建漂移：Go 版本、`GOTOOLCHAIN`、`GOPROXY`、私有模块、`replace`、`go.work`、cgo 和目标平台差异导致本地与 CI 结果偏离。\n- 抽象失衡：过早引入大接口、包循环依赖、全局状态、panic 控制业务流程，会降低测试性和演进能力。",
      "排查实践：Go 服务排查要把应用日志、runtime 证据、系统资源和依赖状态放在同一时间窗里看。\n\n```bash\n# 1. 构建与依赖\ngo version\ngo env GOMOD GOWORK GOTOOLCHAIN GOPROXY GOSUMDB GOPRIVATE\ngo list -m all\ngo mod why -m example.com/dependency\n\n# 2. 并发与数据竞争\ngo test -race ./...\ncurl -s http://127.0.0.1:6060/debug/pprof/goroutine?debug=2 | head -80\n\n# 3. CPU、内存、锁与阻塞\ncurl -o /tmp/cpu.pprof http://127.0.0.1:6060/debug/pprof/profile?seconds=30\ngo tool pprof -top /tmp/cpu.pprof\ncurl -o /tmp/heap.pprof http://127.0.0.1:6060/debug/pprof/heap\ngo tool pprof -top /tmp/heap.pprof\n\n# 4. 压测与分配\ngo test -run=^$ -bench=. -benchmem ./...\nGODEBUG=gctrace=1 ./app\n```\n\n建议流程：\n\n1. 固定现象：版本、提交、Go 版本、流量、错误码、p95/p99、CPU、RSS、goroutine 数、GC 指标和依赖变更。\n2. 判断类型：构建失败、请求慢、CPU 高、内存涨、goroutine 涨、连接池耗尽、数据错乱或下游超时。\n3. 收集证据：pprof 看热点，goroutine dump 看等待点，race detector 查并发读写，trace 看调度与阻塞，`go env` 看环境差异。\n4. 做小实验：降低并发、缩短超时、关闭新特性、扩大连接池、调整 `GOGC`、复用对象或回滚依赖版本。\n5. 固化修复：补测试、基准、profile 回归、context 取消、资源关闭、监控告警和 CI 门禁。",
      "常见误区：成熟的 Go 理解来自“简单语法 + 明确边界 + 运行时证据”的组合。\n\n- goroutine 是轻量并发单元，工程上需要退出条件、取消传播和资源释放责任。\n- channel 是同步和通信工具，互斥锁、原子操作、条件变量和封装类型同样是 Go 并发设计的一部分。\n- `interface` 适合由调用方定义最小行为集合，具体实现可以保持简单结构体和函数。\n- `error` 是业务与系统边界的显式信号，错误包装、哨兵错误、类型断言和日志上下文共同决定可排查性。\n- `panic` 适合不可恢复的程序错误或初始化失败，业务失败通常用 `error` 和明确状态表达。\n- 性能优化要从 profile、benchmark 和线上指标出发，关注分配、锁、阻塞、系统调用、GC 和下游等待。",
      "面试与复盘抓手：回答 Go 概览题时，建议按“语言定位 -> 工具链 -> 并发运行时 -> 工程边界 -> 排查证据”展开。\n\n高频追问包括：\n\n- Go 相比传统后端语言的工程优势主要来自哪些设计？\n- 一个 Go 程序从源码到运行经历哪些阶段？`go.mod`、包图、编译器、链接器和 runtime 各自负责什么？\n- goroutine、channel、context、Mutex、WaitGroup 和 runtime scheduler 怎样协作？\n- Go 内存模型中的 happens-before 和数据竞争排查有什么关系？\n- GC 如何影响吞吐、内存和尾延迟？怎样用 pprof、`-benchmem`、GC 指标定位分配问题？\n- 线上 goroutine 数持续增长、CPU 飙高、heap 增长、请求超时分别怎么收集证据？\n- Go Module、`GOPROXY`、`GOSUMDB`、`GOPRIVATE`、`GOTOOLCHAIN` 如何影响构建可复现性？\n- Go 项目如何设计包边界、接口、错误处理和 CI 门禁，保持长期可维护？",
      "参考来源：本文用 Go Documentation 与语言规范校准语言定位和语义边界；用 cmd/go、Go Modules Reference 和标准库文档说明工具链、模块与库生态；用 Effective Go、Go CodeReviewComments、Uber Go Style Guide 组织工程实践；用 Go Diagnostics、Profiling Go Programs、Data Race Detector、GC Guide 和 Memory Model 说明诊断、并发、GC 与性能证据；用 Go Blog Pipelines and cancellation、Draveness Go 语言设计与实现补充并发生命周期、调度器和中文工程表达。",
    ],
    typicalProblems: [
      "Go 语言主要解决哪些工程交付问题？请从语言语义、工具链、模块系统、运行时和诊断能力回答。",
      "一个 Go 程序从源码到线上运行经历哪些阶段？`go.mod`、包图、编译链接和 runtime 分别承担什么职责？",
      "goroutine、channel、context、Mutex、WaitGroup、runtime scheduler 在高并发服务中怎样协作？",
      "Go 内存模型中的 happens-before、数据竞争和 `go test -race` 有什么关系？",
      "Go GC 如何影响吞吐、内存占用和尾延迟？怎样用 pprof、`-benchmem`、GC 指标定位分配热点？",
      "线上 goroutine 数持续增长、CPU 飙高、heap 增长、请求超时分别应该收集哪些证据？",
      "Go Module、`GOPROXY`、`GOSUMDB`、`GOPRIVATE`、`GOTOOLCHAIN` 如何影响构建可复现性和 CI 稳定性？",
      "Go 项目如何设计包边界、接口、错误处理、context 取消和 CI 门禁，保持长期可维护？",
      "Go 的常见生产故障包括哪些？如何区分代码并发问题、运行时压力、下游等待和构建环境差异？",
    ],
    commonIssues: ["goroutine 泄漏", "数据竞争", "context 取消链断开", "GC 分配压力", "pprof 暴露面未隔离", "私有模块拉取失败", "replace 或 go.work 残留", "工具链版本差异"],
    practiceAdvice: ["用 gofmt/go test/go vet 保持基础质量", "用 race detector 验证并发读写", "用 pprof、trace 和 runtime 指标定位性能问题", "用 context 管理请求超时和取消", "用小接口和清晰 package 边界组织代码", "在 CI 中固定 Go 版本、模块代理和私有模块策略"],
    commands: ["go version", "go env", "go mod init example.com/app", "go test ./...", "go test -race ./...", "go test -run=^$ -bench=. -benchmem ./...", "go tool pprof", "GODEBUG=gctrace=1 ./app"],
    prerequisites: [],
    related: ["go-toolchain", "go-module", "goroutine", "go-runtime"],
    order: 1,
  }),
  /* <!-- KG_REVIEWED: Go 工具链 | 2026-06-05 | source_count=10 --> */
  goPoint({
    id: "go-toolchain",
    zh: "Go 工具链",
    en: "Go Toolchain",
    area: "foundation",
    difficulty: "easy",
    summary: "Go 工具链用一个 go 命令串起格式化、依赖解析、测试、构建、安装、诊断和工具链版本选择。",
    concept: "用 go 命令统一完成源码管理、模块维护、测试、构建、运行和诊断。",
    focus: "go 命令、环境变量、包模式、模块缓存、构建缓存、交叉编译、工具链版本和 CI 可复现性",
    mechanism: "go 命令读取当前目录、包模式、go.mod、go.work、环境变量和构建标记，解析包图与模块图，复用 build cache 与 module cache，再调用编译器、汇编器、链接器、测试框架、vet、gofmt 或其他子工具输出结果。",
    scenarios: ["本地开发", "CI 构建", "交叉编译", "依赖诊断", "发布前质量检查"],
    pitfalls: "GOROOT、GOPATH、GOMODCACHE、GOCACHE、GOPROXY、GOSUMDB、GOPRIVATE、CGO_ENABLED、GOOS、GOARCH 和 GOTOOLCHAIN 的环境差异",
    sourceRefs: [
      "go-command",
      "go-toolchains",
      "go-blog-toolchain",
      "go-modules-reference",
      "go-managing-dependencies",
      "go-diagnostics",
      "go-effective-go",
      "go-docs",
      "go-how-to-write-code",
      "go101-toolchain",
    ],
    explanation: [
      "概念定位：Go 工具链解决的是“一个 Go 仓库怎样稳定完成开发、依赖解析、测试、构建、诊断和发布”的问题。\n\nGo Toolchain 由 `go` 命令、编译器、汇编器、链接器、标准工具、模块系统、缓存目录和版本选择机制组成。开发者日常看到的是 `go test ./...`、`go build`、`go mod tidy`、`go env`、`go vet` 等命令；工程系统真正依赖的是它们背后的包图解析、模块校验、构建缓存、测试缓存、目标平台选择和工具链版本切换。\n\n在真实团队里，Go 工具链直接决定本地开发反馈、CI 稳定性、私有模块拉取、跨平台产物、供应链校验和线上二进制可追踪性。",
      "准确定义：Go 工具链是一组由 `go` 命令统一调度的构建与诊断组件，它把源码包、模块版本、环境变量、缓存、编译链接和辅助工具串成可重复执行的工程流程。\n\n核心组成可以按职责拆开：\n\n- 入口命令：`go env`、`go list`、`go test`、`go build`、`go run`、`go install`、`go mod`、`go work`、`go tool`。\n- 编译链路：`compile` 处理 Go 源码，`asm` 处理汇编，`link` 生成可执行文件或测试二进制。\n- 质量工具：`gofmt` 固定格式，`go vet` 做官方静态检查，race detector 查运行期数据竞争，`pprof` 和 trace 支撑性能诊断。\n- 模块系统：`go.mod`、`go.sum`、`go.work`、MVS、`GOPROXY`、`GOSUMDB`、`GOPRIVATE` 决定依赖来源、版本和校验。\n- 缓存系统：`GOCACHE` 保存构建与测试缓存，`GOMODCACHE` 保存下载模块，二者共同影响速度、隔离性和 CI 可复现性。\n- 版本选择：Go 1.21 起，`go` 命令结合 `GOTOOLCHAIN`、`go` 行和 `toolchain` 行选择合适工具链。",
      "心智模型：把 Go 工具链看成“项目驾驶舱”。\n\n你给它输入当前目录、包模式、模块文件、环境变量和命令参数，它会先确认工作区和依赖图，再决定目标平台与工具链版本，随后复用缓存并调用编译、测试、链接或诊断组件，最后输出二进制、测试报告、包信息或排查证据。\n\n这套模型能帮助新手把命令串起来，也能帮助经验工程师定位线上差异：同一份源码在本地和 CI 表现不同，通常要同时检查 Go 版本、`GOTOOLCHAIN`、`GOWORK`、`GOFLAGS`、`GOOS/GOARCH`、`CGO_ENABLED`、私有模块配置、缓存状态和构建标签。",
      "主流程机制：执行一次 `go test ./...` 或 `go build ./...` 时，工具链大致按这些步骤运行。\n\n1. 定位工作区：从当前目录向上寻找 `go.mod`，必要时读取 `go.work`，并确定主模块、工作区模块和包模式。\n2. 选择工具链：读取本机 `go` 命令版本、`GOTOOLCHAIN`、`go.mod` 或 `go.work` 中的 `go`/`toolchain` 行；Go 1.21+ 会在需要时切换到满足要求的工具链。\n3. 展开包图：根据 `./...`、导入路径、构建标签、文件后缀、`GOOS`、`GOARCH` 和 cgo 状态选择参与构建的文件与包。\n4. 解析模块图：读取主模块与依赖模块的 `go.mod`，按 MVS 生成 build list，结合 `replace`、`exclude`、`retract` 和 workspace 规则确定实际版本。\n5. 下载与校验：通过 `GOPROXY` 或源码仓库获取模块，用 `GOSUMDB` 和 `go.sum` 校验公开模块，用 `GOPRIVATE` 等变量隔离私有模块策略。\n6. 命中缓存或编译：用 `GOCACHE` 复用包编译结果、测试结果和部分工具输出；缓存失效时调用编译器、汇编器和链接器。\n7. 运行测试或产生产物：`go test` 生成并运行测试二进制，`go build` 生成目标产物，`go run` 编译临时二进制并执行，`go list` 输出结构化包与模块信息。\n8. 记录证据：命令输出、`go env`、`go list -json`、`go version -m`、profile、trace 和测试 JSON 构成后续排查依据。\n\n```bash\ngo env GOMOD GOWORK GOFLAGS GOTOOLCHAIN GOPROXY GOSUMDB GOPRIVATE GOMODCACHE GOCACHE\ngo list -deps -json ./cmd/server\ngo list -m all\ngo mod graph\ngo mod why -m example.com/dependency\ngo test -json ./... > test-report.json\ngo build -trimpath -ldflags='-s -w -X main.version=v1.2.3' ./cmd/server\ngo version -m ./server\n```",
      "关键命令速查：Go 工具链常见命令要按输出物理解。\n\n```text\ngo env        查看和设置 Go 环境变量，排查版本、缓存、代理、私有模块和目标平台。\ngo list       输出包图或模块图，适合 CI、脚本、依赖审计和构建范围判断。\ngo mod tidy   根据源码导入整理 go.mod/go.sum，清理无效依赖并补齐测试依赖。\ngo test       编译并运行测试二进制，可叠加 -race、-cover、-bench、-json。\ngo build      编译包或命令，生成二进制或验证库包可构建。\ngo install    构建并安装命令；带 @version 时按指定模块版本安装工具。\ngo run        构建临时命令并执行，适合示例、脚本和快速验证。\ngo vet        运行官方分析器，发现格式化字符串、复制锁、结构体标签等高风险模式。\ngo tool       调用底层工具；Go 1.24+ 可运行 go.mod tool 指令声明的工具。\n```\n\n实战中可以把 `go list` 当作“看清世界”的命令：它能告诉你实际包、文件、依赖、模块、编译条件和错误来源。",
      "版本选择与前向兼容：Go 1.21 之后，工具链版本成为项目可复现性的显式输入。\n\n`go.mod` 或 `go.work` 的 `go` 行定义模块需要的最低 Go 语言版本与语义边界，`toolchain` 行可以给出推荐工具链。`GOTOOLCHAIN` 控制当前 `go` 命令是否自动选择或下载满足要求的工具链。官方工具链管理机制让模块可以声明“需要更新的 Go”，从而减少新语言语义、新标准库或新 bugfix 在旧工具链下被误用的风险。\n\n常见配置判断：\n\n- `GOTOOLCHAIN=auto`：允许 `go` 命令根据模块要求切换工具链，适合多数开发环境。\n- `GOTOOLCHAIN=local`：只使用当前本地工具链，适合严格离线或受控构建环境。\n- `toolchain go1.24.3`：给主模块声明推荐工具链，团队和 CI 可据此统一版本。\n- `go 1.23`：声明模块最低 Go 版本，并影响语言语义、模块图裁剪和部分工具行为。\n\nCI 中要把 `go version`、`go env GOTOOLCHAIN` 和构建镜像版本一起记录，发布产物再用 `go version -m` 验证二进制携带的模块与构建信息。",
      "模块、缓存与私有依赖：工具链稳定性通常卡在依赖来源、校验策略和缓存隔离上。\n\n`GOMODCACHE` 保存已下载模块，`GOCACHE` 保存构建和测试缓存。公开模块默认通过 `GOPROXY` 获取，并通过 checksum database 校验；私有模块通常需要设置 `GOPRIVATE`，让匹配路径跳过公开代理和公开校验数据库，并配合 Git 凭据或企业代理访问。\n\n排查依赖问题时优先看这些信号：\n\n- `go env GOPROXY GOSUMDB GOPRIVATE GONOPROXY GONOSUMDB`：判断模块从哪里下载、是否校验、私有路径是否匹配。\n- `go env GOMOD GOWORK`：判断当前命令是否进入正确模块或 workspace。\n- `go list -m -json all`：查看最终版本、替换来源和错误信息。\n- `go mod why -m <module>`：解释某个模块为什么进入 build list。\n- `go clean -modcache` 与 `go clean -cache -testcache`：清理模块缓存、构建缓存和测试缓存，适合验证缓存污染或 CI 重放问题。\n\n`replace` 和 `go.work` 很适合本地联调；团队协作时要明确提交策略，避免 CI 使用本地路径、fork 版本或工作区覆盖结果。",
      "交叉编译与发布：Go 的静态链接倾向和标准工具链让跨平台产物生成很顺滑，发布质量仍取决于目标平台、cgo、元数据和验证方式。\n\n常见发布脚本会显式设置 `GOOS`、`GOARCH`、`CGO_ENABLED`，再用 `-trimpath` 去掉本地路径，用 `-ldflags` 注入版本、提交和构建时间。纯 Go 程序跨平台通常直接可编译；依赖 cgo、系统库、SQLite、Kerberos、图像处理库或平台专属 syscall 时，还需要目标平台 C 编译器、头文件、动态库和运行时环境匹配。\n\n```bash\n# Linux amd64 服务端产物\nGOOS=linux GOARCH=amd64 CGO_ENABLED=0 \\\n  go build -trimpath -ldflags='-s -w -X main.version=v1.2.3 -X main.commit=abc123' \\\n  -o dist/server-linux-amd64 ./cmd/server\n\n# macOS arm64 本地工具\nGOOS=darwin GOARCH=arm64 go build -o dist/tool-darwin-arm64 ./cmd/tool\n\n# 查看二进制中的构建和模块信息\ngo version -m dist/server-linux-amd64\n```\n\n发布前建议同时运行目标平台烟测、`go test ./...`、关键包基准、镜像构建验证和二进制元数据检查。",
      "生产排查实践：Go 工具链排查要先固定“命令、环境、模块、版本、缓存、平台”六类证据。\n\n```bash\n# 构建环境快照\ngo version\ngo env -json > go-env.json\ngo env GOMOD GOWORK GOFLAGS GOTOOLCHAIN GOOS GOARCH CGO_ENABLED GOPROXY GOSUMDB GOPRIVATE\n\n# 包图与模块图\ngo list -deps ./cmd/server\ngo list -json ./cmd/server > package.json\ngo list -m -json all > modules.json\n\n# CI 门禁\ngofmt -w .\ngo test ./...\ngo test -race ./...\ngo vet ./...\n\n# 缓存验证\ngo clean -cache -testcache\ngo test ./...\n```\n\n建议流程：\n\n1. 复现命令：记录完整命令、工作目录、环境变量、Go 版本和容器镜像。\n2. 定位范围：用 `go list` 判断实际参与构建的包、文件、tags 和模块版本。\n3. 比对环境：对比本地与 CI 的 `go env -json`、`go version`、`go.work`、`GOFLAGS` 和私有模块配置。\n4. 排除缓存：清理构建缓存、测试缓存或模块缓存后重跑，观察错误是否随缓存变化。\n5. 验证产物：用 `go version -m`、目标平台烟测和依赖扫描确认二进制来源与运行环境。",
      "边界与故障模式：Go 工具链问题通常表现为“本地能过、CI 失败”“升级 Go 后行为变化”“依赖版本突然变化”“跨平台编译失败”。\n\n- 工具链漂移：本地 `GOTOOLCHAIN=auto` 下载了新版本，CI 镜像仍固定旧版本，导致语义、标准库或 vet 结果不同。\n- 工作区覆盖：`go.work` 把本地多个模块串起来，CI 使用单模块解析，依赖路径和版本产生差异。\n- 私有模块策略缺口：`GOPRIVATE` 匹配范围过窄，命令把私有路径发往公开代理或 checksum database，表现为 404、认证失败或校验失败。\n- 缓存污染：测试缓存复用了过期结果，模块缓存保存损坏下载，构建缓存掩盖编译参数变化。\n- cgo 与平台差异：`CGO_ENABLED`、目标 C 编译器、系统头文件、动态库和 libc 版本改变链接结果。\n- `replace` 残留：本地路径替换进入提交或发布流程，依赖方和 CI 无法获得同一模块内容。\n- 构建标签遗漏：`-tags`、文件名后缀、`GOOS/GOARCH` 组合改变参与编译的文件，导致某些平台才暴露错误。",
      "常见误区：成熟的 Go 工具链使用方式，是把命令结果和环境输入一起管理。\n\n- `go build` 负责证明包可构建，完整质量门禁还需要 `go test`、`go vet`、格式检查、race 检查和关键平台组合。\n- `go run` 适合快速执行，发布流程应生成明确命名、带版本元数据、经过目标平台验证的产物。\n- `go env` 是排查工具链问题的第一证据，尤其是 `GOMOD`、`GOWORK`、`GOTOOLCHAIN`、`GOFLAGS`、`GOCACHE`、`GOMODCACHE` 和模块代理配置。\n- 缓存提升速度，也要求 CI 明确缓存键、Go 版本、目标平台、模块文件和清理策略。\n- 私有模块需要同时处理代理、校验数据库和凭据，单纯能在本机 `git clone` 成功只能说明源码仓库可访问。\n- 工具依赖适合用 `go install <module>@<version>` 或 Go 1.24+ `tool` 指令固定，团队脚本应避免依赖开发者机器上的随机版本。",
      "面试与复盘抓手：回答 Go 工具链题时，建议按“入口命令 -> 模块/包解析 -> 缓存 -> 版本选择 -> CI/发布 -> 排查证据”展开。\n\n高频追问包括：\n\n- `go build`、`go run`、`go install`、`go test` 的输入、输出和副作用分别是什么？\n- `go` 命令如何根据 `go.mod`、`go.work`、包模式和 build tags 决定构建范围？\n- `GOCACHE`、`GOMODCACHE`、`GOPROXY`、`GOSUMDB`、`GOPRIVATE` 各自影响什么？\n- Go 1.21+ 的 `GOTOOLCHAIN`、`go` 行和 `toolchain` 行如何影响工具链选择？\n- CI 中怎样设计 Go 项目的格式化、测试、静态检查、race 检查、构建和缓存策略？\n- 私有模块拉取失败时，应怎样区分凭据、代理、checksum database、模块路径和版本标签问题？\n- 交叉编译和 cgo 出错时，怎样判断是 Go 目标平台、C 工具链、系统库还是构建标签问题？\n- 线上二进制来源存疑时，怎样用 `go version -m`、构建日志、模块清单和镜像元数据还原构建过程？",
      "参考来源：本文用 `cmd/go` 官方命令文档校准命令语义、包模式、环境变量和缓存行为；用 Go Toolchains 与 Go Blog 的 Go 1.21 工具链管理文章说明 `GOTOOLCHAIN`、`go` 行和 `toolchain` 行；用 Go Modules Reference 与 Managing dependencies 校准模块、校验、私有依赖和 Go 1.24+ 工具依赖；用 Go Diagnostics、Effective Go、How to Write Go Code 和 Go101 工具链文章补充开发、CI、排查和中文工程表达。",
    ],
    typicalProblems: [
      "Go 工具链由哪些组件组成？`go` 命令、编译器、链接器、模块系统和缓存分别承担什么职责？",
      "`go build`、`go run`、`go install`、`go test`、`go list` 的输入、输出和典型副作用分别是什么？",
      "`go` 命令如何根据 `go.mod`、`go.work`、包模式、build tags、`GOOS/GOARCH` 和 cgo 状态决定构建范围？",
      "`GOCACHE`、`GOMODCACHE`、`GOPROXY`、`GOSUMDB`、`GOPRIVATE`、`GOFLAGS` 分别影响什么？",
      "Go 1.21+ 的 `GOTOOLCHAIN`、`go` 行和 `toolchain` 行如何影响工具链版本选择和前向兼容？",
      "CI 中如何设计格式化、测试、静态检查、race 检查、构建、缓存和私有模块策略？",
      "私有模块拉取失败时，怎样区分凭据、代理、checksum database、模块路径和版本标签问题？",
      "交叉编译或 cgo 构建失败时，应怎样检查目标平台、C 编译器、系统库、构建标签和链接参数？",
      "线上二进制来源存疑时，怎样用 `go version -m`、构建日志、模块清单和镜像元数据还原构建过程？",
    ],
    commonIssues: ["工具链版本漂移", "私有模块拉取失败", "checksum database 校验失败", "构建缓存或测试缓存复用过期结果", "replace 或 go.work 残留", "cgo 交叉编译失败", "构建标签遗漏", "CI 环境变量缺失"],
    practiceAdvice: ["用 go env -json 固化关键环境判断", "用 go list 查看包图和模块图", "用 go test ./... 与 go vet ./... 做基础门禁", "用 go test -race 覆盖并发敏感路径", "用 go version -m 检查产物构建信息", "在 CI 中显式设置工具链版本、缓存键和私有模块策略"],
    commands: ["go version", "go env -json", "go list ./...", "go list -m -json all", "go mod graph", "go mod why -m example.com/dependency", "go build -trimpath ./...", "go test -json ./...", "go test -race ./...", "go vet ./...", "go version -m ./app"],
    prerequisites: ["go-overview"],
    related: ["go-module", "gofmt", "go-test", "go-vet"],
    order: 2,
  }),
  /* <!-- KG_REVIEWED: gofmt | 2026-06-05 | source_count=6 --> */
  goPoint({
    id: "gofmt",
    zh: "gofmt",
    en: "gofmt",
    area: "tooling",
    difficulty: "easy",
    summary: "gofmt 用官方语法树打印规则重写 Go 源码，让格式稳定、评审聚焦逻辑，并支撑批量机械改造。",
    concept: "用官方标准格式统一 Go 源码布局，减少风格协商、审查噪音和代码生成差异。",
    focus: "语法树格式化、制表符缩进、注释保留、包级 go fmt、CI 差异检查、简化规则、机械重写和 goimports 边界",
    mechanism: "gofmt 读取 Go 源文件或标准输入，解析成 Go 语法树并按固定打印规则输出；go fmt 会按包调用 gofmt 写回源文件；-w 写回文件，-l 列出格式不一致文件，-d 输出 diff，-s 执行简单化简，-r 按模式规则做机械重写。",
    scenarios: ["提交前格式化", "CI 格式门禁", "团队协作", "生成代码整理", "大规模机械重写", "迁移旧仓库"],
    pitfalls: "编辑器保存钩子、CI 检查范围、生成代码格式、注释位置、-w 写回范围、-r 重写规则验证、go fmt 包入口和 goimports/import 管理边界",
    sourceRefs: ["go-gofmt-command", "go-blog-gofmt", "go-command", "goimports-command", "go-doc-comments", "gopl-zh-go-tools"],
    explanation: [
      "概念定位：gofmt 解决的是“Go 代码格式由谁决定、怎样稳定执行、如何让评审只看语义变化”的问题。\n\nGo 团队把格式化做成官方工具，让团队成员、编辑器、代码生成器、CI 和批量重构脚本共享同一套格式输出。它在真实工程里常出现在保存文件、提交前检查、Pull Request 门禁、生成代码后处理、迁移旧仓库和大规模机械改造中。\n\n对新手来说，gofmt 是写 Go 的基础习惯；对经验工程师来说，gofmt 是降低协作噪音、控制重构 diff、统一生成器输出和建立自动化质量门禁的工具链基础。",
      "准确定义：gofmt 是 Go 官方源码格式化命令，它读取 Go 源码，构建语法结构，再按标准打印规则输出格式化后的源码。\n\n关键边界可以这样记：\n\n- `gofmt` 是文件级工具，可读取文件、目录或标准输入。\n- `go fmt` 是 `go` 命令提供的包级入口，会对导入路径或包模式匹配到的包运行 `gofmt -w`。\n- `-w` 写回文件，`-l` 列出需要格式化的文件，`-d` 输出统一 diff，`-s` 做简单语法化简，`-r` 按模式执行机械重写。\n- `gofmt` 管格式布局，`goimports` 在格式化之外管理 import 增删和分组。\n- `go vet`、staticcheck、测试和 race detector 负责语义风险、可疑模式与运行行为验证。",
      "心智模型：把 gofmt 看成“源码排版的编译器后端”。\n\n普通排版工具常按文本处理空格和换行，gofmt 先理解 Go 源码结构，再把语法树重新打印成统一文本。这个模型解释了两个现象：第一，gofmt 输出稳定，团队可以停止争论缩进、括号、对齐和空行；第二，注释位置、空行分组、原始字符串和构建标签仍然会影响可读性，开发者需要写出清晰的源码结构和注释位置。\n\n工程上更重要的是可重复性：同一份有效 Go 源码在同一 gofmt 规则下会收敛到一致格式，适合放进编辑器、生成器、脚本和 CI。",
      "主流程机制：一次格式化通常经历这些步骤。\n\n1. 输入收集：开发者传入 `.go` 文件、目录、包入口，或从标准输入传入源码片段。\n2. 解析源码：gofmt 读取 Go 语法、注释和位置信息，形成可打印的语法结构。\n3. 应用规则：工具按 Go 官方格式规则处理缩进、换行、对齐、空格、复合字面量、函数签名和注释布局。\n4. 可选化简：开启 `-s` 后，gofmt 会做安全的简单源码化简，例如把冗余切片表达式或复合字面量写法收敛得更短。\n5. 可选重写：开启 `-r 'pattern -> replacement'` 后，工具基于表达式模式做机械替换，适合 API 迁移前的小步验证。\n6. 输出结果：默认写到标准输出；`-w` 写回源文件；`-l` 只列文件名；`-d` 输出 diff，便于 CI 和评审查看。\n\n```bash\n# 格式化单个文件并写回\ngofmt -w internal/service/user.go\n\n# 列出当前目录下需要格式化的 Go 文件\ngofmt -l .\n\n# 在 CI 中输出格式差异\ngofmt -d .\n\n# 包级格式化，等价于对匹配包运行 gofmt -w\ngo fmt ./...\n\n# 简单化简并写回\ngofmt -s -w .\n```",
      "命令与参数速查：日常使用要按“是否写回、是否检查、是否改语义形状”区分。\n\n```text\ngofmt file.go          读取文件并把格式化结果写到标准输出。\ngofmt -w file.go       直接写回文件，适合编辑器保存和提交前处理。\ngofmt -l .             列出格式与 gofmt 输出不同的文件，适合 CI 门禁。\ngofmt -d .             输出 diff，适合让失败日志直接展示修改建议。\ngofmt -s -w .          在格式化基础上做简单语法化简。\ngofmt -r 'a[b:len(a)] -> a[b:]' -w .  按模式做机械重写。\ngo fmt ./...           以包为单位运行格式化，适合 Go 项目常规入口。\ngoimports -w .         格式化并维护 import，适合编辑器保存动作。\n```\n\n团队推荐把 `go fmt ./...` 或 `gofmt -w` 放进本地自动保存，把 `gofmt -l` 或 `gofmt -d` 放进 CI，把 `gofmt -r` 放在有 review 边界的重构分支里。",
      "工程场景：gofmt 的价值来自自动化一致性。\n\n- 代码评审：格式差异自动收敛，评审者把注意力放在 API、控制流、错误处理、并发和测试上。\n- 代码生成：protobuf、mock、ORM、OpenAPI 和自研生成器输出后立即 gofmt，生成结果更稳定。\n- 大仓迁移：旧项目接入 Go 标准格式后，后续 diff 更容易审查。\n- 机械重写：用 `-r` 做小范围可验证替换，再跑 gofmt、go test 和 go vet 控制风险。\n- 文档质量：Go Doc Comments 的格式规则与 gofmt 兼容，注释段落、列表和代码块更容易被 pkg.go.dev 正确呈现。\n- 团队规范：gofmt 把风格规范变成可执行命令，适合新成员、多人协作和跨 IDE 工作流。",
      "go fmt、gofmt、goimports 与 go vet 的边界：这几类工具都常出现在提交前，但职责不同。\n\n- `gofmt`：对 Go 源码做官方格式化，核心输入是文件、目录或标准输入。\n- `go fmt`：通过 `go` 命令按包运行格式化，适合仓库级常规操作。\n- `goimports`：在 gofmt 的基础上增删 import、整理分组，并可按导入路径补齐缺失包。\n- `go vet`：基于语法和类型信息检查可疑代码模式，例如格式化字符串、复制锁、结构体标签等。\n- 测试与静态分析：`go test`、race detector、staticcheck 用来验证行为、并发和更广规则集合。\n\n实际项目常用组合是：编辑器保存时跑 `goimports`，CI 中跑格式检查、`go test ./...` 和 `go vet ./...`，并在并发敏感包上补充 `go test -race`。",
      "边界与故障模式：gofmt 很稳定，工程失败多来自接入方式和使用范围。\n\n- CI 范围偏小：只检查某个目录，生成代码或子模块中的未格式化文件进入主分支。\n- 保存钩子不一致：不同 IDE 分别运行 gofmt、goimports 或自定义格式化器，import diff 来回震荡。\n- `-w` 范围过大：格式化全仓叠加逻辑修改，review 难以聚焦真实语义变化。\n- `-r` 规则过宽：机械重写命中过多上下文，API 迁移引入行为差异或可读性下降。\n- 生成器输出漂移：生成代码模板改变但缺少 gofmt，导致同一输入跨版本产生多余 diff。\n- 注释布局粗糙：gofmt 会保留注释语义位置，注释挂错声明、列表缩进混乱或代码块围栏缺失会影响文档呈现。\n- 构建标签位置错误：Go 文件开头的 build constraints 需要放在正确区域，格式化前后都要保持工具可识别。",
      "排查与落地实践：格式门禁要让失败原因清晰、修复命令直接、diff 边界可审查。\n\n```bash\n# 推荐的 CI 检查脚本：列出未格式化文件并失败\nfiles=$(gofmt -l .)\ntest -z \"$files\" || { echo \"$files\"; exit 1; }\n\n# 带 diff 的 CI 输出，便于开发者直接定位\nif [ -n \"$(gofmt -l .)\" ]; then\n  gofmt -d .\n  exit 1\nfi\n\n# 提交前本地收敛\n gofmt -w .\n go test ./...\n go vet ./...\n```\n\n推荐流程：\n\n1. 本地编辑器保存时运行 `goimports` 或 `gofmt`，团队统一一个保存动作。\n2. 提交前运行 `gofmt -w .` 或 `go fmt ./...`，再运行 `go test ./...`。\n3. CI 用 `gofmt -l` 或 `gofmt -d` 检查全仓，失败日志给出文件名或 diff。\n4. 生成代码在生成脚本末尾执行 gofmt，保证生成物稳定。\n5. 批量格式化和逻辑改动分成不同提交，便于审查和回滚。\n6. 使用 `-r` 前先在小目录跑 `gofmt -r 'old -> new' -d`，确认 diff 后再写回。",
      "性能、安全与设计取舍：gofmt 的运行成本通常很低，工程取舍集中在流程设计。\n\n格式化提交会触碰大量文件，短期内影响 blame 和 review；长期看统一格式能降低每次评审的认知成本。保存时运行 goimports 能减少导入维护工作，也可能因为本地模块缓存、工作区和依赖解析状态产生额外 import 变化。CI 中只做 `gofmt -l` 速度快、输出短；`gofmt -d` 输出更可执行，但大范围差异会让日志变长。\n\n安全角度主要关注供应链和脚本边界：CI 使用官方 Go 工具链或固定版本容器；批量脚本先输出 diff；自动格式化机器人只提交格式文件；生成代码和第三方导入整理在明确目录内执行。",
      "常见误区：成熟的 gofmt 使用方式，是把格式化变成稳定流水线的一环。\n\n- gofmt 统一源码布局，代码质量仍由设计、测试、静态检查、review 和线上证据共同保证。\n- `go fmt` 适合按包处理项目，`gofmt` 适合文件、目录、标准输入和更细参数控制。\n- goimports 覆盖 import 管理场景，团队保存动作要统一到同一工具和版本策略。\n- `-r` 是机械重写工具，适合配合 diff、测试和小步提交完成 API 迁移。\n- 生成代码应在生成阶段完成格式化，减少开发者手工改生成物的机会。\n- 注释、空行和声明分组仍需要人工表达清楚，gofmt 会把清晰结构打印得更稳定。",
      "面试与复盘抓手：回答 gofmt 题时，建议按“官方格式 -> AST 打印 -> 命令参数 -> 工程接入 -> 工具边界 -> 失败排查”展开。\n\n高频追问包括：\n\n- gofmt 解决了 Go 团队协作中的哪些问题？为什么它能减少代码评审噪音？\n- gofmt 和 go fmt 的关系是什么？文件级入口和包级入口如何选择？\n- gofmt 的 `-w`、`-l`、`-d`、`-s`、`-r` 分别适合什么场景？\n- gofmt 为什么比普通文本格式化更稳定？语法树打印模型有什么工程意义？\n- gofmt、goimports、go vet、staticcheck 和 go test 的职责怎样划分？\n- CI 中如何检查格式并给出可执行反馈？本地和 CI 格式结果不同怎样排查？\n- 代码生成、大规模重构、API 迁移中如何控制 gofmt 带来的 diff 范围？\n- 使用 `gofmt -r` 做机械重写时，怎样设计规则、验证 diff 和控制行为变化风险？",
      "参考来源：本文用 `cmd/gofmt` 官方文档校准参数语义和输入输出行为；用 Go Blog 的 gofmt 文章说明官方格式化的工程价值、`-r` 机械重写和减少风格争论的背景；用 `cmd/go` 文档说明 `go fmt` 包级入口；用 `goimports` 官方文档说明 import 管理边界；用 Go Doc Comments 校准注释和文档呈现；用《Go 语言圣经》中文工具章节补充中文读者熟悉的工程表达。",
    ],
    typicalProblems: [
      "gofmt 解决了 Go 团队协作中的哪些问题？为什么它能减少代码评审噪音？",
      "gofmt 的工作机制是什么？为什么语法树打印模型比普通文本排版更稳定？",
      "gofmt 和 go fmt 的关系是什么？文件级入口和包级入口分别适合什么场景？",
      "`gofmt -w`、`-l`、`-d`、`-s`、`-r` 的输入、输出和风险边界分别是什么？",
      "gofmt、goimports、go vet、staticcheck 和 go test 分别解决什么工程问题？",
      "CI 中如何检查 Go 代码已经格式化，并让失败日志给出可执行反馈？",
      "生成代码、仓库迁移或大规模重构后，如何控制 gofmt 带来的 diff 范围？",
      "使用 `gofmt -r` 做机械重写时，怎样设计规则、验证结果并控制行为变化风险？",
      "本地和 CI 格式结果不同，应该检查编辑器保存动作、工具版本、工作目录、生成代码和 import 管理哪些证据？",
    ],
    commonIssues: ["编辑器格式化配置缺失", "CI 检查范围偏小", "生成代码格式漂移", "-w 写回范围过大", "goimports 与 gofmt 保存动作不一致", "机械重写规则验证不足", "注释位置影响文档呈现", "格式化提交混入逻辑修改"],
    practiceAdvice: ["保存时统一运行 goimports 或 gofmt", "CI 使用 gofmt -l 或 gofmt -d 检查全仓库", "批量修改前先用 -d 查看 diff", "生成代码后立即格式化", "格式化提交和逻辑提交分开审查", "使用 -r 前先小范围验证并跑测试"],
    commands: ["gofmt -w file.go", "gofmt -l .", "gofmt -d .", "gofmt -s -w .", "gofmt -r 'a[b:len(a)] -> a[b:]' -d .", "go fmt ./...", "goimports -w ."],
    prerequisites: ["go-toolchain"],
    related: ["go-vet", "package", "import", "go-test"],
    order: 3,
  }),
  /* <!-- KG_REVIEWED: Go Module | 2026-06-05 | source_count=11 --> */
  goPoint({
    id: "go-module",
    zh: "Go Module",
    en: "Go Module",
    area: "foundation",
    difficulty: "easy",
    summary: "Go Module 用模块路径、go.mod、MVS、go.sum 和代理校验链把源码包与依赖版本固定成可复现的构建单元。",
    concept: "用模块路径、版本选择、校验数据和发布规则管理 Go 项目的依赖、导入命名空间与版本边界。",
    focus: "module path、go.mod、require、replace、exclude、retract、语义化版本、MVS、go.sum、GOPROXY 和私有模块",
    mechanism: "go 命令从当前模块的 go.mod 读取 module、go、toolchain、require、replace、exclude、retract 等指令，加载依赖模块的 go.mod，按最小版本选择生成 build list，并用 go.sum 与校验和数据库校验下载内容。",
    scenarios: ["新项目初始化", "依赖升级", "私有模块接入", "可复现构建", "库发布与多版本维护"],
    pitfalls: "模块路径变更、v2+ 路径后缀、replace 残留、间接依赖升级、go.sum 冲突、私有仓库访问和 workspace 影响",
    sourceRefs: [
      "go-modules-reference",
      "go-gomod-reference",
      "go-managing-dependencies",
      "go-create-module",
      "go-blog-using-modules",
      "go-blog-publishing-modules",
      "go-blog-v2-modules",
      "go-mvs",
      "go-command",
      "go-wiki-modules",
      "eddycjy-go-modules",
    ],
    explanation: [
      "概念定位：Go Module 解决的是“同一份 Go 源码在不同机器、不同 CI、不同时间构建时怎样得到同一组依赖”的问题。\n\nGo Module 是 Go 的依赖管理与发布边界。一个 module 是一组一起发布、一起版本化的 package；根目录 `go.mod` 声明模块路径、Go 版本、工具链建议和依赖要求；`go.sum` 记录模块内容与 `go.mod` 文件的校验数据。它在真实系统里出现在新项目初始化、微服务依赖升级、私有仓库接入、公共库发布、供应链审计、CI 可复现构建和线上二进制溯源中。\n\n新手先把 Module 看成“项目的依赖合约”；经验工程师继续关注 MVS、语义导入版本、校验数据库、私有模块策略、workspace 覆盖、`replace` 风险和发布兼容性。",
      "准确定义：Go Module 用 `module path` 建立导入命名空间，用语义化版本标识发布结果，用 `go.mod` 描述最低依赖要求，用 Minimal Version Selection（MVS）生成最终 build list，用 `go.sum` 与 checksum database 校验下载内容。\n\n关键对象可以这样区分：\n\n- `package`：编译和导入的代码单元，同一目录通常对应一个包。\n- `import path`：源码里的导入路径，通常由 `module path` 加子目录组成。\n- `module`：版本化发布单元，包含一个或多个 package。\n- `go.mod`：模块的声明文件，记录 `module`、`go`、`toolchain`、`require`、`replace`、`exclude`、`retract` 等指令。\n- `go.sum`：校验数据文件，记录已经参与解析或构建的模块版本及其 `go.mod` 校验和。\n- `build list`：当前命令实际使用的一组模块版本，由主模块、workspace 和依赖图共同决定。",
      "心智模型：把一次 Go 构建想成一次“带签名的采购流程”。\n\n`go.mod` 像采购清单：主模块说自己需要哪些模块、最低版本是多少、哪些来源临时替换。MVS 像采购规则：同一个模块只选一个版本，并选择依赖图里出现的最高最低要求版本。`GOPROXY` 像供应商；`GOSUMDB` 和 `go.sum` 像验签记录；`GOPRIVATE` 像内部供应商白名单。\n\n这个模型能解释多数故障：依赖版本异常看 MVS 输入，下载失败看代理与凭据，校验失败看内容来源，CI 与本地结果不同看 `go env`、`go.work`、`replace` 和缓存。",
      "主流程机制：执行 `go test ./...`、`go build ./...` 或 `go mod tidy` 时，Module 机制大致按这些步骤工作。\n\n1. 定位主模块：`go` 命令从当前目录向上寻找 `go.mod`，并读取可能存在的 `go.work`，确定主模块与 workspace 模块集合。\n2. 读取模块合约：解析主模块 `go.mod` 里的 `module`、`go`、`toolchain`、`require`、`replace`、`exclude`、`retract` 等指令。\n3. 展开导入需求：扫描参与构建和测试的 package，解析 import path，找到能提供这些 package 的模块。\n4. 加载模块图：读取依赖模块的 `go.mod`，收集每个模块对其他模块的最低版本要求。\n5. 执行 MVS：对同一个模块路径选择依赖图中需要的最高版本，形成单一 build list。\n6. 应用本地覆盖：主模块或 workspace 中的 `replace` 决定某些模块改用本地路径、fork 路径或指定版本。\n7. 下载与校验：通过 `GOPROXY` 或 VCS 获取模块；公开模块通常经过 checksum database 校验，私有模块按 `GOPRIVATE`、`GONOPROXY`、`GONOSUMDB` 策略处理。\n8. 更新文件：`go get`、`go mod tidy` 等命令可能更新 `go.mod` 与 `go.sum`；普通构建命令在需要补齐缺失校验时也可能触碰 `go.sum`。\n9. 输出证据：命令输出、`go list -m -json all`、`go mod graph`、`go env` 和最终二进制的 `go version -m` 支撑后续排查。",
      "go.mod 指令速查：`go.mod` 的每一行都在改变依赖解析或发布语义。\n\n```go\nmodule example.com/team/payment\n\ngo 1.23.0\ntoolchain go1.23.4\n\nrequire (\n    github.com/gin-gonic/gin v1.10.0\n    golang.org/x/sync v0.10.0 // indirect\n)\n\nreplace example.com/internal/common => ../common\nreplace example.com/fork/lib => github.com/team/lib v1.2.4\n\nexclude github.com/old/lib v1.2.0\n\nretract v1.4.0 // contains a critical regression\n```\n\n- `module` 定义模块路径，也是导入路径前缀和发布身份。\n- `go` 定义模块面向的 Go 语言版本语义和模块图裁剪规则。\n- `toolchain` 为 Go 1.21+ 工具链管理提供建议版本。\n- `require` 表示最低依赖版本，`// indirect` 表示该依赖由其他依赖或测试链路引入。\n- `replace` 只在主模块或 workspace 生效，适合本地联调、临时 fork 和依赖修复验证。\n- `exclude` 排除主模块构建中某个有问题的版本。\n- `retract` 由模块作者发布，用来提示使用者避开有问题或误发的版本。",
      "版本与发布边界：Go Module 的版本判断同时受语义化版本、模块路径和 MVS 约束。\n\n公共库发布时要守住三条线：\n\n1. `v0` 表示 API 仍在快速变化；`v1` 是稳定主线；`v2+` 需要在 module path 和 import path 中加入 `/v2`、`/v3` 这类 major version suffix。\n2. MVS 选择的是满足依赖图最低要求的版本集合；当多个依赖要求同一模块的不同版本时，最终 build list 使用其中最高的最低要求版本。\n3. 一个构建中的同一个模块路径只出现一个版本；`example.com/lib` 与 `example.com/lib/v2` 是两个不同模块路径，适合大版本并存迁移。\n\n工程取舍是清晰的：MVS 让构建结果稳定、可解释、升级路径保守；团队需要主动运行 `go get`、`go mod tidy`、依赖扫描和测试矩阵来推进安全升级。",
      "代理、校验与私有模块：Module 下载链路同时处理可用性、完整性和隐私。\n\n公开依赖默认经过 `GOPROXY` 下载，并通过 `GOSUMDB` 参与校验。`go.sum` 记录本项目遇到过的模块版本校验数据，适合提交进仓库，用来让团队和 CI 复现相同内容。私有模块需要让匹配路径避开公开代理和公开校验数据库，并让底层 Git、SSH 或 HTTPS 凭据可用。\n\n```bash\n# 依赖链路快照\ngo env GOPROXY GOSUMDB GOPRIVATE GONOPROXY GONOSUMDB GOMOD GOWORK GOMODCACHE\n\n# 私有模块常见配置\ngo env -w GOPRIVATE='git.example.com/team/*'\ngo env -w GONOSUMDB='git.example.com/team/*'\n\n# 清理模块缓存后验证下载链路\ngo clean -modcache\ngo mod download -x\n```\n\n排查时关注错误文字：`404`、`410 Gone`、`terminal prompts disabled`、`invalid version`、`checksum mismatch`、`unrecognized import path`、`module declares its path as ...` 往往分别指向代理不可达、凭据缺失、版本标签错误、校验异常或模块路径声明不一致。",
      "实战例子：一个新服务从初始化到依赖收敛，可以用一组短命令建立稳定基线。\n\n```bash\nmkdir payment && cd payment\ngo mod init example.com/team/payment\n\ngo get github.com/gin-gonic/gin@v1.10.0\ngo mod tidy\n\ngo list -m all\ngo mod graph | head\ngo mod why -m github.com/gin-gonic/gin\n\ngo test ./...\ngo build -trimpath ./cmd/payment\ngo version -m ./payment\n```\n\n推荐工作流：\n\n1. 初始化时先确定稳定的 `module path`，公共库尤其要确认未来发布路径。\n2. 新增依赖优先写明版本，例如 `go get module@v1.2.3`，让变更在 code review 中可见。\n3. 提交前运行 `go mod tidy` 和 `go test ./...`，同时提交 `go.mod` 与 `go.sum`。\n4. 发布库前检查 `replace`、本地路径、`go.work` 影响和 v2+ 路径后缀。\n5. 线上排查时用 `go version -m` 查看二进制嵌入的模块版本、替换信息和构建参数。",
      "边界与故障模式：Go Module 的生产问题通常来自“解析输入”和“下载链路”两个面。\n\n- 版本被抬高：某个间接依赖提高了最低版本要求，用 `go mod graph` 与 `go mod why -m` 找来源。\n- 本地能过、CI 失败：检查 `GOWORK`、本地 `replace`、私有模块凭据、`GOPRIVATE`、代理配置、Go 版本和缓存键。\n- `go.sum` 冲突：两边同时引入或清理了校验记录，运行 `go mod tidy` 后提交最终文件。\n- `checksum mismatch`：模块内容在同一版本标签下发生变化，优先确认上游是否重打 tag、代理缓存是否污染、企业代理是否改写内容。\n- `module declares its path as X but was required as Y`：模块自身 `go.mod` 的 `module` 行和引用路径不一致，需要调整 import path、依赖路径或上游模块声明。\n- `replace` 残留：本地路径让 CI 找不到目录，fork 替换让发布库的使用者解析到不同依赖图。\n- v2+ 导入失败：模块路径、tag、目录布局和 import path 需要一致，例如 `module example.com/lib/v2` 与 `import \"example.com/lib/v2/pkg\"`。",
      "排查实践：遇到依赖或构建问题时，先固定证据，再缩小到模块图、下载链路或发布规则。\n\n```bash\n# 1. 环境与工作区\ngo version\ngo env GOMOD GOWORK GOFLAGS GOTOOLCHAIN GOPROXY GOSUMDB GOPRIVATE GONOPROXY GONOSUMDB\n\n# 2. 模块图与版本来源\ngo list -m -json all > modules.json\ngo mod graph > mod-graph.txt\ngo mod why -m example.com/dependency\n\n# 3. 下载链路与校验\ngo env GOMODCACHE\ngo mod download -json example.com/dependency@v1.2.3\ngo mod download -x example.com/dependency@v1.2.3\n\n# 4. 收敛文件与验证构建\ngo mod tidy\ngit diff -- go.mod go.sum\ngo test ./...\n```\n\n判断顺序建议固定为：环境变量与 workspace、`go.mod` 指令、MVS build list、代理与凭据、校验数据、目标平台构建。这个顺序能减少盲目删缓存和手工改 `go.sum` 带来的二次问题。",
      "常见误区：成熟的 Module 使用方式，是把依赖文件、环境变量和发布规则一起管理。\n\n- `require` 表示最低版本要求，最终版本由整个模块图和 MVS 决定。\n- `go.sum` 是校验记录，依赖清单以 `go.mod` 和 build list 为准。\n- `replace` 是主模块或 workspace 的本地决策，团队需要在提交和发布前检查它的影响范围。\n- 私有模块接入同时需要路径匹配、代理策略、校验策略和 VCS 凭据。\n- `go.work` 提高多模块本地联调效率，CI 与发布流程需要明确是否启用 workspace。\n- v2+ 公共模块的路径后缀是 API 并存和迁移策略的一部分。",
      "参考来源：本文用 Go Modules Reference 与 go.mod file reference 校准模块定义、`go.mod` 指令、MVS、checksum database、私有模块和版本语义；用 Managing dependencies、Create a Go module、Using Go Modules、Publishing Go Modules 和 v2 and Beyond 校准日常命令、依赖维护、发布流程和 v2+ 路径规则；用 cmd/go 文档校准 `go` 命令、环境变量和诊断命令；用 Russ Cox 的 Minimal Version Selection 文章理解算法动机；用 Go Wiki 和煎鱼的 Go Modules 入门补充迁移、中文实践表达和常见排查上下文。",
    ],
    typicalProblems: [
      "Go Module 解决了 Go 工程中的哪些依赖管理和可复现构建问题？",
      "module、package、import path、module path 和 package name 的关系是什么？",
      "`go.mod` 中 `module`、`go`、`toolchain`、`require`、`replace`、`exclude`、`retract` 分别影响什么？",
      "`go.sum` 记录什么？它和依赖清单、checksum database、模块缓存的关系是什么？",
      "MVS 如何从依赖图中选择最终版本？为什么间接依赖会抬高 build list 里的版本？",
      "公共库发布 v2+ 时为什么需要 `/v2` 路径后缀？它如何支持大版本并存？",
      "本地 `replace`、fork 依赖和 `go.work` 在团队协作、CI 和发布中分别有哪些风险？",
      "私有模块拉取失败时，怎样检查 `GOPRIVATE`、`GOPROXY`、`GOSUMDB`、凭据和代理日志？",
      "出现 `checksum mismatch`、`module declares its path as`、`unknown revision` 时分别如何定位？",
      "线上二进制依赖版本异常时，如何用 `go version -m`、`go list -m all` 和构建日志还原证据链？",
    ],
    commonIssues: ["replace 残留", "go.sum 冲突", "私有模块认证失败", "v2 路径后缀错误", "间接依赖版本被抬高", "go.work 影响本地结果", "checksum mismatch", "模块路径声明不一致", "代理缓存或企业代理改写内容", "CI 缓存键覆盖范围过宽"],
    practiceAdvice: ["用 go mod init 建立模块边界", "用 go mod tidy 收敛依赖", "用 go list -m all 查看最终版本", "用 go mod why -m 定位依赖来源", "用 go mod download -x 排查下载链路", "发布前清理本地 replace 和 workspace 影响", "私有模块统一配置 GOPRIVATE 与凭据", "用 go version -m 审计线上二进制依赖"],
    commands: ["go mod init example.com/app", "go get example.com/lib@v1.2.3", "go mod tidy", "go list -m all", "go list -m -json all", "go mod graph", "go mod why -m example.com/lib", "go mod download -x example.com/lib@v1.2.3", "go env GOPROXY GOSUMDB GOPRIVATE GONOPROXY GONOSUMDB", "go version -m ./app"],
    prerequisites: ["go-toolchain"],
    related: ["go-mod-tidy", "package", "import"],
    order: 4,
  }),
  /* <!-- KG_REVIEWED: go mod tidy | 2026-06-05 | source_count=8 --> */
  goPoint({
    id: "go-mod-tidy",
    zh: "go mod tidy",
    en: "go mod tidy",
    area: "tooling",
    difficulty: "easy",
    summary: "go mod tidy 根据源码包图、测试依赖、工具依赖和兼容性规则收敛 go.mod 与 go.sum，让依赖清单回到可复现构建状态。",
    concept: "用当前模块真实包图重新计算依赖声明、间接依赖注释和校验记录。",
    focus: "主模块包、测试依赖、工具依赖、构建标签、直接/间接依赖、go.sum 校验和和 CI 差异检查",
    mechanism: "go mod tidy 加载主模块中的包、工具、递归依赖以及测试导入，补齐提供相关 package 的模块 require，移除无关 require，更新 go.sum，并按 go 指令和 -compat 处理兼容性所需校验和。",
    scenarios: ["依赖清理", "CI 校验", "升级后收敛", "提交前检查", "删除代码后的依赖收敛"],
    pitfalls: "测试文件依赖、构建标签影响、私有依赖下载、tools.go 管理、go.work 影响、vendor 模式预期和 go.sum 兼容性",
    sourceRefs: [
      "go-mod-tidy-reference",
      "go-modules-reference",
      "go-gomod-reference",
      "go-managing-dependencies",
      "go-command",
      "go-blog-using-modules",
      "go-wiki-modules",
      "eddycjy-go-modules",
    ],
    explanation: [
      "概念定位：`go mod tidy` 解决的是“源码导入关系已经变化，模块依赖文件怎样重新回到真实、最小、可复现状态”的问题。\n\nGo 项目中的 `go.mod` 是依赖合约，`go.sum` 是校验记录。日常开发新增 import、删除包、修改测试、引入工具、升级 Go 版本、切换 workspace 或替换私有模块后，这两个文件都可能和真实包图产生偏移。`go mod tidy` 通过重新加载主模块的包集合和相关依赖，把这种偏移收敛成可提交、可审查、可复现的文件变更。\n\n对新手来说，它是提交前整理依赖的标准动作；对经验工程师来说，它是排查依赖漂移、CI 差异、供应链校验、工具依赖和跨平台构建问题的第一证据。",
      "准确定义：`go mod tidy` 是 Go Module 的依赖收敛命令，它确保 `go.mod` 与模块源码匹配，并同步 `go.sum` 中构建和兼容性检查需要的哈希记录。\n\n它会处理几类内容：\n\n- `require`：补齐当前包图需要的模块，移除已经脱离相关包图的模块。\n- `// indirect`：根据模块是否直接提供主模块导入的包，添加或移除间接依赖注释。\n- `go.sum`：补齐缺失的 `.mod` 或 `.zip` 哈希，清理当前包图和兼容性检查范围外的记录。\n- `tool`：Go 1.24+ 的工具依赖会参与 tidy，缺失的工具模块 requirement 会被补齐。\n- `go` 与兼容性：`-go` 会更新 `go` 指令并触发模块图裁剪、懒加载相关变化；`-compat` 决定按哪个旧 Go 版本校验模块图稳定性。",
      "心智模型：把 `go mod tidy` 看成“依赖账本的审计员”。\n\n源码 import、测试 import、工具依赖、构建标签和 `replace` 是审计输入；`go.mod` 与 `go.sum` 是账本；MVS、checksum database、模块代理和本地缓存是核算规则。审计结束后，账本应该能解释三个问题：当前源码需要哪些模块、这些模块为什么以这些版本出现、下载到的内容怎样被校验。\n\n这个模型能解释常见现象：删除一段代码后仍可能保留测试依赖；新增一个只在 Linux 文件里使用的包也会影响 tidy；`go.sum` 出现旧版本哈希可能来自 MVS 加载中间版本的 `go.mod`；私有模块配置缺失时，tidy 会在解析包图或校验阶段暴露凭据和代理问题。",
      "主流程机制：一次 `go mod tidy` 通常按这些步骤收敛依赖文件。\n\n1. 定位模块上下文：`go` 命令读取当前目录、`go.mod`、可能存在的 `go.work`、`GOWORK`、`GOFLAGS`、`GOPROXY`、`GOSUMDB`、`GOPRIVATE` 等环境输入。\n2. 加载包集合：加载主模块中的所有包、Go 1.24+ `tool` 指令声明的工具、这些包和工具递归导入的依赖，以及测试导入路径，包含其他模块测试中出现的包。\n3. 扩展构建条件：tidy 按几乎全部 build tags 评估源码，平台专用文件和自定义 build tag 文件也会参与；`ignore` build tag 是特殊例外。\n4. 解析模块提供者：根据 import path 在 build list、代理、VCS、本地 `replace` 或 workspace 模块中寻找提供 package 的模块。\n5. 执行版本选择：按 Go Module 的 MVS 规则得到最终模块版本集合，并结合 `go` 指令、模块图裁剪和懒加载决定直接/间接要求。\n6. 写回账本：补齐缺失 `require`，移除脱离相关包图的 `require`，维护 `// indirect`，添加或删除 `go.sum` 哈希记录。\n7. 输出证据：普通模式写回 `go.mod` 与 `go.sum`；`-diff` 输出统一 diff 并用退出码表达差异；`-v` 打印被移除模块；`-x` 展示执行的下载或 VCS 命令。\n\n```bash\n# 日常收敛\ngo mod tidy\n\n# CI 中只输出差异并让流水线失败\ngo mod tidy -diff\n\n# 查看移除的模块\ngo mod tidy -v\n\n# 排查代理、VCS、下载链路\ngo mod tidy -x\n\n# 按指定 Go 版本改写 go 指令并收敛模块图\ngo mod tidy -go=1.24\n\n# 按指定兼容版本检查 go.sum 和模块图稳定性\ngo mod tidy -compat=1.23\n```",
      "实战例子：一个依赖变化的仓库可以用 tidy 建立清晰的提交边界。\n\n```bash\n# 1. 新增或删除 import、测试、工具依赖后先收敛\ngo mod tidy\n\n# 2. 审查依赖文件差异\ngit diff -- go.mod go.sum\n\n# 3. 解释意外模块来源\ngo mod why -m example.com/suspect\ngo mod graph | grep 'example.com/suspect'\ngo list -m -json all > modules.json\n\n# 4. 验证构建、测试和只读模块模式\ngo test ./...\ngo test -mod=readonly ./...\n```\n\n典型提交策略是：业务代码、`go.mod`、`go.sum` 一起进入同一变更；依赖版本升级带单独说明；大规模清理依赖时单独提交，便于 review 只关注模块图变化。",
      "命令与职责边界：`go mod tidy` 是依赖账本收敛工具，周边命令负责不同证据。\n\n```text\ngo get module@version      主动选择依赖版本，适合升级、降级、加入或移除指定模块。\ngo mod tidy                根据源码包图收敛 go.mod 与 go.sum。\ngo mod download            预下载模块，适合构建镜像、离线缓存和下载链路排查。\ngo mod graph               输出模块 requirement 图，适合看版本由谁抬高。\ngo mod why -m module       输出主模块到目标模块的最短包导入路径。\ngo mod vendor              生成 vendor 目录，适合需要提交依赖源码副本的环境。\ngo mod verify              校验模块缓存中的 zip 与解压目录保持原始下载内容。\ngo list -m -json all       输出最终 build list，适合审计和自动化扫描。\n```\n\n工程判断可以按一句话记：版本选择交给 `go get`，账本收敛交给 `go mod tidy`，来源解释交给 `go mod why` 和 `go mod graph`，缓存完整性检查交给 `go mod verify`。",
      "深层细节：tidy 的关键价值来自“加载范围比普通构建更宽”。\n\n- 测试依赖会参与 tidy，所以只运行 `go build ./...` 得到的依赖集合通常比 tidy 更窄。\n- 平台文件和自定义 build tag 文件会参与 tidy，所以 Linux、Windows、Darwin 或 `//go:build integration` 文件都可能带来模块变化。\n- `testdata`、点开头目录和下划线开头目录里的包通常由 `go` 命令跳过；显式 import 会把它们带入包图。\n- `go.sum` 记录哈希，覆盖模块 `.mod` 文件和 `.zip` 内容；公开模块通常通过 checksum database 校验，私有模块由 `GOPRIVATE`、`GONOSUMDB` 和企业代理策略决定。\n- `-compat` 会让 tidy 保留某个旧 Go 版本加载模块图需要的校验记录；升级 `go` 指令后，`go.sum` 和 `// indirect` 变化需要结合兼容版本理解。\n- Go 1.24+ `tool` 指令把开发工具纳入模块图，tidy 会确保声明工具所需的 requirement 存在。\n- `replace` 和 `go.work` 会改变解析输入，适合本地联调，也会让本地 tidy 结果与 CI 出现差异。",
      "边界与故障模式：`go mod tidy` 失败或产生意外 diff，通常说明包图、环境、代理或校验链存在可解释变化。\n\n- CI 产生 tidy diff：检查开发者 Go 版本、`GOTOOLCHAIN`、`GOWORK`、`GOFLAGS`、`GOOS/GOARCH`、build tags、生成代码和提交中的 `go.mod/go.sum`。\n- 私有模块拉取失败：检查 `GOPRIVATE`、`GONOPROXY`、`GONOSUMDB`、`GOPROXY`、Git 凭据、SSH/HTTPS URL 和企业代理访问。\n- 意外新增 `// indirect`：用 `go mod why -m` 查包导入路径，用 `go mod graph` 查哪个 requirement 抬高版本。\n- `checksum mismatch`：优先核对上游 tag、企业代理缓存、模块代理返回内容和 `go.sum` 记录，确认同一版本内容保持一致。\n- 本地可 tidy、流水线失败：重点查看本地 `replace`、`go.work`、未提交生成代码、私有仓库凭据和 Go 工具链版本。\n- vendor 场景差异：`go mod tidy` 仍会访问模块缓存和下载链路；`vendor/modules.txt` 需要由 `go mod vendor` 重新生成来匹配 `go.mod`。\n- 工作区联调污染：`go env GOWORK` 能显示当前 workspace 文件，发布前建议用 `GOWORK=off go mod tidy` 验证单模块视角。",
      "排查实践：把 tidy 问题拆成“环境 -> 包图 -> 模块图 -> 下载校验 -> 提交差异”五层，定位会更快。\n\n```bash\n# 1. 固定环境证据\ngo version\ngo env GOMOD GOWORK GOFLAGS GOTOOLCHAIN GOPROXY GOSUMDB GOPRIVATE GONOPROXY GONOSUMDB GOOS GOARCH\n\n# 2. 看 tidy 准备改什么\ngo mod tidy -diff\n\n# 3. 找依赖来源\ngo mod why -m example.com/dependency\ngo mod graph | grep 'example.com/dependency'\ngo list -deps -test ./... | grep 'example.com/dependency'\n\n# 4. 排查下载和校验\ngo mod download -json example.com/dependency@v1.2.3\ngo mod download -x example.com/dependency@v1.2.3\ngo mod verify\n\n# 5. 隔离 workspace 和只读模块模式\nGOWORK=off go mod tidy -diff\ngo test -mod=readonly ./...\n```\n\n推荐处理顺序：先让 `go mod tidy -diff` 明确差异，再用 `go mod why -m` 解释来源，随后确认代理和校验链，最后运行 `go test ./...` 与关键平台构建。这样能把“依赖文件为什么变了”转化为可审查证据。",
      "工程取舍：把 tidy 放进流程时，要兼顾一致性、速度和可审查性。\n\n- 本地提交前运行 `go mod tidy && go test ./...`，能提前发现测试依赖、工具依赖和私有模块问题。\n- CI 使用 `go mod tidy -diff`，能保持工作区文件只读，同时把修复 diff 直接展示给开发者。\n- 依赖升级 PR 先用 `go get module@version` 明确版本动作，再运行 tidy 收敛账本，review 能区分“主动升级”和“自动清理”。\n- 企业私有模块场景应把 `GOPRIVATE`、代理、凭据和 checksum 策略写进开发环境与 CI 镜像，减少个人机器配置影响。\n- 多平台或多 tag 项目建议在 CI 中固定 Go 版本，并补充关键 `GOOS/GOARCH`、build tags、生成代码步骤和 tidy 检查。\n- 使用 `replace`、fork、workspace 联调时，在提交说明里交代用途和退出条件，公共库发布前清理本地路径替换。",
      "常见误区：成熟的 tidy 理解来自“源码包图决定依赖账本”。\n\n- `go.sum` 变化是校验证据变化，应该结合包图、兼容版本和下载链路审查。\n- `go build` 验证当前构建路径，`go mod tidy` 审计更宽的包集合、测试依赖、工具依赖和构建标签。\n- `// indirect` 表示模块通过间接路径参与当前模块图，版本仍会影响最终构建。\n- `go mod tidy -diff` 适合 CI 守门，开发者本地仍然需要运行普通 tidy 写回文件。\n- 私有模块成功 `git clone` 只说明仓库可访问，Go 命令还会经过代理、checksum、VCS 策略和模块路径校验。\n- workspace 和 `replace` 提升联调效率，提交前要用单模块视角验证依赖账本。",
      "面试与复盘抓手：回答 `go mod tidy` 题时，建议按“解决问题 -> 加载范围 -> 文件变化 -> 版本与校验 -> CI 落地 -> 故障排查”展开。\n\n高频追问包括：\n\n- `go mod tidy` 会读取哪些输入？为什么测试依赖、工具依赖和 build tags 会影响结果？\n- 它会怎样修改 `go.mod`、`go.sum` 和 `// indirect` 注释？\n- `go mod tidy`、`go get`、`go mod vendor`、`go mod verify` 的职责怎样划分？\n- `go.sum` 中的 `/go.mod` 行和模块 zip 哈希分别代表什么？checksum database 在公开模块里承担什么角色？\n- `-diff`、`-v`、`-x`、`-go`、`-compat` 分别适合什么场景？\n- 本地 tidy 干净，CI 出现 diff，应怎样检查 Go 版本、workspace、build tags、生成代码和私有模块环境？\n- 意外新增间接依赖或版本被抬高时，怎样用 `go mod why -m`、`go mod graph` 和 `go list -m -json all` 找证据？\n- 私有模块、企业代理、checksum mismatch、vendor 目录、`replace` 残留分别怎样排查？",
      "参考来源：本文用 Go Modules Reference 的 `go mod tidy` 小节校准命令语义、参数、加载范围、build tags、`// indirect`、`-go` 与 `-compat` 行为；用 Go Modules Reference 的 `go.sum` 与 checksum database 小节说明哈希记录和公开模块校验链；用 Managing dependencies 说明添加、移除、替换和 Go 1.24+ tool 依赖流程；用 `cmd/go` 文档补充 `-mod`、`GOPRIVATE`、`GOWORK`、代理和环境变量；用 Go Blog Using Go Modules、Go Wiki 和煎鱼 Go Modules 文章补充中文工程语境、常见操作和实践表达。",
    ],
    typicalProblems: [
      "`go mod tidy` 解决什么问题？它和 Go Module 的 `go.mod`、`go.sum`、MVS、checksum database 有什么关系？",
      "`go mod tidy` 的加载范围包括哪些包？测试依赖、工具依赖、平台文件和 build tags 为什么会影响结果？",
      "`go mod tidy` 会怎样修改 `go.mod`、`go.sum` 和 `// indirect`？哪些变化应该进入同一个提交？",
      "`go mod tidy -diff`、`-v`、`-x`、`-go`、`-compat` 的输出、退出码和适用场景分别是什么？",
      "`go mod tidy`、`go get`、`go mod vendor`、`go mod verify`、`go mod download` 的职责怎样划分？",
      "`go.sum` 中的 `/go.mod` 哈希和模块 zip 哈希分别代表什么？`checksum mismatch` 应该怎样排查？",
      "本地 tidy 结果和 CI 结果存在差异时，应怎样检查 `GOWORK`、Go 版本、build tags、生成代码、私有模块和代理配置？",
      "意外新增 `// indirect` 或依赖版本被抬高时，怎样用 `go mod why -m`、`go mod graph`、`go list -m -json all` 找到来源？",
      "私有模块场景下，`GOPRIVATE`、`GONOPROXY`、`GONOSUMDB`、`GOPROXY` 和 Git 凭据分别影响 tidy 的哪个阶段？",
    ],
    commonIssues: ["测试依赖导致 go.mod 变化", "build tags 与平台文件引入额外模块", "Go 1.24+ tool 指令补齐工具 requirement", "私有模块代理或凭据失败", "go.sum 变化缺少来源解释", "本地 go.work 或 replace 影响 CI 结果", "vendor/modules.txt 与 go.mod 漂移", "checksum mismatch 暴露上游 tag 或代理缓存问题"],
    practiceAdvice: ["提交前运行 go mod tidy && go test ./...", "CI 使用 go mod tidy -diff 展示依赖文件差异", "用 go mod why -m 解释模块来源", "用 go mod graph 查看 requirement 图", "用 go list -m -json all 审计最终 build list", "用 GOWORK=off 验证单模块视角", "私有模块统一配置 GOPRIVATE、GONOPROXY、GONOSUMDB 与凭据", "依赖升级先 go get module@version 再 tidy"],
    commands: ["go mod tidy", "go mod tidy -diff", "go mod tidy -v", "go mod tidy -x", "go mod tidy -go=1.24", "go mod tidy -compat=1.23", "go mod why -m example.com/lib", "go mod graph", "go list -m -json all", "go mod download -x example.com/lib@v1.2.3", "GOWORK=off go mod tidy -diff", "go test -mod=readonly ./..."],
    prerequisites: ["go-module"],
    related: ["import", "build-tags", "go-toolchain"],
    order: 5,
  }),
  /* <!-- KG_REVIEWED: Package | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "package",
    zh: "Package",
    en: "Package",
    area: "foundation",
    difficulty: "easy",
    summary: "Package 是 Go 的代码组织和编译边界，同一包内文件共享包级声明，对外通过导出标识符提供 API。",
    concept: "用目录级包边界组织代码职责、导入路径、可见性和初始化顺序。",
    focus: "package clause、目录、import path、包级作用域、导出标识符、internal 目录、package main 和测试包",
    mechanism: "每个 Go 源文件以 package clause 开头；同一目录中声明同一 package name 的文件一起编译，形成包级作用域；import path 由模块路径和子目录组成，导入方通过包名访问导出的常量、变量、类型和函数。",
    scenarios: ["代码分层", "公共库设计", "内部包隔离", "测试包组织", "命令行程序拆分"],
    pitfalls: "循环依赖、包名膨胀、internal 边界、init 副作用、导出 API 过宽、package main 滥大和测试包命名混乱",
    sourceRefs: ["go-spec", "go-how-to-write-code", "go-module-layout", "go-blog-package-names", "go-effective-go"],
    explanation: [
      "概念定义：Package 是 Go 组织源码的基本单位，也是编译、可见性和 API 设计的边界。一个包由同一目录下声明相同 package name 的 Go 文件组成，包内文件共享顶层常量、变量、类型和函数。",
      "工作机制：源码文件先声明 package，再声明 import 和顶层代码。编译时，同一包的文件一起形成包级作用域；导入方使用 import path 找到包，再通过包名加导出标识符访问 API。module path 提供导入前缀，子目录形成子包路径。",
      "适用场景：公共库用 package 暴露稳定 API；服务端项目用 internal 保存可自由调整的内部实现；命令行程序用 package main 定义可执行入口；多命令仓库常把入口放在 cmd 目录；测试可使用同包测试或 xxx_test 外部测试包观察公开 API。",
      "特殊情况：package main 编译为可执行程序；internal 目录限制导入范围，适合隐藏实现细节；同一模块可以包含多个包，每个包独立命名和导入；包初始化会按依赖、变量和 init 顺序执行，隐藏副作用会影响启动与测试稳定性。",
      "常见误区：把 util、common、types 这类大杂烩包当作通用分层会让依赖持续膨胀；按技术名拆包会掩盖业务职责；导出过多符号会提高兼容成本；包之间互相导入会形成循环依赖；在 init 中做网络、文件或复杂注册会增加排查难度。",
      "实战建议：包名保持短小、清晰、全小写，优先用领域名表达职责；从调用方视角设计 API，让 package name 和导出名组合后自然可读，例如 http.Server；服务项目优先把内部逻辑放入 internal；用 go list ./... 和 go test ./... 及时暴露循环依赖和初始化问题。",
      "参考来源：本讲解参考 Go Spec、How to Write Go Code、Organizing a Go module、Go Blog Package names 和 Effective Go，右侧来源支撑包定义、源码组织、项目布局、命名建议和 API 设计原则。",
    ],
    typicalProblems: [
      "Go 中 package、module 和 import path 的关系是什么",
      "同一目录下多个 Go 文件如何组成一个包",
      "package main 和普通 package 的区别是什么",
      "internal 目录解决什么工程边界问题",
      "为什么 Go 包名强调短小、清晰、全小写",
      "循环依赖、init 副作用和过宽 API 如何排查与治理",
    ],
    commonIssues: ["循环依赖", "util/common 大包膨胀", "导出 API 过宽", "internal 边界设计混乱", "init 副作用", "测试包组织混乱"],
    practiceAdvice: ["用 go list ./... 检查包图", "按职责拆包", "用 internal 收敛内部实现", "从调用方视角命名导出 API", "把包初始化逻辑保持轻量"],
    prerequisites: ["go-module"],
    related: ["import", "visibility", "init-function"],
    order: 6,
  }),
  /* <!-- KG_REVIEWED: import | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "import",
    zh: "import",
    en: "Import",
    area: "foundation",
    difficulty: "easy",
    summary: "import 把当前文件依赖的包引入文件作用域，并通过导入名访问被导出标识符。",
    concept: "把标准库、第三方库或本地包引入当前文件，形成明确的包依赖关系。",
    focus: "import path、导入名、别名导入、空白导入、点导入、文件作用域、循环导入和依赖初始化顺序",
    mechanism: "import 声明写在 package clause 之后，声明当前文件依赖某个 import path；默认导入名来自被导入包的 package name，也可显式起别名、使用 . 导入导出标识符，或用 _ 触发初始化副作用。",
    scenarios: ["复用标准库", "拆分业务包", "注册驱动", "解决包名冲突", "组织模块依赖"],
    pitfalls: "循环导入、空白导入副作用、别名滥用、点导入污染命名空间、路径与包名混淆和导入未使用",
    sourceRefs: ["go-spec", "go-how-to-write-code", "go-modules-reference", "go-command", "go-effective-go"],
    explanation: [
      "概念定义：import 是 Go 源文件声明外部包依赖的语法。它让当前文件可以通过导入名访问另一个包的导出标识符，同时让编译器和 go 命令知道包之间的依赖关系。",
      "工作机制：import path 是字符串，指向标准库包、模块中的本地包或第三方模块包。编译时，Go 会先编译被导入包，再编译当前包；导入名默认使用被导入包的 package name，并且只在当前文件作用域内有效。",
      "适用场景：业务代码导入标准库处理 HTTP、JSON、时间和文件；项目内部通过模块路径加子目录导入其他包；第三方能力通过模块依赖进入包图；遇到包名冲突时用别名导入；数据库驱动、图片解码器等注册型包常使用空白导入。",
      "特殊情况：`import m \"path\"` 可改本文件中的导入名；`import _ \"path\"` 只执行被导入包初始化，用于注册副作用；`import . \"path\"` 会把导出名直接放入当前文件作用域，常见于少量测试场景；Go 会拒绝未使用的普通导入，也会拒绝直接或间接导入自身。",
      "常见误区：把 import path 当成 package name 会在别名、v2 模块和仓库路径变化时出错；滥用空白导入会隐藏初始化副作用；用点导入会降低可读性；通过调整 import 绕过循环依赖会让包职责更加混乱，真正的处理方式是重新划分包边界。",
      "实战建议：优先保持导入名与包名一致；用 gofmt/goimports 维护导入分组和排序；驱动注册类空白导入写清注释；出现 import cycle 时先画包依赖图，再把公共类型、接口或实现迁到更合适的包；用 go list -deps 查看完整依赖链。",
      "参考来源：本讲解参考 Go Spec、How to Write Go Code、Go Modules Reference、cmd/go 文档和 Effective Go，右侧来源支撑 import 语法、文件作用域、依赖关系、模块路径和工程约定。",
    ],
    typicalProblems: [
      "import path 和 package name 的关系是什么",
      "别名导入、空白导入和点导入分别适合什么场景",
      "为什么 Go 会禁止未使用的 import",
      "import cycle 出现时通常说明什么设计问题",
      "第三方模块包如何通过 import 进入 go.mod",
      "数据库驱动为什么常用空白导入注册",
    ],
    commonIssues: ["import cycle", "未使用导入", "空白导入副作用隐藏", "别名导入过多", "点导入污染命名空间", "import path 与 package name 混淆"],
    practiceAdvice: ["用 gofmt 或 goimports 管理导入", "用 go list -deps 查看依赖链", "空白导入配注释说明副作用", "通过重构包边界处理循环导入", "保持导入名与包名一致"],
    prerequisites: ["package"],
    related: ["visibility", "go-module", "database-sql"],
    order: 7,
  }),
  /* <!-- KG_REVIEWED: 可见性 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "visibility",
    zh: "可见性",
    en: "Visibility",
    area: "foundation",
    difficulty: "easy",
    summary: "Go 通过标识符首字母大小写决定包外访问能力，大写标识符构成包的公开 API。",
    concept: "通过标识符首字母大小写控制包外访问能力，并把导出 API 变成文档和兼容性承诺。",
    focus: "导出标识符、Unicode 大写、包级声明、字段名、方法名、包内封装、doc comment 和兼容性",
    mechanism: "Go 规定首字符为 Unicode 大写字母，并且声明在包块中，或作为字段名、方法名的标识符可以被其他包访问；其余标识符保持包内可见。跨包访问必须通过 qualified identifier，例如 bytes.Buffer。",
    scenarios: ["公共 API 设计", "封装内部状态", "库文档生成", "接口收敛", "兼容性管理"],
    pitfalls: "导出字段过多、命名含糊、注释缺失、零值语义未说明、并发安全承诺不清晰和兼容性成本上升",
    sourceRefs: ["go-spec", "go-doc-comments", "go-blog-package-names", "go-effective-go"],
    explanation: [
      "概念定义：Go 的可见性由标识符名称直接表达。首字母大写的包级常量、变量、类型、函数、方法和字段可以被其他包访问；首字母小写的标识符留在当前包内部，用于隐藏实现细节。",
      "工作机制：跨包访问使用包名加导出标识符，例如 http.Client。导出条件同时要求首字符是 Unicode 大写字母，并且标识符处在包级声明、字段名或方法名位置。文档工具会围绕导出的 API 生成说明，调用方也会把这些符号视为稳定契约。",
      "适用场景：写公共库时用导出类型和函数表达稳定能力；业务服务中用小写字段、函数和类型封装内部实现；struct 对外导出字段时让编码、反射和调用方都能访问；interface 中导出方法会成为跨包行为约束。",
      "特殊情况：struct 类型本身导出后，未导出字段仍然只能由定义包直接访问；未导出类型可以作为导出函数的返回值或参数出现，但会限制调用方直接构造和引用；嵌入字段和方法提升会把可见性问题带到外层 API；JSON、ORM、验证库通常只能直接操作导出字段。",
      "常见误区：为了测试或省事把字段全部导出，会让内部结构变成长期兼容负担；导出标识符缺少 doc comment 会让 pkg.go.dev 和 go doc 信息贫弱；把并发安全、零值可用性、错误返回语义藏在实现里，会让调用方误用 API。",
      "实战建议：导出前先问调用方是否真的需要这个符号；导出的类型、函数和字段写 doc comment，说明用途、零值语义、并发安全和特殊情况；优先导出小接口和构造函数，保留结构体内部字段；用 internal 包和小写标识符共同收敛实现边界。",
      "参考来源：本讲解参考 Go Spec、Go Doc Comments、Go Blog Package names 和 Effective Go，右侧来源支撑导出规则、文档规范、命名风格和 API 设计建议。",
    ],
    typicalProblems: [
      "Go 为什么用首字母大小写控制可见性",
      "哪些标识符可以被其他包访问",
      "导出类型中的未导出字段有什么影响",
      "导出 API 为什么需要 doc comment",
      "JSON 或反射库为什么常要求字段导出",
      "如何控制公共库的 API 兼容性成本",
    ],
    commonIssues: ["导出字段过多", "导出 API 缺少注释", "零值语义不清晰", "并发安全承诺模糊", "未导出字段影响反射", "嵌入导致 API 暴露扩大"],
    practiceAdvice: ["导出符号前先定义调用方需求", "给导出 API 写完整 doc comment", "用构造函数封装内部字段", "用 internal 包管理工程边界", "在注释中说明零值和并发安全"],
    prerequisites: ["package"],
    related: ["struct", "interface", "method"],
    order: 8,
  }),
  /* <!-- KG_REVIEWED: main 函数 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "main-function",
    zh: "main 函数",
    en: "main Function",
    area: "foundation",
    difficulty: "easy",
    summary: "main 函数是 Go 可执行程序的入口，程序初始化完成后调用它，main 返回后进程结束。",
    concept: "作为可执行程序入口启动业务逻辑，并组织配置加载、依赖初始化、服务运行和退出流程。",
    focus: "package main、func main、包初始化、命令行参数、退出码、defer、信号处理和后台 goroutine 生命周期",
    mechanism: "完整程序由一个未被导入的 main package 和其传递依赖链接而成；main package 必须声明 func main()；运行时先完成包初始化，再调用 main.main，main 返回后程序退出，并且不会等待其他 goroutine 自行结束。",
    scenarios: ["命令行工具", "HTTP 服务启动", "任务程序", "本地示例", "守护进程入口"],
    pitfalls: "配置读取时机、资源释放、信号处理、错误退出码、后台 goroutine 生命周期、os.Exit 跳过 defer 和 main 函数过重",
    sourceRefs: ["go-spec", "go-how-to-write-code", "go-os-exit", "go-os-signal", "go-effective-go"],
    explanation: [
      "概念定义：main 函数是 Go 可执行程序的启动入口。只有 package main 中的 func main() 会成为程序入口，普通库包中的函数需要被其他包调用。",
      "工作机制：程序启动时，Go 先按导入依赖初始化包级变量和 init 函数，再调用 main.main。main 函数没有参数和返回值；需要命令行参数时读取 os.Args 或 flag 包；需要表示失败时通常记录错误并调用 os.Exit，或由封装函数返回错误后在 main 中统一处理。",
      "适用场景：命令行工具在 main 中解析参数、调用核心逻辑并设置退出码；HTTP 服务在 main 中加载配置、初始化日志和依赖、启动 server、监听信号并执行 shutdown；定时任务在 main 中建立上下文、执行任务、释放资源并返回进程状态。",
      "特殊情况：main 返回后程序直接退出，其他 goroutine 的工作会被终止；defer 会在 main 正常返回或 panic 展开时执行，os.Exit 会立即结束进程并跳过 defer；信号默认行为会结束进程，服务常用 signal.NotifyContext 把 SIGINT/SIGTERM 转成 context 取消。",
      "常见误区：把所有业务逻辑写进 main 会降低测试性；在 init 中做复杂依赖初始化会让 main 难以控制启动顺序；启动后台 goroutine 后缺少 context 和 WaitGroup 会导致退出不可控；调用 os.Exit 前依赖 defer 释放资源会失效。",
      "实战建议：把 main 保持为编排层，业务逻辑放到可测试函数中；常见结构是 `func run(ctx context.Context, args []string) error`，main 负责创建 context、处理信号、调用 run、打印错误和设置退出码；服务入口在收到信号后调用 shutdown，并给关闭流程设置超时。",
      "参考来源：本讲解参考 Go Spec、How to Write Go Code、os.Exit、os/signal 和 Effective Go，右侧来源支撑程序入口、初始化顺序、退出行为、信号处理和入口函数设计建议。",
    ],
    typicalProblems: [
      "为什么可执行程序必须使用 package main 和 func main",
      "Go 程序启动时 init 函数和 main 函数的顺序是什么",
      "main 返回后后台 goroutine 会发生什么",
      "os.Exit 和 return from main 在 defer 执行上有什么差异",
      "HTTP 服务如何在 main 中处理 SIGINT/SIGTERM",
      "怎样设计 main 函数才能方便测试和维护",
    ],
    commonIssues: ["main 函数过重", "os.Exit 跳过 defer", "信号处理缺失", "后台 goroutine 退出不可控", "配置初始化顺序混乱", "错误退出码不一致"],
    practiceAdvice: ["把业务逻辑放进 run 函数", "main 中统一处理错误和退出码", "用 signal.NotifyContext 管理退出信号", "用 context 和 WaitGroup 控制 goroutine 生命周期", "给服务 shutdown 设置超时"],
    prerequisites: ["package"],
    related: ["init-function", "defer", "http-server"],
    order: 9,
  }),
  /* <!-- KG_REVIEWED: init 函数 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "init-function",
    zh: "init 函数",
    en: "init Function",
    area: "foundation",
    difficulty: "medium",
    summary: "init 函数在包初始化阶段自动执行，适合做轻量注册和默认值准备。",
    concept: "在包初始化阶段执行轻量注册、默认配置和必要准备工作。",
    focus: "包初始化顺序、包级变量、多个 init、导入依赖、空白导入、注册副作用、启动耗时和测试隔离",
    mechanism: "每个包先初始化包级变量，再按源码顺序执行该包的 init 函数；导入包先于当前包初始化；一个包可以有多个 init 函数；整个初始化流程按包串行推进，init 函数本身不能被普通代码调用。",
    scenarios: ["驱动注册", "指标注册", "配置默认值", "测试夹具准备", "插件式能力挂载"],
    pitfalls: "隐藏副作用、初始化顺序依赖、启动耗时、测试隔离困难、panic 影响进程启动和空白导入意图不清",
    sourceRefs: ["go-spec", "go-how-to-write-code", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：init 函数是 Go 的包初始化钩子。它没有参数和返回值，由运行时自动调用，常用于注册驱动、注册指标、填充默认配置或准备包级状态。",
      "工作机制：一个包初始化时，Go 先按依赖关系初始化包级变量，再执行该包里的 init 函数。多个文件和多个 init 会按编译器接收文件及源码声明顺序执行；导入的包先完成初始化；同一个包在程序中只初始化一次。",
      "适用场景：database/sql 驱动通过空白导入触发 init 注册；图片解码器、压缩算法、指标采集器和插件注册也常用 init 完成轻量挂载；测试中可用 init 准备少量不可变默认状态。",
      "特殊情况：init 函数不能被普通代码引用或调用；init 可以启动 goroutine，但下一段初始化会等待当前 init 返回；init 中 panic 会阻止程序正常启动；多个包之间的初始化顺序由 import 图决定，包内隐藏依赖会让行为难以推断。",
      "常见误区：在 init 中读取远程配置、连接数据库、启动后台任务或依赖环境变量，会让程序启动路径隐藏在导入行为里；空白导入缺少注释会让注册来源难以追踪；用 init 改全局可变状态会增加测试隔离成本。",
      "实战建议：把 init 控制在轻量、确定、快速的注册逻辑内；需要失败处理的初始化放到显式函数中，由 main 或调用方返回 error；空白导入写清楚注册目的；单元测试中优先用构造函数和依赖注入替代 init 副作用。",
      "参考来源：本讲解参考 Go Spec、How to Write Go Code、Effective Go 和 Go Documentation，右侧来源支撑包初始化顺序、init 语法、导入副作用和工程实践建议。",
    ],
    typicalProblems: [
      "Go 的包级变量和 init 函数谁先执行",
      "多个 init 函数的执行顺序如何确定",
      "空白导入如何触发 init 注册副作用",
      "为什么 init 中的复杂逻辑会影响测试和排障",
      "init 函数 panic 会对程序启动产生什么影响",
      "哪些初始化逻辑适合放到显式函数中",
    ],
    commonIssues: ["隐藏网络或文件副作用", "初始化顺序依赖", "启动耗时过长", "panic 阻断程序启动", "空白导入缺少说明", "全局状态影响测试隔离"],
    practiceAdvice: ["让 init 保持轻量和确定", "把可能失败的初始化返回 error", "空白导入配注释说明注册目的", "用构造函数替代复杂全局初始化", "用 go test 检查初始化副作用"],
    prerequisites: ["package"],
    related: ["main-function", "import", "database-sql"],
    order: 10,
  }),
  /* <!-- KG_REVIEWED: 变量 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "variable",
    zh: "变量",
    en: "Variable",
    area: "syntax",
    difficulty: "easy",
    summary: "变量把标识符绑定到带类型的存储位置，Go 通过零值、作用域和短变量声明让状态读写保持明确。",
    concept: "保存可变状态，并通过类型、作用域和初始化规则约束读写行为。",
    focus: "var、短变量声明、零值、类型推断、作用域、遮蔽、空白标识符、逃逸分析和未使用变量检查",
    mechanism: "var 声明创建变量并绑定标识符、类型和初始值；省略初始化时使用该类型零值；:= 只能在函数内部声明并初始化局部变量；标识符作用域由块决定，内层块可以声明同名变量遮蔽外层变量。",
    scenarios: ["局部状态", "配置解析", "循环计算", "返回值接收", "错误分支处理"],
    pitfalls: "变量遮蔽、零值语义、指针逃逸、作用域过大、短变量声明误用、未使用变量和并发共享状态",
    sourceRefs: ["go-spec", "go-faq-stack-heap", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：变量是 Go 中保存可变状态的基本手段。声明变量时会得到一个名字、一个类型和一个当前值；后续代码可以在作用域内读取或重新赋值。",
      "工作机制：`var x int` 会创建 int 变量并赋零值 0；`var x = 1` 由初始化表达式推断类型；`x := 1` 是函数内部常用的声明加初始化简写。短变量声明在同一块里允许多变量场景下复用已有变量，但至少要引入一个新变量。",
      "适用场景：局部变量适合承载中间计算、错误值和临时对象；包级变量适合表达共享只读配置、注册表或少量全局状态；接收多个返回值时可用空白标识符 `_` 丢弃无关值。",
      "特殊情况：Go 会拒绝未使用的局部变量；未显式初始化会得到零值，零值在 sync.Mutex、bytes.Buffer 等类型上有实际设计意义；变量放在栈上还是堆上由编译器逃逸分析决定，返回局部变量地址在 Go 中是安全的；并发读写共享变量需要同步。",
      "常见误区：`:=` 在内层 if、for 或函数块中可能创建同名新变量，导致外层变量值保持旧状态；把零值当成无效值会误判 bool、int、string、slice、map、指针等类型；把包级变量当成配置中心会增加测试耦合和并发风险。",
      "实战建议：变量作用域尽量贴近使用位置；错误处理常用 `if err := ...; err != nil` 收窄 err 的作用域；需要跨分支复用时先用 var 声明再赋值；用 `go test -race` 检查共享变量读写；怀疑逃逸和分配压力时用 `go test -gcflags=-m` 观察编译器报告。",
      "参考来源：本讲解参考 Go Spec、Go FAQ 的栈与堆说明、Effective Go 和 Go Documentation，右侧来源支撑变量声明、零值、作用域、短变量声明和逃逸分析相关判断。",
    ],
    typicalProblems: [
      "var 声明和短变量声明有什么差异",
      "Go 的零值机制解决了什么问题",
      "短变量声明在什么情况下会遮蔽外层变量",
      "局部变量返回地址为什么是安全的",
      "变量作用域过大有哪些维护成本",
      "共享变量并发读写时如何保证安全",
    ],
    commonIssues: ["变量遮蔽", "零值语义误判", "未使用变量", "包级可变状态滥用", "共享变量数据竞争", "逃逸导致分配增加"],
    practiceAdvice: ["把变量声明放近使用点", "跨分支复用变量时显式 var", "用短变量声明收窄错误作用域", "用 go test -race 检查数据竞争", "用 -gcflags=-m 观察逃逸"],
    commands: ["go test ./...", "go test -race ./...", "go test -gcflags=-m ./..."],
    prerequisites: ["go-overview"],
    related: ["constant", "basic-types", "pointer"],
    order: 11,
  }),
  /* <!-- KG_REVIEWED: 常量 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "constant",
    zh: "常量",
    en: "Constant",
    area: "syntax",
    difficulty: "easy",
    summary: "常量表示编译期稳定的标量值，Go 的无类型常量让数字和字符串在赋值前保持更灵活的精度与类型适配。",
    concept: "表达编译期稳定值、枚举式业务语义和可读的配置边界。",
    focus: "const、typed constant、untyped constant、默认类型、常量表达式、iota、枚举、溢出和精度",
    mechanism: "const 声明把标识符绑定到常量表达式；省略类型时常量可以保持无类型状态，直到赋值、参数传递或接口装箱等上下文需要具体类型；iota 在每个 const 声明组中从 0 开始按 ConstSpec 递增。",
    scenarios: ["状态枚举", "默认配置", "单位换算", "协议常量", "位掩码定义"],
    pitfalls: "iota 插入影响、数值溢出、默认类型误判、导出枚举兼容性、字符串常量滥用和位移边界",
    sourceRefs: ["go-spec", "go-blog-constants", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：常量是 Go 中在编译期确定的标量值，常见类型包括布尔、字符串、rune、整数、浮点、复数。它适合表达协议码、状态枚举、默认值、单位换算和位标志。",
      "工作机制：`const` 声明可以显式写类型，也可以省略类型形成无类型常量。无类型常量存在于更高精度的常量空间里，只有在赋给变量、传给函数或放进 interface 时才根据上下文取得具体类型；整数默认 int，浮点默认 float64，rune 默认 rune，复数默认 complex128。",
      "适用场景：业务状态可用自定义类型加 const 枚举表达；时间、容量和位标志常用常量表达式增强可读性；iota 适合一组有规律的值，例如递增枚举、位掩码和等级序号；导出常量适合表达调用方可依赖的公共协议。",
      "特殊情况：常量表达式可以具有任意高精度，赋给具体类型时必须能被目标类型表示；`const Huge = 1e1000` 可以存在，使用到 float64 上下文时会触发溢出；iota 在每个 const 组内按行计数，空行或插入项会改变后续值；无类型常量让 `time.Second * 5` 这类表达更自然。",
      "常见误区：把 iota 值持久化到数据库或网络协议后随意插入枚举项，会破坏兼容性；用字符串常量散落业务状态会缺少类型约束；导出常量没有配套类型会削弱 API 语义；把无类型常量放进 interface 后会得到默认类型，可能影响类型断言。",
      "实战建议：业务枚举优先定义专用类型，例如 `type Status int`；iota 枚举预留 Unknown/Invalid 作为零值；对外协议值显式赋值保持稳定；位标志用 `1 << iota`；导出常量配合 doc comment 说明取值范围和兼容性承诺。",
      "参考来源：本讲解参考 Go Spec、Go Blog Constants、Effective Go 和 Go Documentation，右侧来源支撑 const 语法、无类型常量、默认类型、iota、精度和溢出边界。",
    ],
    typicalProblems: [
      "typed constant 和 untyped constant 有什么差异",
      "无类型常量什么时候获得默认类型",
      "iota 在 const 组中如何递增",
      "为什么常量表达式可以有很高精度",
      "枚举值对外发布后如何保持兼容",
      "把常量传入 interface 后类型会怎样确定",
    ],
    commonIssues: ["iota 插入破坏兼容", "无类型常量默认类型误判", "常量溢出", "字符串状态缺少类型约束", "导出常量缺少文档", "位移表达式边界错误"],
    practiceAdvice: ["业务枚举定义专用类型", "对外协议常量显式赋值", "iota 枚举保留零值语义", "位标志使用 1 << iota", "导出常量写清兼容承诺"],
    prerequisites: ["variable"],
    related: ["basic-types", "control-flow"],
    order: 12,
  }),
  /* <!-- KG_REVIEWED: 基本类型 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "basic-types",
    zh: "基本类型",
    en: "Basic Types",
    area: "type-system",
    difficulty: "easy",
    summary: "Go 基本类型提供 bool、数字、字符串、byte 和 rune 等基础表达能力，并通过显式转换保持类型边界清晰。",
    concept: "提供布尔、整数、浮点、复数、字符串、字节和 Unicode 码点等基础表达能力。",
    focus: "bool、int/uint、float、complex、string、byte、rune、零值、显式转换、比较、溢出、UTF-8 和可比较性",
    mechanism: "基本类型由语言预声明，bool 表示真假，数字类型按位宽和类别约束取值，string 是不可变字节序列，byte 是 uint8 别名，rune 是 int32 别名；不同命名数字类型混用时需要显式转换。",
    scenarios: ["业务字段", "协议解析", "数值计算", "文本处理", "哈希键与比较"],
    pitfalls: "整数溢出、浮点精度、字符串按字节索引、byte 与 rune 混用、int 位宽差异、uintptr 滥用和 interface 比较 panic",
    sourceRefs: ["go-spec", "go-blog-strings", "go-blog-constants", "go-effective-go"],
    explanation: [
      "概念定义：基本类型是 Go 表达数据的底层积木，包括 bool、整数、浮点、复数、string、byte 和 rune。它们决定值能表示什么、能参与哪些运算、能否比较、能否作为 map key。",
      "工作机制：bool 只能取 true/false；整数有固定宽度类型和实现相关宽度的 int/uint；float32/float64 遵循 IEEE 754；complex64/complex128 存储实部和虚部；string 是不可变字节序列，len 返回字节数，索引得到 byte；range string 会按 UTF-8 解码 rune。",
      "适用场景：bool 表达开关和判断；int 常用于索引、计数和长度；明确位宽的 int32、uint64 适合协议、文件格式和跨平台数据；float64 常用于一般浮点计算；string 适合文本和字节载体；byte 处理原始字节，rune 表达 Unicode 码点。",
      "特殊情况：int 和 uint 的位宽随平台变化，跨平台协议优先使用明确位宽类型；无符号整数溢出按模回绕， signed integer 溢出也有确定结果；浮点比较受 NaN、舍入和精度影响；string 可以包含任意字节，字符串字面量通常是 UTF-8；slice、map、function 只能和 nil 比较。",
      "常见误区：把 len(string) 当成字符数会在中文和 emoji 上出错；把 byte 当字符处理会拆开多字节 UTF-8；把 float64 用于金额会引入精度问题；把 int 用于持久化协议会带来平台位宽风险；把可比较性和可排序性混为一谈会导致 map key 或泛型约束设计错误。",
      "实战建议：协议字段使用明确位宽类型；金额和精确小数使用整数分单位或 decimal 库；文本遍历按需求选择字节、rune 或分词库；比较浮点时使用误差范围；作为 map key 前确认类型可比较；遇到字符串乱码先用 `% x`、`%q`、utf8.ValidString 检查字节和编码。",
      "参考来源：本讲解参考 Go Spec、Go Blog Strings、Go Blog Constants 和 Effective Go，右侧来源支撑基本类型定义、字符串字节模型、rune/UTF-8、常量默认类型、比较和溢出规则。",
    ],
    typicalProblems: [
      "Go 的基本类型有哪些",
      "byte 和 rune 分别表示什么",
      "为什么 len(string) 返回字节数",
      "int、int64 和 uint64 混用为什么需要显式转换",
      "整数溢出和浮点精度有哪些边界",
      "哪些类型可以作为 map key",
    ],
    commonIssues: ["字符串按字符数截断错误", "byte/rune 混用", "浮点精度误差", "int 位宽跨平台差异", "整数溢出", "不可比较类型放入 map key"],
    practiceAdvice: ["协议和存储字段使用明确位宽类型", "文本处理区分 byte 与 rune", "金额使用整数分单位或 decimal", "浮点比较使用误差范围", "调试字符串时打印字节和 quoted 形式"],
    prerequisites: ["variable"],
    related: ["constant", "array-slice", "map"],
    order: 13,
  }),
  /* <!-- KG_REVIEWED: 控制流 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "control-flow",
    zh: "控制流",
    en: "Control Flow",
    area: "syntax",
    difficulty: "easy",
    summary: "Go 用 if、for、switch、select、break、continue 和 return 组织执行路径，语法少，边界规则很具体。",
    concept: "用 if、for、switch、select 和跳转语句描述条件分支、循环、匹配、并发等待和提前结束。",
    focus: "if simple statement、for 三种形式、range、switch/type switch、select、fallthrough、label、break/continue、return 和 Go 1.22 loop variable",
    mechanism: "if 根据 bool 条件选择分支；for 覆盖条件循环、三段式循环和 range；switch 执行多路匹配，默认单 case 结束；select 在多个 channel 操作中选择可执行分支；break、continue、goto、fallthrough 和 return 改变默认顺序执行。",
    scenarios: ["业务分支", "集合遍历", "状态机", "输入校验", "并发等待和超时控制"],
    pitfalls: "range 变量版本语义、map 遍历顺序、switch fallthrough、select nil channel、标签跳转、循环闭包和分支膨胀",
    sourceRefs: ["go-spec", "go-blog-loopvar", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：控制流决定代码按什么路径执行。Go 保留少量结构：if 处理条件分支，for 处理所有循环，switch/type switch 处理多路选择，select 处理 channel 等待，return/break/continue/fallthrough/goto 处理提前离开或跳转。",
      "工作机制：if 和 switch 都可以带一个前置 simple statement，用来限制临时变量作用域；Go 只有 for 循环，既能写成 `for cond`，也能写成 `for init; cond; post`，还可以用 range 遍历数组、切片、字符串、map、channel、整数和迭代函数；switch 默认匹配一个分支后结束，表达式缺省时等价于 switch true。",
      "适用场景：输入校验常用 early return 拉平分支；状态机适合 switch；集合处理用 range；channel 多路等待用 select；带超时和取消的并发流程通常把 select、context.Done 和 timer 组合使用。",
      "特殊情况：map 的 range 顺序没有稳定承诺；range string 返回字节索引和 rune；range channel 会持续接收直到通道关闭；select 遇到多个可执行分支会伪随机选择；nil channel 在 send/receive 中会阻塞；Go 1.22 起 for 循环变量按每轮独立作用域处理，旧 go 版本模块仍需警惕闭包捕获旧语义。",
      "常见误区：以为 switch 默认贯穿会误读 Go 代码，Go 需要显式 fallthrough；在 for/select 中缺少退出条件会形成 goroutine 泄漏；依赖 map 遍历顺序会造成不稳定测试；深层 if-else 会掩盖失败路径；旧版本循环变量被闭包捕获会让 goroutine 或子测试读到意外值。",
      "实战建议：错误分支优先 early return；复杂 switch 每个 case 保持短小；range map 后需要稳定输出时先收集 key 并排序；select 中为取消和超时保留 case；维护 Go 1.21 及更早代码时，循环中启动 goroutine 或闭包前显式拷贝迭代变量。",
      "参考来源：本讲解参考 Go Spec、Go Blog Fixing For Loops in Go 1.22、Effective Go 和 Go Documentation，右侧来源支撑控制语句语义、range 行为、switch/select 边界和循环变量版本变化。",
    ],
    typicalProblems: [
      "Go 为什么只有 for 循环",
      "if/switch 的 simple statement 作用域如何计算",
      "switch 为什么默认不会贯穿到下一个 case",
      "range map、range string、range channel 分别返回什么",
      "Go 1.22 的循环变量语义变化解决了什么问题",
      "select 中 nil channel、default 和超时 case 如何影响行为",
    ],
    commonIssues: ["深层分支膨胀", "map 遍历顺序依赖", "旧版本循环变量闭包捕获", "select 缺少取消分支", "fallthrough 误用", "range string 字节索引误判"],
    practiceAdvice: ["用 early return 拉平错误分支", "map 输出前排序 key", "select 加入 context.Done 或超时", "维护旧版本代码时复制循环变量", "把复杂状态机拆成清晰 case"],
    prerequisites: ["basic-types"],
    related: ["error-handling", "function", "map"],
    order: 14,
  }),
  /* <!-- KG_REVIEWED: 错误处理 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "error-handling",
    zh: "错误处理",
    en: "Error Handling",
    area: "syntax",
    difficulty: "easy",
    summary: "Go 把错误当作普通值显式返回，让调用方在每个边界清楚决定重试、降级、返回还是终止。",
    concept: "用显式 error 返回值表达失败路径，并用包装、匹配和类型信息保留可诊断上下文。",
    focus: "error 接口、nil 判断、sentinel error、自定义错误类型、fmt.Errorf %w、errors.Is、errors.As、errors.Join、日志边界和 API 承诺",
    mechanism: "error 是包含 Error() string 方法的接口；函数通常把成功值和 error 一起返回；调用方用 err != nil 判断失败，用 fmt.Errorf(\"...: %w\", err) 增加上下文并保留链路，用 errors.Is/As 在错误树中匹配哨兵值或具体类型。",
    scenarios: ["输入校验", "I/O 调用", "服务边界", "重试决策", "API 错误分类"],
    pitfalls: "错误上下文缺失、吞掉错误、过度包装、日志重复、把实现细节包装进公共 API、直接比较包装错误和 nil 接口陷阱",
    sourceRefs: ["go-builtin-error", "go-errors-package", "go-blog-errors-1-13", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：Go 的错误处理以显式 error 返回值为核心。错误是普通值，调用方在收到错误后决定补充上下文、转换类型、重试、降级、记录日志或返回给上层。",
      "工作机制：内置 `error` 接口只有 `Error() string`。标准库 `errors.New` 创建文本错误，`fmt.Errorf` 用 `%w` 包装底层错误，`errors.Is` 沿错误树匹配哨兵错误，`errors.As` 提取具体错误类型，`errors.Join` 汇总多个错误。",
      "适用场景：I/O、网络、数据库、解析、权限和业务校验都适合返回 error；库函数通过哨兵错误或自定义错误类型给调用方可判断的失败语义；服务边界把内部错误转换成 HTTP 状态码、gRPC code 或统一业务错误码。",
      "特殊情况：选择 `%w` 意味着把底层错误暴露给调用方判断，公共库需要把它视为 API 承诺；多个错误可形成树，Is/As 会遍历包装链和 Join 子节点；typed nil 放入 error 接口后可能得到非 nil 接口；panic 适合不可恢复的程序错误，普通业务失败更适合 error。",
      "常见误区：只返回 `err` 会丢失发生位置；每一层都打日志会造成重复噪音；把所有错误转成字符串会破坏 Is/As 判断；直接 `err == sentinel` 会漏掉包装错误；把数据库、文件系统等实现细节用 `%w` 暴露给外部会限制后续重构。",
      "实战建议：在边界处加上下文，例如操作名、关键参数和资源名；库层返回可判断错误，入口层统一记录日志；判断错误优先用 errors.Is/As；需要隐藏实现时用 `%v` 或自定义错误转换；并发收集错误时用 errors.Join，再在上层统一处理。",
      "参考来源：本讲解参考 builtin error、errors 包文档、Go Blog Working with Errors in Go 1.13、Effective Go 和 Go Documentation，右侧来源支撑 error 接口、包装语义、Is/As/Join、API 承诺和工程实践。",
    ],
    typicalProblems: [
      "Go 为什么把 error 设计成普通返回值",
      "errors.Is 和直接 == 比较有什么差异",
      "errors.As 和类型断言有什么差异",
      "fmt.Errorf 的 %w 会带来什么 API 承诺",
      "typed nil error 为什么容易造成判断异常",
      "服务端如何设计错误日志、错误码和重试策略",
    ],
    commonIssues: ["错误上下文缺失", "重复日志", "吞掉错误", "字符串化破坏错误链", "typed nil error", "包装实现细节造成 API 锁定"],
    practiceAdvice: ["在返回路径补充操作上下文", "入口层统一记录日志", "用 errors.Is/As 做语义判断", "按 API 承诺决定是否 %w", "并发错误用 errors.Join 汇总"],
    prerequisites: ["function"],
    related: ["defer", "panic-recover", "interface"],
    order: 15,
  }),
  /* <!-- KG_REVIEWED: defer | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "defer",
    zh: "defer",
    en: "defer",
    area: "syntax",
    difficulty: "medium",
    summary: "defer 把清理动作注册到当前函数返回前执行，常用于关闭资源、释放锁和统一收尾。",
    concept: "在函数返回前按后进先出顺序执行清理动作，保证多返回路径下的资源释放一致。",
    focus: "资源释放、参数求值时机、LIFO、命名返回值、panic 栈展开、循环内 defer、锁释放和错误收尾",
    mechanism: "defer 执行时会立即求值函数值和参数，并把调用保存到当前函数的延迟调用栈；当前函数正常 return、走到末尾或发生 panic 时，保存的调用按后进先出顺序执行；return 语句先设置返回值，再执行 defer。",
    scenarios: ["文件关闭", "锁释放", "事务回滚", "耗时统计", "panic 边界收尾"],
    pitfalls: "循环内注册过多、参数求值时机误判、命名返回值被修改、锁持有时间过长、os.Exit 跳过 defer 和 nil 函数值延迟 panic",
    sourceRefs: ["go-spec", "go-blog-defer-panic-recover", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：defer 是 Go 的延迟调用机制，用来把清理或收尾逻辑贴近资源获取位置。它让函数存在多个 return 路径时仍能稳定关闭文件、释放锁、回滚事务或记录耗时。",
      "工作机制：执行到 defer 语句时，函数值和参数立即求值并保存，真正调用发生在当前函数返回前。多个 defer 按后进先出执行。return 语句会先给返回值赋值，再运行 defer，因此 defer 可以读取或修改命名返回值。",
      "适用场景：打开文件后立刻 defer Close；加锁后立刻 defer Unlock；开启事务后 defer rollback 并在成功路径 commit；HTTP 中间件用 defer 记录耗时或 recover panic；测试中用 defer 恢复全局状态。",
      "特殊情况：defer 的参数是注册时的值，闭包读取外层变量则会看到执行时的变量状态；循环中 defer 会累积到函数退出才执行，处理大量文件或锁时应把循环体拆成小函数；panic 展开时会执行当前 goroutine 栈上的 defer；os.Exit 会直接结束进程，已注册 defer 不会运行。",
      "常见误区：在长循环里 defer Close 会拖延资源释放；`defer mu.Unlock()` 放在大函数开头会扩大锁持有范围；忽略 Close 返回的错误可能丢失写入失败；用 defer 修改命名返回值会让返回路径变得隐蔽；把 nil 函数值放进 defer 会在函数退出时 panic。",
      "实战建议：获取资源后紧跟 defer；循环内资源用内部函数包住；事务模式写成 defer rollback，成功后 commit 并让 rollback 变成无害操作；锁粒度复杂时优先拆小函数；需要保留 Close 错误时在 defer 中合并到返回 error。",
      "参考来源：本讲解参考 Go Spec、Go Blog Defer, Panic, and Recover、Effective Go 和 Go Documentation，右侧来源支撑 defer 语义、三条经典规则、panic 展开、命名返回值和资源释放实践。",
    ],
    typicalProblems: [
      "defer 的参数什么时候求值",
      "多个 defer 的执行顺序是什么",
      "return、命名返回值和 defer 的执行顺序如何理解",
      "为什么循环中 defer 关闭资源可能造成问题",
      "panic 发生时 defer 会怎样执行",
      "os.Exit 对 defer 有什么影响",
    ],
    commonIssues: ["循环内资源释放过晚", "锁持有范围过大", "Close 错误被忽略", "命名返回值被隐式修改", "参数求值时机误判", "os.Exit 跳过清理"],
    practiceAdvice: ["资源获取后立刻 defer 清理", "循环内用小函数限制 defer 生命周期", "锁相关逻辑保持函数短小", "需要保留 Close 错误时合并返回 error", "避免用 defer 隐式改写复杂返回值"],
    prerequisites: ["function"],
    related: ["panic-recover", "error-handling", "mutex"],
    order: 16,
  }),
  /* <!-- KG_REVIEWED: panic 与 recover | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "panic-recover",
    zh: "panic 与 recover",
    en: "panic and recover",
    area: "syntax",
    difficulty: "medium",
    summary: "panic 表示当前 goroutine 进入异常展开，recover 只能在同一 goroutine 的延迟函数中把展开过程截住。",
    concept: "处理不可恢复错误、框架保护层和局部异常恢复边界。",
    focus: "panic 值、运行时 panic、栈展开、defer 执行、recover 有效位置、goroutine 边界、服务保护和错误转换",
    mechanism: "panic 会停止当前函数正常执行，沿当前 goroutine 调用栈向上展开，并执行沿途已注册的 defer；recover 只有在 panicking goroutine 中由 deferred function 直接调用时才返回 panic 值并停止展开。",
    scenarios: ["HTTP 中间件保护", "任务隔离", "库边界保护", "测试断言", "不可恢复内部状态"],
    pitfalls: "恢复范围过宽、业务错误泛化、goroutine 边界失效、日志缺失、panic(nil) 语义、资源清理遗漏和恢复后状态不一致",
    sourceRefs: ["go-spec", "go-blog-defer-panic-recover", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：panic 表示当前 goroutine 出现严重异常或程序主动触发异常展开。recover 是配套的恢复函数，用在明确边界处把 panic 转换成日志、error 或受控响应。",
      "工作机制：调用 panic 后，当前函数停止正常执行，已注册的 defer 按 LIFO 执行，调用栈逐层展开。某个 deferred function 中直接调用 recover 时，会拿到 panic 的值并停止展开，然后外层函数按该 defer 之后的返回路径结束。",
      "适用场景：HTTP/RPC 框架在请求边界 recover，避免单个请求 panic 影响整个进程；任务调度器在任务边界 recover，记录错误并继续处理其他任务；测试中用 recover 断言某些非法输入会触发 panic；库内部可用 panic/recover 简化深层解析流程，再在公开 API 边界转成 error。",
      "特殊情况：recover 只对同一 goroutine 的 panic 有效，另一个 goroutine 的 panic 需要在那个 goroutine 内部 recover；recover 必须由 deferred function 直接调用；运行时 panic 与显式 panic 都会触发同样的展开；panic(nil) 在现代 Go 中会变成非 nil 的运行时 panic 值，保证有效 recover 返回非 nil。",
      "常见误区：把普通业务失败写成 panic 会破坏调用方可控处理；在过宽范围 recover 会掩盖数据损坏和程序 bug；recover 后继续使用已被部分修改的对象可能导致状态不一致；只 recover 不打印栈会让线上定位困难；以为父 goroutine 可以 recover 子 goroutine 的 panic 会导致进程崩溃。",
      "实战建议：业务错误优先返回 error；panic 用于不可恢复状态、编程错误或框架边界保护；每个 goroutine 的入口函数自行 defer recover；recover 时记录 panic 值、堆栈、请求 ID 和关键上下文；恢复后返回明确错误或终止当前任务，保持状态边界干净。",
      "参考来源：本讲解参考 Go Spec、Go Blog Defer, Panic, and Recover、Effective Go 和 Go Documentation，右侧来源支撑 panic 展开、recover 有效条件、defer 关系和工程边界建议。",
    ],
    typicalProblems: [
      "panic 发生后 defer 的执行顺序是什么",
      "recover 在什么位置调用才有效",
      "父 goroutine 能否 recover 子 goroutine 的 panic",
      "panic 和 error 分别适合什么失败场景",
      "HTTP 中间件如何用 recover 保护请求边界",
      "recover 后应该记录哪些诊断信息",
    ],
    commonIssues: ["业务错误滥用 panic", "recover 范围过宽", "跨 goroutine recover 误判", "恢复后状态不一致", "缺少堆栈日志", "panic 边界缺少资源清理"],
    practiceAdvice: ["在 goroutine 入口设置 recover 边界", "业务失败返回 error", "recover 时记录堆栈和上下文", "恢复后终止当前任务或返回明确错误", "避免吞掉不可恢复程序错误"],
    prerequisites: ["defer", "error-handling"],
    related: ["http-middleware", "goroutine"],
    order: 17,
  }),
  /* <!-- KG_REVIEWED: 函数 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "function",
    zh: "函数",
    en: "Function",
    area: "function",
    difficulty: "easy",
    summary: "函数把输入、处理和输出封装成可复用单元，是 Go 组织业务逻辑、错误返回和回调组合的基础。",
    concept: "封装输入、处理和输出，是 Go 代码复用、测试和抽象的基本单元。",
    focus: "函数声明、签名、参数复制、多返回值、命名返回值、可变参数、函数值、匿名函数、泛型函数和 API 设计",
    mechanism: "函数声明把函数名绑定到一个函数体和签名；调用时实参赋给形参，形参是函数内局部变量；函数可以返回多个值，可以作为值赋给变量、传参或返回；匿名函数可以捕获外层变量形成闭包。",
    scenarios: ["业务逻辑封装", "回调", "中间件", "测试替身", "错误边界收敛"],
    pitfalls: "参数过多、返回值语义含糊、命名返回值滥用、可变参数分配、闭包捕获、副作用扩散和 nil 函数值调用 panic",
    sourceRefs: ["go-spec", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：函数是 Go 中封装行为的基本单位。它用参数描述输入，用返回值描述输出，用函数体承载处理过程，让代码可以复用、测试和组合。",
      "工作机制：函数签名由参数和返回值组成；调用时参数按值传递，传入 slice、map、channel、指针、接口时复制的是描述符或指针语义；函数可以返回多个值，常见模式是 `(value, error)`；函数值可以存入变量、作为回调传递或由工厂函数返回。",
      "适用场景：业务规则写成普通函数便于测试；HTTP 中间件、排序规则、重试策略和并发任务适合用函数值组合；复杂入口可拆成 `run(ctx, args) error`；可变参数适合日志、格式化和少量配置项。",
      "特殊情况：命名返回值在函数开始时被零值初始化，return 可省略表达式，defer 可以修改它们；可变参数在函数内表现为切片，已有切片传入时需要 `s...`；泛型函数需要类型实参可推断或显式指定；nil 函数值调用会 panic。",
      "常见误区：函数参数过多会让调用点难以阅读；返回多个同类型值容易混淆语义；命名返回值配合裸 return 会降低可读性；函数里修改传入 slice/map 的底层数据会影响调用方；闭包捕获共享变量会带来生命周期和并发问题。",
      "实战建议：函数保持单一职责，参数超过三四个时考虑结构体配置；返回值用类型、命名和文档说明语义；错误作为最后一个返回值；公共函数写 doc comment；可测试逻辑放在纯函数或依赖显式传入的函数里；回调和闭包中的共享状态用锁、channel 或局部复制保护。",
      "参考来源：本讲解参考 Go Spec、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑函数声明、签名、参数、返回值、函数值、命名返回值和 API 设计建议。",
    ],
    typicalProblems: [
      "Go 函数参数是按值传递还是按引用传递",
      "多返回值为什么常用于 value/error 模式",
      "命名返回值和裸 return 有哪些边界",
      "可变参数在函数内部是什么类型",
      "函数值、匿名函数和闭包有什么关系",
      "如何设计一个易测试、易阅读的公共函数",
    ],
    commonIssues: ["参数过多", "同类型返回值语义混乱", "命名返回值滥用", "闭包共享状态竞争", "nil 函数值调用 panic", "函数副作用过多"],
    practiceAdvice: ["函数保持单一职责", "错误作为最后返回值", "公共函数写 doc comment", "参数过多时引入配置结构体", "回调中显式处理共享状态"],
    prerequisites: ["control-flow"],
    related: ["closure", "method", "error-handling"],
    order: 18,
  }),
  /* <!-- KG_REVIEWED: 闭包 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "closure",
    zh: "闭包",
    en: "Closure",
    area: "function",
    difficulty: "medium",
    summary: "闭包是能引用外层变量的函数值，外层变量会在闭包可达期间继续存活。",
    concept: "函数值捕获外部变量并延续状态，用于回调、封装状态和组合行为。",
    focus: "function literal、变量捕获、共享变量、生命周期、逃逸分析、循环变量、Go 1.22 语义和并发安全",
    mechanism: "函数 literal 可以引用外层函数中的变量；这些变量由外层函数和函数 literal 共享，并且只要闭包仍可访问就继续存活；编译器会根据闭包逃逸情况决定变量存储位置。",
    scenarios: ["HTTP 中间件", "回调注册", "延迟执行", "状态封装", "函数式配置"],
    pitfalls: "循环变量捕获版本差异、共享状态竞争、逃逸成本、闭包持有大对象、可读性下降和生命周期过长",
    sourceRefs: ["go-spec", "go-blog-loopvar", "go-faq-stack-heap", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：闭包是引用了外层变量的函数值。它让函数在被返回、保存或延迟执行后，仍然能访问当初捕获的状态。",
      "工作机制：Go 的 function literal 可以赋值给变量、立即调用或作为参数传递。函数 literal 引用外层变量时，捕获的是变量本身的存储位置，因此多个闭包可能共享同一个变量；编译器会根据逃逸分析把需要延续生命周期的变量放到合适位置。",
      "适用场景：HTTP 中间件用闭包捕获下一个 handler 和配置；回调注册用闭包保留上下文；函数式选项模式用闭包修改配置对象；计数器、缓存包装器、重试器可以用闭包封装少量私有状态。",
      "特殊情况：Go 1.22 起 for/range 循环变量按每轮独立作用域处理，减少闭包捕获循环变量的经典问题；使用 Go 1.21 及更早模块语义时，循环中启动 goroutine 或注册闭包仍需要显式复制变量；闭包捕获大对象、request、context 或 buffer 会延长它们的生命周期。",
      "常见误区：以为闭包捕获的是当时的值，会忽略后续共享修改；在并发闭包中读写同一变量会产生数据竞争；把大量逻辑塞进匿名函数会降低可读性；闭包持有外部对象会造成内存持续占用；循环中注册回调时忽略 Go 版本语义会得到意外结果。",
      "实战建议：闭包只捕获真正需要的变量；并发闭包中复制局部值或使用锁/channel；捕获大对象前先提取小字段；中间件和函数选项保持短小；维护旧版本代码时在循环体内写 `v := v` 固定每轮值；用 `go test -race` 和 `-gcflags=-m` 检查竞争与逃逸。",
      "参考来源：本讲解参考 Go Spec、Go Blog Fixing For Loops in Go 1.22、Go FAQ 栈与堆说明、Effective Go 和 Go Documentation，右侧来源支撑闭包定义、变量共享、生命周期、循环变量语义和逃逸分析。",
    ],
    typicalProblems: [
      "闭包捕获的是变量还是值",
      "闭包为什么会延长外层变量生命周期",
      "Go 1.22 的循环变量变化对闭包有什么影响",
      "闭包中共享变量并发读写有什么风险",
      "HTTP 中间件为什么常用闭包",
      "如何判断闭包导致的逃逸和内存持有问题",
    ],
    commonIssues: ["共享变量数据竞争", "旧版本循环变量捕获", "闭包持有大对象", "逃逸导致分配增加", "匿名函数过长", "回调生命周期不清晰"],
    practiceAdvice: ["只捕获必要变量", "并发闭包中复制局部值", "用 go test -race 检查竞争", "用 -gcflags=-m 看逃逸", "中间件和函数选项保持短小"],
    commands: ["go test -race ./...", "go test -gcflags=-m ./..."],
    prerequisites: ["function", "variable"],
    related: ["goroutine", "escape-analysis", "http-middleware"],
    order: 19,
  }),
  /* <!-- KG_REVIEWED: 指针 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "pointer",
    zh: "指针",
    en: "Pointer",
    area: "type-system",
    difficulty: "medium",
    summary: "指针保存变量地址，让函数、方法和数据结构共享同一份状态，同时带来 nil、逃逸和并发写入边界。",
    concept: "保存变量地址，用于共享状态、减少复制、表达可选引用和支持递归数据结构。",
    focus: "取址、解引用、nil、指针参数、指针接收者、比较、逃逸分析、递归类型、unsafe.Pointer 和并发安全",
    mechanism: "指针类型 `*T` 的值指向一个 T 类型变量；`&x` 取得地址，`*p` 访问指向的变量；未初始化指针为 nil；函数参数按值传递指针时复制地址，因此调用方和被调用方可以通过同一地址观察或修改同一对象。",
    scenarios: ["结构体修改", "大对象传递", "可选字段", "方法接收者", "链表和树等递归结构"],
    pitfalls: "nil 解引用、共享状态竞争、逃逸成本、指针接收者滥用、指针比较误判、unsafe 转换和长期持有大对象",
    sourceRefs: ["go-spec", "go-faq-stack-heap", "go-unsafe-package", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：指针是保存变量地址的值。它让多个函数或对象访问同一份数据，适合修改大结构体、表达可选对象、实现链表树结构，以及让方法拥有修改接收者状态的能力。",
      "工作机制：`*T` 表示指向 T 的指针类型，`&x` 取得变量地址，`*p` 读写指针指向的变量。指针本身仍然按值传递，传参时复制的是地址值；两个指针可以比较，只有指向同一变量或都为 nil 时相等。",
      "适用场景：函数需要修改调用方对象时使用指针参数；方法需要改变接收者状态或避免复制大对象时使用指针接收者；JSON/数据库模型里用指针表示可选字段；递归结构中用指针打破类型直接包含自身的限制。",
      "特殊情况：Go 支持指针，普通代码没有指针算术；指针与整数转换需要 unsafe，并且受垃圾回收、对象移动假设、对齐和生命周期规则约束；局部变量取地址返回是安全的，编译器会通过逃逸分析决定存储位置；指向零大小变量的不同指针比较结果有特殊边界。",
      "常见误区：以为传指针就一定更快会导致不必要的共享和堆分配；nil 指针调用方法时，方法内部访问接收者字段仍会 panic；把指针暴露给多个 goroutine 写入会产生数据竞争；把指针长期保存在缓存或闭包中会延长对象生命周期。",
      "实战建议：小值和不可变语义优先按值传递；需要修改状态、避免大对象复制或表达可选值时使用指针；指针接收者和值接收者在同一类型上保持一致；并发共享指针配合 mutex、channel 或原子操作；怀疑分配时用 `go test -gcflags=-m` 观察逃逸。",
      "参考来源：本讲解参考 Go Spec、Go FAQ 栈与堆说明、unsafe 包文档、Effective Go 和 Go Documentation，右侧来源支撑指针类型、nil、比较、逃逸分析、unsafe 边界和工程实践。",
    ],
    typicalProblems: [
      "Go 指针和 C 指针有什么核心差异",
      "函数参数传指针时到底复制了什么",
      "什么时候应该使用指针接收者",
      "局部变量地址返回为什么是安全的",
      "nil 指针方法调用有哪些边界",
      "指针共享状态在并发中如何保证安全",
    ],
    commonIssues: ["nil 解引用", "不必要指针导致逃逸", "指针共享造成数据竞争", "指针接收者和值接收者混用", "unsafe.Pointer 滥用", "长期持有对象导致内存保留"],
    practiceAdvice: ["小值优先按值传递", "需要修改状态时使用指针", "统一同一类型的方法接收者选择", "并发共享指针加同步", "用 -gcflags=-m 分析逃逸"],
    commands: ["go test -race ./...", "go test -gcflags=-m ./..."],
    prerequisites: ["variable", "basic-types"],
    related: ["struct", "method", "escape-analysis"],
    order: 20,
  }),
  /* <!-- KG_REVIEWED: 数组与切片 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "array-slice",
    zh: "数组与切片",
    en: "Array and Slice",
    area: "type-system",
    difficulty: "easy",
    summary: "数组是固定长度值，切片是指向底层数组片段的描述符，Go 中大多数动态序列都用切片表达。",
    concept: "用固定数组和动态切片管理连续元素，兼顾性能、可变长度和内存共享。",
    focus: "数组值语义、长度属于类型、切片头、len、cap、底层数组、make、append、copy、full slice expression 和内存保留",
    mechanism: "数组值直接包含固定数量元素，长度是类型的一部分；切片包含指向底层数组的指针、长度和容量；切片操作创建新的描述符并共享底层数组；append 在容量足够时复用底层数组，容量不足时分配新数组并复制元素。",
    scenarios: ["列表处理", "批量读写", "缓存缓冲区", "序列化", "分页和窗口数据"],
    pitfalls: "底层数组共享、append 后引用变化、越界访问、大切片保留内存、nil/empty 差异、并发修改和数组复制成本",
    sourceRefs: ["go-spec", "go-blog-slices", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：数组和切片都表示连续元素。数组长度固定，值本身包含所有元素；切片长度可变，是对底层数组某个连续片段的描述，是 Go 日常使用最多的序列结构。",
      "工作机制：`[4]int` 和 `[5]int` 是不同数组类型，数组赋值或传参会复制整个数组。`[]int` 是切片类型，零值是 nil；切片有 len 和 cap，切片表达式会创建新切片并共享底层数组；make 会分配底层数组并返回切片；append 返回更新后的切片值。",
      "适用场景：数组适合固定大小、值语义明确的数据，例如哈希摘要 `[32]byte`；切片适合动态列表、请求批量、缓冲区、序列化结果和分页窗口；copy 适合复制片段、去除底层数组共享或处理重叠区域。",
      "特殊情况：append 后必须接收返回值，因为底层数组可能已变化；子切片会保留整个底层数组，长文件或大 buffer 中截取小片段返回时需要 copy；full slice expression `s[i:j:k]` 可以限制容量，减少后续 append 覆盖共享数组；nil slice 和 empty slice 在 len 上相同，在 JSON 等编码场景可能表现不同。",
      "常见误区：把切片传参理解成传引用会忽略切片头复制；在函数里 append 后没返回新切片会丢失长度变化；多个切片共享底层数组时修改元素会互相影响；遍历时追加同一个切片可能让逻辑难以预测；大切片取小片段长期保存会造成内存保留。",
      "实战建议：append 后始终使用返回值；需要隔离数据时用 `clone := append([]T(nil), s...)` 或 copy；返回大 buffer 的小片段前复制；预估容量时用 make 指定 cap 减少扩容；需要稳定 JSON 空数组时初始化为 `[]T{}`；并发读写切片底层数组时加同步。",
      "参考来源：本讲解参考 Go Spec、Go Blog Go Slices: usage and internals、Effective Go 和 Go Documentation，右侧来源支撑数组类型、切片描述符、len/cap、append/copy、共享底层数组和内存保留边界。",
    ],
    typicalProblems: [
      "数组和切片的核心区别是什么",
      "切片传参时复制了什么",
      "append 为什么一定要接收返回值",
      "子切片为什么可能保留整个底层数组",
      "nil slice 和 empty slice 有哪些行为差异",
      "如何避免多个切片共享底层数组造成意外修改",
    ],
    commonIssues: ["append 返回值未接收", "底层数组共享导致串改", "大数组被小切片保留", "数组传参复制成本", "nil/empty 编码差异", "并发修改切片数据竞争"],
    practiceAdvice: ["append 后接收返回值", "需要隔离时显式 copy", "预估容量时用 make 指定 cap", "返回小片段前复制大 buffer", "并发共享切片时加同步"],
    prerequisites: ["basic-types"],
    related: ["map", "range-loop", "gc"],
    order: 21,
  }),
  /* <!-- KG_REVIEWED: range 循环 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "range-loop",
    zh: "range 循环",
    en: "range Loop",
    area: "syntax",
    difficulty: "medium",
    summary: "range 按数据结构生成迭代值，让数组、切片、字符串、map、channel、整数和迭代函数拥有统一遍历写法。",
    concept: "按集合或生成器产生迭代变量，简化数组、切片、map、字符串、通道、整数和迭代函数遍历。",
    focus: "索引和值、值副本、字符串 rune、map 顺序、channel 关闭、nil 行为、Go 1.22 循环变量、range integer 和 Go 1.23 iterator function",
    mechanism: "range 在循环开始前求值右侧表达式，按表达式类型产生 0 到 2 个迭代值：数组/切片给索引和值，字符串给字节索引和 rune，map 给键和值，channel 给接收值，整数给 0..n-1，迭代函数通过 yield 产生值。",
    scenarios: ["集合遍历", "文本扫描", "管道消费", "批处理", "自定义迭代器"],
    pitfalls: "值副本修改、循环变量版本语义、map 顺序、字符串索引含义、nil channel 阻塞、遍历时修改 map 和迭代函数 yield 约束",
    sourceRefs: ["go-spec", "go-blog-loopvar", "go-blog-strings", "go-blog-slices", "go-effective-go"],
    explanation: [
      "概念定义：range 是 Go 的统一遍历语法。它让不同数据结构用相似形式产生迭代变量，同时保留每种类型自己的语义。",
      "工作机制：数组和切片 range 产生 int 索引和元素值副本；字符串 range 产生 UTF-8 编码中每个 rune 的起始字节索引和 rune 值；map range 产生 key 和 value；channel range 持续接收直到通道关闭；Go 1.22 支持对整数 n 产生 0 到 n-1；Go 1.23 支持对特定签名的迭代函数 range。",
      "适用场景：遍历切片做批量处理；扫描字符串处理 Unicode；消费 channel 管道；遍历 map 做统计或转换；自定义容器可以用迭代函数暴露遍历能力。",
      "特殊情况：range 表达式通常在循环开始前求值一次；nil slice 和 nil map 迭代 0 次；nil channel 会永久阻塞；map 遍历顺序没有稳定承诺，遍历期间删除尚未到达的 key 会跳过它，新增 key 可能出现也可能跳过；Go 1.22 起 `:=` 声明的迭代变量每轮独立。",
      "常见误区：修改 range 得到的元素值副本不会改切片里的 struct 元素；拿 range 变量地址会得到循环变量地址语义，需要按版本和写法判断；用 map 遍历顺序做测试会产生不稳定结果；把字符串 range 的 index 当字符序号会在中文和 emoji 上出错；range channel 需要发送方关闭通道才能结束。",
      "实战建议：需要修改切片元素时用索引访问 `s[i]`；map 输出先收集 key 并排序；文本处理区分字节索引、rune 序号和用户感知字符；channel range 搭配明确关闭责任；维护 Go 1.21 及更早代码时，goroutine 或闭包内使用迭代变量前显式复制。",
      "参考来源：本讲解参考 Go Spec、Go Blog Fixing For Loops in Go 1.22、Go Blog Strings、Go Blog Slices 和 Effective Go，右侧来源支撑 range 类型表、版本变化、字符串遍历、切片值副本和工程边界。",
    ],
    typicalProblems: [
      "range 切片时拿到的是元素副本还是元素引用",
      "range string 的 index 和 value 分别是什么",
      "range map 的顺序为什么不能依赖",
      "range channel 什么时候结束",
      "Go 1.22 的迭代变量语义变化影响了什么",
      "Go 1.23 的 iterator function range 适合什么场景",
    ],
    commonIssues: ["修改元素副本无效", "map 遍历顺序依赖", "字符串索引含义误判", "nil channel range 阻塞", "旧版本循环变量捕获", "channel 未关闭导致循环不退出"],
    practiceAdvice: ["修改切片元素时使用索引", "map 结果输出前排序 key", "文本遍历时明确 byte/rune 需求", "channel range 明确关闭方", "旧版本闭包中复制迭代变量"],
    prerequisites: ["control-flow", "array-slice"],
    related: ["map", "channel", "closure"],
    order: 22,
  }),
  /* <!-- KG_REVIEWED: map | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "map",
    zh: "map",
    en: "Map",
    area: "type-system",
    difficulty: "easy",
    summary: "map 用可比较 key 快速关联 value，是 Go 中做索引、计数、缓存和去重的核心结构。",
    concept: "用哈希表按键关联值，支持快速查找、插入、删除和存在性判断。",
    focus: "key 可比较性、nil map、make、len、读缺失键零值、comma ok、delete、clear、range 无序、并发安全和 sync.Map",
    mechanism: "map 是引用语义的数据结构，key 类型必须支持 == 和 !=；读取不存在 key 会得到元素类型零值，双赋值形式返回 ok；make 创建可写 map；delete 删除 key；clear 清空 map；遍历顺序没有稳定承诺。",
    scenarios: ["索引缓存", "计数统计", "去重", "配置表", "按 ID 关联对象"],
    pitfalls: "并发读写、缺失键判断、map 值不可取址、遍历顺序依赖、nil map 写入 panic、interface key 动态类型不可比较和结构体 key 设计不当",
    sourceRefs: ["go-spec", "go-sync-map", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：map 是 Go 的键值关联结构，用一个可比较的 key 找到对应 value。它适合做索引、缓存、计数、去重、配置映射和对象关联。",
      "工作机制：map key 类型必须能完整定义 == 和 !=，因此 slice、map、function 不能直接做 key。未初始化 map 的零值是 nil，读取表现像空 map，写入会 panic；用 make 或字面量创建后才能写。`v, ok := m[k]` 能区分 key 缺失和 value 刚好是零值。",
      "适用场景：`map[string]int` 做词频统计；`map[id]User` 做索引；`map[T]struct{}` 做集合；`map[string]func()` 做路由或命令表；`map[string]any` 可承载动态 JSON，但业务核心模型优先用结构体。",
      "特殊情况：map 元素不可取址，因为扩容和重排会改变内部存储位置；range map 顺序没有稳定承诺；遍历时删除尚未访问的 key 会跳过它，新增 key 可能被访问；interface 作为 key 时，动态值本身也必须可比较；Go 1.21 引入 clear 内建函数清空 map。",
      "常见误区：用 `m[k] == 0` 判断 key 是否存在会混淆缺失和零值；依赖 map 遍历顺序会让测试和输出不稳定；多个 goroutine 同时读写普通 map 会触发数据竞争甚至运行时崩溃；把复杂可变对象编码成 key 会让相等性和哈希语义难以维护。",
      "实战建议：存在性判断用 comma ok；稳定输出先收集 key 并排序；并发访问普通 map 用 mutex/RWMutex 保护；读多写少且 key 写入后长期读取的特殊场景可考虑 sync.Map；集合用 `map[T]struct{}`；结构体做 key 时只放稳定且可比较字段。",
      "参考来源：本讲解参考 Go Spec、sync.Map 文档、Effective Go 和 Go Documentation，右侧来源支撑 map 类型、key 约束、nil 行为、index 表达式、delete/clear、遍历顺序和并发场景。",
    ],
    typicalProblems: [
      "哪些类型可以作为 map key",
      "读取缺失 key 为什么返回零值",
      "comma ok 如何区分缺失和零值",
      "为什么 map 元素不能取地址",
      "map 遍历顺序为什么不能依赖",
      "普通 map、mutex map 和 sync.Map 如何选择",
    ],
    commonIssues: ["缺失 key 与零值混淆", "nil map 写入 panic", "map 并发读写", "遍历顺序依赖", "map 元素取址失败", "interface key 动态类型不可比较"],
    practiceAdvice: ["存在性判断用 comma ok", "稳定输出先排序 key", "并发访问用锁保护", "特殊读多写少场景评估 sync.Map", "结构体 key 保持字段稳定可比较"],
    prerequisites: ["basic-types"],
    related: ["struct", "sync-map", "range-loop"],
    order: 23,
  }),
  /* <!-- KG_REVIEWED: struct | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "struct",
    zh: "struct",
    en: "Struct",
    area: "type-system",
    difficulty: "easy",
    summary: "struct 把多个具名字段组合成一个值，是 Go 表达业务实体、配置、DTO 和内部状态的核心类型。",
    concept: "把多个字段组合成业务实体或数据载体，并通过字段、标签、嵌入和方法表达结构化语义。",
    focus: "字段、零值、复合字面量、导出字段、struct tag、嵌入、字段提升、可比较性、内存布局、JSON/反射和复制成本",
    mechanism: "struct 类型由一组唯一字段组成，每个字段有名称和类型，也可以嵌入类型形成 promoted fields/methods；字段标签是字符串字面量，可通过反射读取；当所有字段都可比较时 struct 才可比较。",
    scenarios: ["业务模型", "JSON DTO", "配置结构", "领域实体", "状态聚合"],
    pitfalls: "导出字段过多、标签拼写、零值语义、复制成本、嵌入冲突、不可比较字段、反射库访问限制和内存对齐浪费",
    sourceRefs: ["go-spec", "go-encoding-json", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：struct 是 Go 的结构化数据类型，用字段把多个值组合成一个整体。它常用来表达用户、订单、配置、请求响应、内部状态和领域对象。",
      "工作机制：struct 字段名在同一结构体内必须唯一；字段可以显式命名，也可以嵌入类型形成 promoted field/method；字段标签本身由语言保留并参与类型身份，encoding/json、ORM、校验库等通过反射读取标签；struct 值赋值和传参会复制整个结构体。",
      "适用场景：业务实体用 struct 承载稳定字段；API DTO 用导出字段和 json tag 控制编码；配置结构用零值表达默认行为；组合能力可用嵌入字段；map key 可用只包含可比较字段的 struct。",
      "特殊情况：struct 的未导出字段对其他包不可直接访问，encoding/json 默认只处理导出字段；嵌入字段会提升字段和方法，但复合字面量中不能用提升字段名初始化；包含 slice、map、function 的 struct 不可比较；递归结构需要通过指针、slice 或 map 间接包含自身。",
      "常见误区：把所有字段导出会扩大 API 和兼容成本；json tag 拼写错误会在运行时才暴露；用值传递大 struct 会增加复制成本；嵌入不是继承，过度嵌入会暴露过多方法；零值未设计好会让调用方必须记住初始化顺序。",
      "实战建议：先设计零值是否可用；公共 struct 字段加注释和标签测试；大结构体和可变状态优先用指针传递；内部字段保持小写并配构造函数或方法；标签变更加编码测试；关注字段顺序以减少 padding，性能敏感结构可用工具检查布局。",
      "参考来源：本讲解参考 Go Spec、encoding/json、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑 struct 语法、字段标签、嵌入、可比较性、导出字段和 JSON 编码行为。",
    ],
    typicalProblems: [
      "struct 字段标签由谁使用",
      "为什么 encoding/json 只处理导出字段",
      "嵌入字段和继承有什么差异",
      "struct 什么时候可以作为 map key",
      "值传递 struct 会带来什么复制成本",
      "如何设计零值可用的结构体",
    ],
    commonIssues: ["json tag 拼写错误", "导出字段过多", "大结构体频繁复制", "嵌入字段冲突", "不可比较字段影响 map key", "零值不可用"],
    practiceAdvice: ["公共字段写注释和标签测试", "大结构体用指针传递", "内部状态字段保持小写", "设计零值语义", "性能敏感结构关注字段排列"],
    prerequisites: ["basic-types"],
    related: ["method", "embedding", "json"],
    order: 24,
  }),
  /* <!-- KG_REVIEWED: 方法 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "method",
    zh: "方法",
    en: "Method",
    area: "function",
    difficulty: "easy",
    summary: "方法是带接收者的函数，用来把行为绑定到同包定义的类型上，并通过方法集参与接口实现。",
    concept: "把函数绑定到具体类型，表达类型行为、封装状态操作并支持接口多态。",
    focus: "receiver、base type、值接收者、指针接收者、方法集、接口满足、nil 接收者、泛型接收者和嵌入方法提升",
    mechanism: "方法声明在 func 后增加 receiver 参数，receiver 的基类型必须是同一包定义的类型或其指针；方法名绑定到 receiver 基类型；值接收者方法进入 T 和 *T 的方法集，指针接收者方法进入 *T 的方法集。",
    scenarios: ["实体行为", "接口实现", "链式调用", "封装状态修改", "领域对象 API"],
    pitfalls: "接收者选择混乱、值复制、nil 接收者、接口方法集误判、复制锁对象、过度链式调用和嵌入方法暴露",
    sourceRefs: ["go-spec", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：方法是带接收者的函数。它把行为放到某个类型名下，让调用方用 `value.Method()` 的形式表达对象能力。",
      "工作机制：receiver 是方法名前的特殊参数，基类型必须在同一包定义。值接收者会复制接收者值，指针接收者共享同一对象并可修改状态。方法集决定类型是否实现接口：T 的方法集包含值接收者方法，*T 的方法集包含值接收者和指针接收者方法。",
      "适用场景：struct 方法表达实体行为；指针接收者修改对象或避免复制大对象；值接收者适合小的不可变值，例如 time.Duration 风格；方法还可让类型满足 io.Reader、fmt.Stringer、json.Marshaler 等接口。",
      "特殊情况：nil 指针也可以调用方法，只要方法内部能处理 nil 接收者；接口赋值会受方法集影响，只有 T 值时不能满足只含指针接收者方法的接口；嵌入字段会提升方法，可能让外层类型意外满足接口；泛型类型的方法 receiver 要声明对应类型参数。",
      "常见误区：同一类型混用值接收者和指针接收者会让接口满足关系难以判断；给包含 mutex 的类型写值接收者会复制锁；为简单 getter/setter 创建大量方法会膨胀 API；用链式方法隐藏错误会降低可诊断性。",
      "实战建议：需要修改状态、包含锁、包含大字段或需要保持对象身份时用指针接收者；小且不可变的值类型可用值接收者；同一类型上接收者选择保持一致；导出方法写 doc comment；接口满足关系用编译期断言确认，例如 `var _ io.Reader = (*T)(nil)`。",
      "参考来源：本讲解参考 Go Spec、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑方法声明、receiver 规则、方法集、接口满足和 API 文档实践。",
    ],
    typicalProblems: [
      "方法和普通函数的区别是什么",
      "值接收者和指针接收者如何影响方法集",
      "为什么 T 和 *T 实现接口的结果可能不同",
      "包含 mutex 的类型为什么通常使用指针接收者",
      "nil 指针接收者方法调用有哪些边界",
      "嵌入字段的方法提升会带来什么影响",
    ],
    commonIssues: ["接收者选择不一致", "复制大对象或锁", "接口满足误判", "nil 接收者 panic", "导出方法缺少注释", "嵌入导致方法暴露扩大"],
    practiceAdvice: ["同一类型接收者保持一致", "可变状态使用指针接收者", "接口实现用编译期断言", "导出方法写 doc comment", "包含锁的类型避免值接收者"],
    prerequisites: ["struct", "function"],
    related: ["interface", "pointer", "receiver"],
    order: 25,
  }),
  /* <!-- KG_REVIEWED: 接收者 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "receiver",
    zh: "接收者",
    en: "Receiver",
    area: "function",
    difficulty: "medium",
    summary: "接收者决定方法调用时绑定哪个类型，以及调用过程复制值还是共享对象。",
    concept: "决定方法所属类型、调用语义、状态修改能力和接口方法集。",
    focus: "receiver declaration、receiver base type、值接收者、指针接收者、方法集、一致性、复制成本、mutex、接口赋值和泛型 receiver",
    mechanism: "接收者是方法名前的单个非可变参数；其基类型必须是同一包定义的类型；值接收者在调用时复制接收者，指针接收者复制地址并共享对象；接收者选择会进入方法集规则，从而影响接口实现。",
    scenarios: ["大结构体方法", "状态修改", "接口适配", "并发对象封装", "不可变值对象"],
    pitfalls: "混用接收者、复制锁对象、接口赋值失败、并发状态暴露、nil 指针处理、方法值捕获接收者副本和泛型 receiver 语法误用",
    sourceRefs: ["go-spec", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：接收者是方法所属对象的特殊参数。它决定这个方法绑定到哪个类型，也决定调用时使用值副本还是共享同一对象。",
      "工作机制：`func (v T) M()` 是值接收者，调用时复制 T；`func (p *T) M()` 是指针接收者，调用时复制地址。编译器在可寻址值上允许自动取址调用指针接收者方法，但接口赋值依然严格按方法集判断。",
      "适用场景：小型不可变值、枚举包装、时间间隔等适合值接收者；需要修改字段、避免复制大对象、包含 mutex 或保持对象身份时使用指针接收者；接口适配时根据接口方法集选择接收者类型。",
      "特殊情况：方法值 `x.M` 会在创建时保存接收者副本或地址，延迟调用时可能看到旧副本或共享新状态；nil 指针接收者可以进入方法，方法内部需要自己处理 nil；泛型类型方法的 receiver 需要声明对应类型参数；接收者名称建议短小且与类型相关。",
      "常见误区：同一类型部分方法用值接收者、部分用指针接收者，会让接口满足和调用语义难以预测；包含 sync.Mutex 的类型用值接收者会复制锁；为追求性能所有方法都用指针接收者，会增加共享可变状态和逃逸机会；把自动取址理解成接口赋值也会自动补齐，会得到编译错误。",
      "实战建议：先判断类型语义是值对象还是实体对象；包含锁、slice/map 指向可变状态、大字段或需要修改时统一用指针接收者；小值且不可变时用值接收者；公共类型用编译期断言检查接口满足；方法值跨 goroutine 使用前明确接收者是副本还是共享地址。",
      "参考来源：本讲解参考 Go Spec、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑 receiver 语法、基类型限制、值/指针接收者、方法集和工程选择规则。",
    ],
    typicalProblems: [
      "值接收者和指针接收者分别复制什么",
      "为什么包含 mutex 的类型要避免值接收者",
      "自动取址调用方法和接口赋值有什么差异",
      "方法值会如何保存接收者",
      "nil 指针接收者方法如何设计",
      "同一类型如何统一接收者选择",
    ],
    commonIssues: ["接收者混用", "复制锁对象", "接口实现判断错误", "方法值保存旧副本", "nil 指针处理缺失", "不必要共享可变状态"],
    practiceAdvice: ["按类型语义选择接收者", "包含锁或可变状态用指针接收者", "小值不可变类型用值接收者", "用编译期断言验证接口实现", "跨 goroutine 方法值明确共享边界"],
    prerequisites: ["method", "pointer"],
    related: ["interface", "mutex"],
    order: 26,
  }),
  /* <!-- KG_REVIEWED: interface | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "interface",
    zh: "interface",
    en: "Interface",
    area: "type-system",
    difficulty: "medium",
    summary: "interface 用方法集描述行为契约，Go 类型只要拥有对应方法就隐式满足它。",
    concept: "用方法集合描述行为契约，实现隐式多态、依赖反转和泛型类型约束。",
    focus: "方法集、隐式实现、小接口、空接口/any、接口值、动态类型、nil 接口、嵌入接口、类型集和约束接口",
    mechanism: "基本接口由方法列表定义类型集，非接口类型的方法集包含这些方法时即实现该接口；接口变量保存动态类型和动态值；Go 1.18 后接口还可包含类型项，用于类型参数约束，这类接口通常只能作为约束使用。",
    scenarios: ["依赖反转", "测试替身", "标准库适配", "插件式扩展", "泛型约束"],
    pitfalls: "大接口、nil 接口陷阱、过早抽象、类型断言分散、约束接口误当值类型、接口返回具体实现和动态值不可比较",
    sourceRefs: ["go-spec", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：interface 是 Go 表达行为契约的类型。调用方只关心某个值能做什么，例如 Read、Write、ServeHTTP，而具体类型无需显式声明实现关系。",
      "工作机制：基本接口列出方法集，任何类型的方法集包含这些方法时就实现该接口。接口值在运行时由动态类型和动态值组成，零值接口为 nil。`any` 是空接口别名，可以保存任意非接口类型的值。",
      "适用场景：标准库用 io.Reader、io.Writer、http.Handler 等小接口连接组件；业务代码用接口隔离外部依赖便于测试；插件和中间件用接口统一扩展点；泛型中用包含类型集的接口约束类型参数可用的运算。",
      "特殊情况：一个包含 nil 指针的接口值本身通常是非 nil，因为它有动态类型；只含方法的基本接口可以作为变量类型，包含类型项或 union 的一般接口主要用于泛型约束；接口比较要求动态值可比较，否则运行时 panic；嵌入接口会合并方法集，方法名重复时签名必须一致。",
      "常见误区：过早设计大接口会让实现类型背负过多方法；在生产者一侧定义接口容易绑定具体实现，Go 更常见的做法是在消费方定义小接口；返回 interface 会隐藏具体能力和错误语义；把 nil 指针赋给接口后用 `err != nil` 类似判断会得到意外结果。",
      "实战建议：接口越小越容易复用，优先按调用方需要定义；函数参数接受接口，返回具体类型；用编译期断言确认实现关系；需要类型分支时集中使用 type switch；对可能装入 nil 指针的接口做测试；泛型约束接口和运行时值接口分开命名与使用。",
      "参考来源：本讲解参考 Go Spec、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑接口类型集、方法集、隐式实现、nil 接口、any、嵌入接口和约束接口规则。",
    ],
    typicalProblems: [
      "Go 的接口为什么是隐式实现",
      "interface 值内部的动态类型和动态值是什么",
      "为什么包含 nil 指针的接口值可能不等于 nil",
      "小接口设计为什么更符合 Go 风格",
      "基本接口和值接口、约束接口有什么差异",
      "接口比较在什么情况下会 panic",
    ],
    commonIssues: ["接口过大", "nil 接口判断错误", "过早抽象", "返回 interface 隐藏能力", "类型断言分散", "约束接口误当值类型"],
    practiceAdvice: ["在消费方定义小接口", "参数接收接口返回具体类型", "用编译期断言验证实现", "集中 type switch 分支", "区分运行时接口和泛型约束"],
    prerequisites: ["method"],
    related: ["type-assertion", "generics", "error-handling"],
    order: 27,
  }),
  /* <!-- KG_REVIEWED: 类型断言 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "type-assertion",
    zh: "类型断言",
    en: "Type Assertion",
    area: "type-system",
    difficulty: "medium",
    summary: "类型断言从接口值中取出期望的具体类型或接口类型，是处理动态值和错误类型识别的基础工具。",
    concept: "从接口值中取回期望的具体类型或接口类型，并在运行时验证动态类型是否匹配。",
    focus: "x.(T)、comma ok、panic、type switch、动态类型、接口目标类型、nil case、错误类型识别和泛型 case",
    mechanism: "对接口表达式 x 执行 `x.(T)` 时，运行时检查 x 的动态类型是否为 T，或是否实现接口 T；单返回形式失败会 panic；双返回形式返回值和 ok，失败时值为 T 的零值；type switch 用 `x.(type)` 按动态类型分支。",
    scenarios: ["错误类型识别", "协议解码", "插件返回值处理", "兼容旧接口", "any 值恢复具体类型"],
    pitfalls: "断言失败 panic、分支膨胀、接口设计过宽、业务类型泄漏、nil 接口分支遗漏和 generics 场景误用",
    sourceRefs: ["go-spec", "go-errors-package", "go-blog-errors-1-13", "go-effective-go"],
    explanation: [
      "概念定义：类型断言用于从接口值里取回期望类型。接口值本身只暴露接口方法，断言让代码在运行时确认它背后的动态类型并取得更具体的能力。",
      "工作机制：`v := x.(T)` 要求 x 是接口值，且动态类型满足 T；T 是具体类型时需要动态类型与 T 相同，T 是接口类型时需要动态类型实现 T。失败会 panic。`v, ok := x.(T)` 失败时 ok 为 false，v 是 T 的零值。",
      "适用场景：从 `any` 解出 JSON 或插件传回的具体值；判断 error 链中的具体错误类型；兼容旧 API 时按动态类型分支；在测试中确认返回值是否实现某个接口。",
      "特殊情况：type switch 的 `switch v := x.(type)` 会在每个 case 中赋予 v 不同静态类型；case nil 可匹配 nil 接口；type switch 的 case 可以使用类型参数或泛型类型，实例化后若重复，按第一个匹配 case 执行；断言对象不能是类型参数本身。",
      "常见误区：直接使用单返回断言会把普通输入差异变成 panic；到处写类型断言通常说明接口过宽；用类型断言处理 error 会漏掉包装链，errors.As 更适合错误类型提取；断言 nil 接口或包含 nil 指针的接口时需要先理解动态类型和值。",
      "实战建议：外部输入、插件返回和 any 值恢复优先使用 comma ok；多类型分支用 type switch 集中处理；错误类型识别优先用 errors.As；频繁断言的接口应重新设计成更小的行为接口；默认分支记录可诊断信息。",
      "参考来源：本讲解参考 Go Spec、errors 包、Go Blog Working with Errors in Go 1.13 和 Effective Go，右侧来源支撑类型断言语义、comma ok、type switch、错误类型提取和工程使用建议。",
    ],
    typicalProblems: [
      "类型断言和类型转换有什么区别",
      "x.(T) 失败时为什么会 panic",
      "comma ok 形式失败时返回什么",
      "type switch 中变量的静态类型如何变化",
      "错误类型识别为什么常用 errors.As",
      "频繁类型断言说明接口设计可能有什么问题",
    ],
    commonIssues: ["单返回断言 panic", "类型分支过散", "接口设计过宽", "错误链识别遗漏", "nil 接口判断混乱", "默认分支缺少诊断"],
    practiceAdvice: ["外部输入用 comma ok", "多类型集中到 type switch", "错误类型用 errors.As", "频繁断言时收窄接口", "默认分支记录动态类型"],
    prerequisites: ["interface"],
    related: ["error-handling", "generics"],
    order: 28,
  }),
  /* <!-- KG_REVIEWED: 泛型 | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "generics",
    zh: "泛型",
    en: "Generics",
    area: "type-system",
    difficulty: "medium",
    summary: "泛型让函数和类型接收类型参数，在保持静态类型安全的同时复用算法、容器和工具代码。",
    concept: "用类型参数复用算法和容器代码，让调用方提供具体类型，编译器检查约束和操作合法性。",
    focus: "type parameter、type argument、constraint、type set、instantiation、type inference、generic function、generic type、comparable 和可读性边界",
    mechanism: "泛型函数或类型声明类型参数列表，每个类型参数都有约束；调用或使用时用类型实参实例化；编译器把类型实参代入类型参数，检查它是否满足约束，并只允许泛型体内执行约束类型集共同支持的操作。",
    scenarios: ["通用集合", "算法复用", "类型安全工具函数", "库代码收敛", "map/slice 辅助函数"],
    pitfalls: "约束设计过宽、泛型滥用、可读性下降、接口职责混合、类型推断失败、丢失命名类型方法和 comparable 边界",
    sourceRefs: ["go-generics-tutorial", "go-blog-intro-generics", "go-spec", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：泛型是 Go 1.18 引入的类型参数机制。它让同一段代码可以服务一组类型，例如对 `map[K]V` 求和、实现通用 Set、写 slice 工具函数，同时保留编译期类型检查。",
      "工作机制：`func Sum[K comparable, V int64 | float64](m map[K]V) V` 中 K 和 V 是类型参数，`comparable` 和 `int64 | float64` 是约束。调用时编译器根据实参推断或使用显式类型实参完成实例化，并检查传入类型是否满足约束。",
      "适用场景：多种元素类型共享同一算法；容器库需要保留元素类型；map key 需要 comparable 约束；业务中多个类型拥有相同底层类型和操作时可以用约束复用逻辑。",
      "特殊情况：类型参数只能使用约束允许的操作；约束接口里的类型项通常用于类型参数约束；函数类型推断主要来自普通参数，返回值单独出现的类型参数常需显式指定；使用 `~[]E` 这类近似类型可保留调用方命名类型；comparable 与接口动态值仍有运行时边界。",
      "常见误区：把泛型当作替代接口的默认选择会让代码难读；约束写得太宽会允许无意义类型，太窄会降低复用；用泛型处理只有一两个调用点的业务逻辑会增加理解成本；返回 `[]E` 会丢失调用方命名 slice 类型的方法，必要时用 `S ~[]E`。",
      "实战建议：先写普通函数，出现真实重复后再抽泛型；约束命名表达可执行操作，例如 Ordered、Number、SliceLike；公共泛型 API 写示例和边界说明；优先让类型推断自然工作；编译错误复杂时把约束拆小并增加中间类型别名。",
      "参考来源：本讲解参考 Go 官方泛型教程、Go Blog An Introduction To Generics、Go Spec、Effective Go 和 Go Documentation，右侧来源支撑类型参数、约束、类型集、实例化、类型推断和生产使用边界。",
    ],
    typicalProblems: [
      "类型参数和普通参数有什么区别",
      "约束如何决定泛型函数体内可用操作",
      "类型实参什么时候可以省略",
      "comparable 为什么常用于 map key 泛型",
      "S ~[]E 这类约束解决什么问题",
      "泛型、interface 和代码生成如何选择",
    ],
    commonIssues: ["泛型滥用", "约束过宽或过窄", "类型推断失败", "命名类型信息丢失", "错误信息复杂", "公共 API 可读性下降"],
    practiceAdvice: ["先确认真实重复再抽泛型", "约束按操作能力命名", "公共泛型写示例", "优先依赖类型推断", "用近似类型保留命名类型"],
    prerequisites: ["interface", "function"],
    related: ["type-constraint", "array-slice", "map"],
    order: 29,
  }),
  /* <!-- KG_REVIEWED: 类型约束 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "type-constraint",
    zh: "类型约束",
    en: "Type Constraint",
    area: "type-system",
    difficulty: "medium",
    summary: "类型约束用接口的类型集限定类型参数，让泛型代码只执行所有允许类型共同支持的操作。",
    concept: "限定泛型类型参数可接受的类型集合和可用操作。",
    focus: "constraint interface、type set、method element、type element、union、~T、comparable、any、constraints 命名、交集和非基本接口",
    mechanism: "类型约束本质上是接口。接口元素可以是方法，也可以是类型项或 union；多个元素取类型集交集，union 取并集，`~T` 表示所有底层类型为 T 的类型；编译器根据最终类型集判断类型参数可用的运算。",
    scenarios: ["数字工具函数", "可比较键", "容器库", "领域泛型抽象", "保留命名类型的 slice/map 工具"],
    pitfalls: "约束命名含糊、类型集合泄漏、方法约束混杂、union 项重叠、约束接口误作值类型、可比较动态值边界和版本兼容",
    sourceRefs: ["go-spec", "go-generics-tutorial", "go-blog-intro-generics", "go-effective-go"],
    explanation: [
      "概念定义：类型约束是给泛型类型参数使用的接口。它回答两个问题：调用方可以传哪些类型，泛型函数内部可以对这些类型做哪些操作。",
      "工作机制：`interface{ ~int | ~int64 }` 的类型集包含底层类型为 int 或 int64 的命名与非命名类型；`comparable` 限定类型支持 == 和 !=，常用于 map key；方法元素和类型元素共同出现时取交集，表示既要有这些方法，也要落在这些类型集合里。",
      "适用场景：数字求和用 Number 约束；通用 Set 用 `K comparable`；保留命名 slice 类型的方法时用 `S ~[]E`；领域中多个 ID 类型底层都是 string 时可用 `~string` 写通用校验或格式化逻辑。",
      "特殊情况：包含类型项或 union 的接口是一般接口，通常只能作为类型约束，不能作为变量类型；union 中非接口类型项的类型集必须互不重叠；`~T` 中 T 的底层类型必须是 T 自身；union 中有多个项时受限于 comparable 和带方法接口的组合规则。",
      "常见误区：用 `interface{}` 或 any 当约束会让泛型体内几乎没有可用操作；约束命名成 T、AnyNumber 这类模糊名字会降低 API 可读性；约束过宽会让无意义类型进入，过窄会让命名类型失去复用；把约束接口当运行时接口会遇到编译限制。",
      "实战建议：约束按能力命名，例如 Ordered、Integer、SliceLike；优先用标准库 `cmp.Ordered`、`comparable` 和小型自定义约束；需要保留调用方命名类型时使用近似类型；公开约束前写示例，说明允许类型和操作；复杂约束先拆成小约束再组合。",
      "参考来源：本讲解参考 Go Spec、Go 官方泛型教程、Go Blog An Introduction To Generics 和 Effective Go，右侧来源支撑类型集、一般接口、union、近似类型、comparable 和约束设计建议。",
    ],
    typicalProblems: [
      "类型约束为什么本质上是接口",
      "~T 表示什么，什么时候使用",
      "union 类型项为什么要求互不重叠",
      "comparable 适合哪些泛型场景",
      "包含类型项的接口为什么通常只能作约束",
      "如何设计一个可读且不过度宽泛的约束",
    ],
    commonIssues: ["约束过宽", "约束过窄", "union 项重叠", "约束接口误作值类型", "命名含糊", "复杂约束错误信息难懂"],
    practiceAdvice: ["按可用操作命名约束", "优先使用 comparable 和标准约束", "用 ~T 保留命名类型", "复杂约束拆小组合", "公开约束配示例"],
    prerequisites: ["generics"],
    related: ["interface", "basic-types"],
    order: 30,
  }),
  /* <!-- KG_REVIEWED: 嵌入 | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "embedding",
    zh: "嵌入",
    en: "Embedding",
    area: "type-system",
    difficulty: "medium",
    summary: "嵌入用匿名字段组合结构体或接口能力，让字段和方法在外层类型上被提升访问。",
    concept: "通过匿名字段组合结构体或接口能力，实现组合复用、接口聚合和行为提升。",
    focus: "embedded field、promoted field、promoted method、selector depth、接口嵌入、命名冲突、方法集提升和复合字面量限制",
    mechanism: "struct 中只写类型名或 *T 作为字段时形成嵌入字段，未限定类型名成为字段名；被嵌入字段的方法和字段可在选择器路径唯一时提升到外层；interface 嵌入会合并方法集和类型集。",
    scenarios: ["组合复用", "接口聚合", "基础能力扩展", "测试替身", "包装标准库类型"],
    pitfalls: "方法冲突、API 暴露过多、隐式依赖、初始化含糊、nil 嵌入指针、复合字面量不能用提升字段名和接口意外满足",
    sourceRefs: ["go-spec", "go-effective-go", "go-doc-comments", "go-docs"],
    explanation: [
      "概念定义：嵌入是 Go 的组合机制。struct 可以嵌入匿名字段，interface 可以嵌入其他接口，外层类型由此获得字段、方法或约束能力的组合效果。",
      "工作机制：嵌入字段以类型名声明，例如 `Logger` 或 `*Logger`，字段名由未限定类型名决定。外层选择器 `x.M` 会按最浅深度查找字段或方法，唯一时可直接访问；若同一深度有多个同名候选，选择器非法。提升字段像普通字段一样可访问，但不能在外层 struct 的复合字面量里直接用提升字段名初始化。",
      "适用场景：用嵌入复用标准库类型方法；用接口嵌入组合小接口，例如 ReadWriter；包装实现时嵌入基础类型并覆写少数方法；测试替身可嵌入接口后只实现需要定制的部分。",
      "特殊情况：嵌入 T 和 *T 对外层类型的方法集影响不同；嵌入 *T 后，即使外层值本身非 nil，内部 *T 也可能是 nil，访问提升字段会 panic；嵌入会扩大外层公开 API，导出的嵌入字段及其方法可能让外层意外满足接口；接口嵌入重复方法时签名必须一致。",
      "常见误区：把嵌入当继承会误判初始化、覆写和可见性；为了省几行转发代码嵌入大对象会暴露过多方法；嵌入带状态对象会让生命周期和并发边界变隐蔽；复合字面量中直接写提升字段会编译失败。",
      "实战建议：优先把嵌入用于稳定、清晰的小能力组合；导出类型中嵌入其他类型前检查暴露 API；需要严格控制行为时显式写字段名并手动转发方法；嵌入指针字段时在构造函数中初始化；接口嵌入保持小接口组合。",
      "参考来源：本讲解参考 Go Spec、Effective Go、Go Doc Comments 和 Go Documentation，右侧来源支撑 embedded field、promoted selector、方法集提升、接口嵌入和工程 API 边界。",
    ],
    typicalProblems: [
      "嵌入字段和普通字段有什么区别",
      "promoted field/method 是如何解析的",
      "嵌入 T 和嵌入 *T 对方法集有什么影响",
      "为什么复合字面量不能直接使用提升字段名",
      "接口嵌入如何组合方法集",
      "嵌入为什么容易导致 API 暴露过多",
    ],
    commonIssues: ["同名提升冲突", "API 暴露扩大", "nil 嵌入指针 panic", "复合字面量初始化错误", "接口意外满足", "把嵌入误当继承"],
    practiceAdvice: ["嵌入前检查公开方法集", "需要控制 API 时显式字段加转发", "构造函数初始化嵌入指针", "接口嵌入保持小而清晰", "避免嵌入大型可变状态对象"],
    prerequisites: ["struct", "interface"],
    related: ["method", "visibility"],
    order: 31,
  }),
  /* <!-- KG_REVIEWED: goroutine | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "goroutine",
    zh: "goroutine",
    en: "goroutine",
    area: "concurrency",
    difficulty: "easy",
    summary: "goroutine 是 Go 的轻量并发执行单元，由 go 语句创建，由运行时调度到 OS 线程上执行。",
    concept: "用轻量执行单元并发运行函数，支撑高并发 I/O、后台任务和流水线处理。",
    focus: "go statement、函数参数求值、运行时调度、栈增长、生命周期、泄漏、panic 边界、context 取消、pprof 和 trace",
    mechanism: "go 语句启动一次函数调用作为独立 goroutine；函数值和参数在创建方 goroutine 中先求值；新 goroutine 独立执行，返回值会被丢弃；运行时把大量 goroutine 调度到较少 OS 线程上，并按需要增长栈。",
    scenarios: ["并发请求", "后台任务", "流水线处理", "异步 I/O", "超时与取消控制"],
    pitfalls: "生命周期失控、阻塞泄漏、panic 边界、共享状态竞争、无界创建、context 未传递和关闭顺序混乱",
    sourceRefs: ["go-spec", "go-diagnostics", "go-concurrency-pipelines", "go-blog-defer-panic-recover", "go-docs"],
    explanation: [
      "概念定义：goroutine 是 Go 运行时管理的轻量并发执行单元。它让一个函数和当前流程并发执行，适合处理 I/O 等待、并发请求、后台任务和流水线步骤。",
      "工作机制：`go f(x)` 会在当前 goroutine 中先求值 f 和 x，然后启动新的 goroutine 执行调用。调用方继续向下执行，不等待 f 完成；被启动函数的返回值会被丢弃。运行时负责调度、栈增长、网络轮询和阻塞管理。",
      "适用场景：每个连接或请求启动处理 goroutine；后台定时同步；fan-out/fan-in 并发任务；生产者消费者流水线；配合 context 实现取消、超时和请求生命周期控制。",
      "特殊情况：main 返回后进程结束，其他 goroutine 会被终止；panic 只会在当前 goroutine 展开，父 goroutine 无法 recover 子 goroutine 的 panic；goroutine 很轻量但仍消耗栈、调度和引用对象；阻塞在 channel、锁、网络或 timer 上的 goroutine 会持续占用资源。",
      "常见误区：把 goroutine 当作无限资源会造成无界并发；启动后缺少退出条件会泄漏；只关闭数据 channel 却没有取消上游会让发送方阻塞；多个 goroutine 共享变量未同步会产生数据竞争；在 goroutine 中 panic 依赖外层 recover 会导致进程崩溃。",
      "实战建议：每个 goroutine 都要有明确退出条件；请求链路传递 context；并发数量用 worker pool、semaphore 或 errgroup 控制；共享状态用 channel、mutex 或 atomic；泄漏排查看 runtime.NumGoroutine、goroutine profile、block profile、trace 和 GODEBUG=schedtrace。",
      "参考来源：本讲解参考 Go Spec、Go Diagnostics、Go Blog Pipelines and cancellation、Go Blog Defer, Panic, and Recover 和 Go Documentation，右侧来源支撑 go 语句语义、诊断工具、取消模式、panic 边界和工程治理建议。",
    ],
    typicalProblems: [
      "go 语句启动 goroutine 时参数什么时候求值",
      "goroutine 和 OS 线程是什么关系",
      "goroutine 泄漏通常有哪些表现",
      "父 goroutine 能否 recover 子 goroutine 的 panic",
      "如何控制无界 goroutine 创建",
      "线上 goroutine 数持续增长时怎么排查",
    ],
    commonIssues: ["无界创建", "channel 阻塞泄漏", "context 未传递", "共享变量竞争", "panic 边界缺失", "main 提前退出"],
    practiceAdvice: ["为每个 goroutine 设计退出条件", "传递 context 控制生命周期", "用 worker pool 限制并发", "用 go test -race 检查竞争", "用 goroutine profile 和 trace 排查泄漏"],
    commands: ["go test -race ./...", "go tool pprof", "go test -trace trace.out ./...", "GODEBUG=schedtrace=1000 ./app"],
    prerequisites: ["function"],
    related: ["channel", "scheduler", "context"],
    order: 32,
  }),
  /* <!-- KG_REVIEWED: channel | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "channel",
    zh: "channel",
    en: "Channel",
    area: "concurrency",
    difficulty: "medium",
    summary: "channel 在 goroutine 之间传递值并同步进度，是 Go 组织并发协作和流水线的重要原语。",
    concept: "在 goroutine 之间传递值、同步执行进度，并表达数据流结束信号。",
    focus: "channel type、方向、make、无缓冲、缓冲、发送、接收、close、range、nil channel、阻塞语义和泄漏",
    mechanism: "channel 有元素类型和可选方向；make 创建可用通道并可指定缓冲容量；发送和接收会阻塞直到通信可进行；无缓冲通道同步交接，缓冲通道在容量内暂存值；关闭通道表示后续不会再发送。",
    scenarios: ["任务队列", "结果汇聚", "事件通知", "流水线", "取消广播"],
    pitfalls: "关闭方职责、阻塞发送、重复关闭、发送到已关闭通道、nil channel 阻塞、缓冲容量掩盖背压和 goroutine 泄漏",
    sourceRefs: ["go-spec", "go-concurrency-pipelines", "go-diagnostics", "go-docs"],
    explanation: [
      "概念定义：channel 是 Go 的并发通信原语。它让 goroutine 通过发送和接收值协作，同时把同步点和数据流边界写进代码结构。",
      "工作机制：`chan T` 可收可发，`chan<- T` 只发，`<-chan T` 只收。无缓冲 channel 需要发送方和接收方同时就绪；缓冲 channel 在容量内接收发送值，满时发送阻塞，空时接收阻塞。接收表达式可以用 `v, ok := <-ch` 判断通道是否已关闭且耗尽。",
      "适用场景：worker pool 用 channel 分发任务；fan-in 用 channel 汇总结果；pipeline 用 channel 连接阶段；事件通知可用 `chan struct{}`；只读和只写方向常用于函数签名表达所有权和约束能力。",
      "特殊情况：nil channel 上发送和接收会永久阻塞，select 中可利用 nil channel 动态禁用 case；关闭已关闭 channel 或发送到已关闭 channel 会 panic；接收已关闭且已耗尽的 channel 会立即返回元素零值和 ok=false；只有发送方或拥有发送所有权的一方适合 close。",
      "常见误区：把 close 当成给接收方发送一个值会混淆语义；多个发送方争抢 close 会引发 panic；缓冲容量设大只能暂时吸收压力，无法替代消费能力；忘记监听 context 或 done 会让上游发送方阻塞；range channel 需要通道关闭才能结束。",
      "实战建议：明确 channel 所有权，谁发送谁负责关闭；任务取消用 context 或 done channel；结果 channel 搭配 WaitGroup 在所有发送者退出后关闭；容量按背压和吞吐设计；用 goroutine profile、block profile 和 trace 排查阻塞与泄漏。",
      "参考来源：本讲解参考 Go Spec、Go Blog Pipelines and cancellation、Go Diagnostics 和 Go Documentation，右侧来源支撑 channel 类型、发送接收、关闭语义、nil 行为、流水线取消和诊断方法。",
    ],
    typicalProblems: [
      "无缓冲 channel 和缓冲 channel 的同步语义有什么差异",
      "关闭 channel 后继续接收会得到什么",
      "为什么发送到已关闭 channel 会 panic",
      "nil channel 在 select 中有什么用途",
      "多个发送方场景下谁应该 close channel",
      "channel 阻塞导致 goroutine 泄漏时怎么排查",
    ],
    commonIssues: ["重复 close", "发送到已关闭通道", "nil channel 永久阻塞", "缓冲容量误用", "range 等不到关闭", "发送方泄漏"],
    practiceAdvice: ["由发送方关闭 channel", "用 context 控制取消", "多发送者先汇合再 close", "用方向类型表达只读只写", "用 block profile 和 trace 查阻塞"],
    commands: ["go test -race ./...", "go tool pprof", "go test -trace trace.out ./..."],
    prerequisites: ["goroutine"],
    related: ["select", "pipeline", "context"],
    order: 33,
  }),
  /* <!-- KG_REVIEWED: select | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "select",
    zh: "select",
    en: "select",
    area: "concurrency",
    difficulty: "medium",
    summary: "select 在多个 channel 发送或接收操作之间选择可执行分支，是超时、取消和多路事件处理的核心语句。",
    concept: "在多个 channel 操作之间选择可执行分支，组织超时、取消、多路事件和非阻塞尝试。",
    focus: "CommClause、send/receive case、default、伪随机选择、case 表达式求值、nil channel、超时、取消和忙轮询",
    mechanism: "进入 select 时，所有 case 的 channel 表达式和发送右值按源码顺序求值一次；若多个通信可执行，运行时伪随机选择一个；若没有可执行通信且有 default，执行 default；若没有 default，当前 goroutine 阻塞。",
    scenarios: ["超时控制", "取消监听", "多路事件", "心跳机制", "非阻塞发送或接收"],
    pitfalls: "default 忙轮询、遗漏取消分支、nil channel 控制失误、case 表达式副作用、closed channel 一直就绪、timer 泄漏和饥饿误判",
    sourceRefs: ["go-spec", "go-concurrency-pipelines", "go-diagnostics", "go-docs"],
    explanation: [
      "概念定义：select 是 Go 针对 channel 的多路选择语句。它让一个 goroutine 同时等待多个发送、接收、超时、取消或默认分支。",
      "工作机制：select 的每个 case 必须是发送或接收操作。进入 select 时，各 case 的 channel 和发送值会先求值；随后运行时检查哪些通信可以立即执行。多个 case 同时就绪时，选择是统一伪随机的；所有 case 都阻塞时，select 阻塞等待；有 default 时立即走 default。",
      "适用场景：`select { case <-ctx.Done(): ... case v := <-ch: ... }` 处理取消；配合 time.After 或 timer 做超时；事件循环监听多个 channel；非阻塞发送/接收用 default；把 channel 设为 nil 可以临时禁用某个 case。",
      "特殊情况：nil channel 的通信永远不能就绪，只有 nil channel 且无 default 的 select 会永久阻塞；closed channel 的接收会立即就绪，循环 select 中需要处理 ok=false 后把 channel 置 nil；default 放在循环里容易形成忙轮询；time.After 在高频循环里会反复创建 timer，性能敏感场景用 NewTimer 并复用。",
      "常见误区：认为 select 会按 case 顺序优先选择会导致错误设计；在 select 外创建发送值有副作用时，未选中 case 也可能已经计算了发送右值；忘记监听 ctx.Done 会让 goroutine 无法退出；closed channel 一直就绪会让其他 case 机会变少；default 会把阻塞等待变成主动轮询。",
      "实战建议：每个长期运行的 select 循环都放取消分支；需要禁用 case 时把 channel 设为 nil；处理关闭通道时收到 ok=false 后清理状态；低延迟非阻塞逻辑用 default，普通等待逻辑让 select 阻塞；高频超时用可停止和复用的 timer。",
      "参考来源：本讲解参考 Go Spec、Go Blog Pipelines and cancellation、Go Diagnostics 和 Go Documentation，右侧来源支撑 select 执行步骤、随机选择、nil channel、取消模式和诊断方法。",
    ],
    typicalProblems: [
      "select 同时有多个就绪 case 时如何选择",
      "select 进入时哪些表达式会先求值",
      "default 分支适合什么场景",
      "nil channel 如何动态禁用 case",
      "closed channel 在 select 中为什么会一直就绪",
      "select 循环如何正确处理 context 取消和超时",
    ],
    commonIssues: ["default 忙轮询", "取消分支缺失", "closed channel 一直就绪", "case 求值副作用", "timer 高频创建", "nil channel 永久阻塞"],
    practiceAdvice: ["长期循环保留取消分支", "用 nil channel 禁用已结束 case", "处理 ok=false 后清理 channel", "高频超时复用 timer", "避免 default 造成忙轮询"],
    prerequisites: ["channel"],
    related: ["context", "timer-ticker", "pipeline"],
    order: 34,
  }),
  /* <!-- KG_REVIEWED: context | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "context",
    zh: "context",
    en: "Context",
    area: "concurrency",
    difficulty: "medium",
    summary: "context 在请求链路中携带取消信号、deadline 和少量跨 API 范围值，是 Go 服务治理 goroutine 生命周期的标准方式。",
    concept: "在请求链路中传递取消信号、超时、deadline 和少量请求范围值。",
    focus: "Context 接口、Done、Err、Deadline、Value、WithCancel、WithTimeout、WithDeadline、CancelFunc、Cause、AfterFunc 和 API 约定",
    mechanism: "context 形成父子树，父 context 取消会关闭所有子 context 的 Done；WithCancel/WithDeadline/WithTimeout 创建派生 context 和 CancelFunc；调用 cancel 会释放子树资源和 timer；Err/Cause 描述取消原因。",
    scenarios: ["HTTP 请求", "数据库调用", "RPC 调用", "后台任务取消", "流水线停止"],
    pitfalls: "忘记 cancel、把 context 存入 struct、传 nil context、存放业务大对象、超时层级冲突、Value key 冲突和 goroutine 泄漏",
    sourceRefs: ["go-context-package", "go-concurrency-pipelines", "go-diagnostics", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：context 是 Go 在 API 边界传递取消、超时、deadline 和请求范围值的标准类型。它让一次请求相关的 goroutine、数据库调用、RPC 调用和下游操作能在同一生命周期内停止。",
      "工作机制：Context 暴露 Deadline、Done、Err、Value。WithCancel 创建可取消子 context；WithTimeout 和 WithDeadline 在时间到达后自动取消；父 context 取消会取消所有派生子 context；CancelFunc 会释放父子引用和 timer 资源。",
      "适用场景：HTTP handler 从 request.Context 开始传递；数据库和 RPC 调用接收 ctx 控制超时；pipeline 每个阶段监听 ctx.Done；后台任务在 shutdown 时接收根 context 取消；request ID、trace ID、认证主体等跨 API 范围值可放入 Value。",
      "特殊情况：CancelFunc 应在所有控制路径调用，遗漏会泄漏子 context、timer 和引用，go vet 会检查部分路径；WithCancelCause、WithTimeoutCause 和 Cause 可记录取消原因；AfterFunc 可在 context 取消时触发回调；WithoutCancel 可断开取消链；Context 可被多个 goroutine 同时使用。",
      "常见误区：把 context 存进 struct 会掩盖调用边界；用 Value 传可选参数或大业务对象会降低可读性并延长生命周期；传 nil context 会让 API 约定失效；多层 WithTimeout 互相覆盖会让真正超时来源难以定位；启动 goroutine 后不监听 Done 会继续泄漏。",
      "实战建议：ctx 作为函数第一个参数，命名为 ctx；派生 context 后 `defer cancel()`；Value 只放请求范围且跨进程/跨 API 的小值；每个阻塞点 select ctx.Done；日志中记录 ctx.Err 和 context.Cause；服务入口统一构建 shutdown 根 context。",
      "参考来源：本讲解参考 context 包文档、Go Blog Pipelines and cancellation、Go Diagnostics、Effective Go 和 Go Documentation，右侧来源支撑 Context 接口、CancelFunc 资源释放、API 约定、取消原因和流水线取消实践。",
    ],
    typicalProblems: [
      "context 解决什么并发生命周期问题",
      "WithCancel、WithTimeout 和 WithDeadline 的区别是什么",
      "为什么派生 context 后必须调用 cancel",
      "Context.Value 适合存什么类型的数据",
      "ctx 应该如何在函数签名中传递",
      "goroutine 如何监听 context 取消避免泄漏",
    ],
    commonIssues: ["忘记 cancel", "context 存入 struct", "Value 滥用", "传 nil context", "超时层级混乱", "goroutine 不监听 Done"],
    practiceAdvice: ["ctx 作为第一个参数", "派生后 defer cancel", "阻塞点监听 Done", "Value 只放请求范围小值", "记录 Err 和 Cause 辅助排查"],
    prerequisites: ["goroutine", "select"],
    related: ["http-server", "database-sql", "pipeline"],
    order: 35,
  }),
  /* <!-- KG_REVIEWED: sync.Mutex | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "mutex",
    zh: "sync.Mutex",
    en: "sync.Mutex",
    area: "concurrency",
    difficulty: "medium",
    summary: "sync.Mutex 用互斥锁保护共享状态，让同一时刻只有一个 goroutine 进入临界区。",
    concept: "保护共享状态的互斥访问，并建立 goroutine 之间的内存可见性顺序。",
    focus: "零值可用、Lock、Unlock、TryLock、临界区、defer 解锁、happens-before、锁复制、死锁和锁粒度",
    mechanism: "Mutex 的零值是未锁状态；Lock 获取锁，若锁已被持有则阻塞；Unlock 释放锁；一次 Unlock 在内存模型中 synchronizes before 后续某次 Lock 返回，因此临界区内写入对后续持锁读可见。",
    scenarios: ["内存缓存", "计数器保护", "连接状态", "对象内部状态", "复合不变量维护"],
    pitfalls: "复制锁、锁粒度过大、忘记解锁、重复解锁、死锁顺序、持锁执行 I/O、TryLock 滥用和 defer 解锁扩大临界区",
    sourceRefs: ["go-sync-mutex", "go-memory-model", "go-diagnostics", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：sync.Mutex 是 Go 标准库的互斥锁，用来保护共享内存。它让多个 goroutine 访问同一份可变状态时，按明确顺序进入临界区。",
      "工作机制：Mutex 零值可直接使用。Lock 成功后当前 goroutine 进入临界区，其他 Lock 调用等待；Unlock 释放锁。根据 Go 内存模型，某次 Unlock 会先于后续某次 Lock 返回，因此前一个临界区写入能被后一个临界区读取到。",
      "适用场景：保护 map、计数器、连接状态、缓存条目和一组需要一起更新的不变量。channel 更适合传递所有权和事件，Mutex 更适合保护已有共享状态。",
      "特殊情况：Mutex 使用后不能复制，包含 Mutex 的 struct 也要避免值复制；Unlock 未加锁的 Mutex 会触发运行时错误；TryLock 成功等价于 Lock，失败没有同步效果；Mutex 不绑定 goroutine，一个 goroutine 可以 Lock，另一个 goroutine 可以 Unlock，但这种设计需要非常清楚的所有权说明。",
      "常见误区：把锁包住整个函数会扩大阻塞范围；持锁期间做网络、磁盘或调用外部回调会放大死锁和延迟风险；多个锁获取顺序不统一会死锁；读写普通变量只靠 bool 标志忙等没有同步保证；用 defer Unlock 在很长函数里会让锁持有过久。",
      "实战建议：临界区只包含读取和更新共享状态的最小代码；用固定顺序获取多个锁；包含 Mutex 的类型使用指针接收者；锁保护的字段和锁放在同一个结构体；defer 解锁适合短函数；用 `go test -race`、mutex profile、block profile 和 trace 排查竞争与锁等待。",
      "参考来源：本讲解参考 sync.Mutex 文档、Go Memory Model、Go Diagnostics、Effective Go 和 Go Documentation，右侧来源支撑零值、Lock/Unlock、happens-before、复制限制、TryLock 和诊断建议。",
    ],
    typicalProblems: [
      "sync.Mutex 的零值为什么可以直接使用",
      "Unlock 和后续 Lock 之间有什么内存可见性保证",
      "为什么使用后的 Mutex 不能复制",
      "defer Unlock 适合哪些函数结构",
      "TryLock 失败为什么不能建立同步关系",
      "如何排查死锁、锁等待和数据竞争",
    ],
    commonIssues: ["复制锁", "忘记解锁", "重复解锁", "锁粒度过大", "持锁 I/O", "多锁顺序不一致"],
    practiceAdvice: ["临界区保持最小", "多个锁固定获取顺序", "包含锁的类型用指针接收者", "短函数中用 defer Unlock", "用 race detector 和 mutex profile 排查问题"],
    commands: ["go test -race ./...", "go test -mutexprofile mutex.out ./...", "go tool pprof mutex.out", "go test -blockprofile block.out ./..."],
    prerequisites: ["goroutine"],
    related: ["defer", "race-detector", "atomic"],
    order: 36,
  }),
  /* <!-- KG_REVIEWED: sync.WaitGroup | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "waitgroup",
    zh: "sync.WaitGroup",
    en: "sync.WaitGroup",
    area: "concurrency",
    difficulty: "easy",
    summary: "sync.WaitGroup 用计数器等待一组任务完成，适合汇合同一批 goroutine 的结束信号。",
    concept: "等待一组 goroutine 或任务全部完成，用于并发任务汇聚和测试同步。",
    focus: "Add、Done、Wait、Go、计数器、Add 时机、复用、panic、错误收集、errgroup 和 memory synchronization",
    mechanism: "WaitGroup 内部维护任务计数；Add 增减计数，Done 等价于 Add(-1)，Wait 阻塞到计数归零；WaitGroup.Go 会启动 goroutine 并在函数返回时移除任务；Done 会 synchronizes before 被它释放的 Wait 返回。",
    scenarios: ["并发任务汇聚", "批量请求", "并行处理", "测试同步", "后台任务收尾"],
    pitfalls: "Add 时机错误、重复 Done、计数为负 panic、任务 panic、错误收集缺失、复制 WaitGroup、复用批次交叉和无界并发",
    sourceRefs: ["go-sync-waitgroup", "go-errgroup", "go-memory-model", "go-concurrency-pipelines", "go-docs"],
    explanation: [
      "概念定义：sync.WaitGroup 是等待一组任务完成的同步工具。它关心任务结束信号，常用于主 goroutine 等待多个工作 goroutine 收尾。",
      "工作机制：传统写法是先 Add，再启动 goroutine，并在 goroutine 中 defer Done，最后 Wait。当前标准库还提供 WaitGroup.Go，把启动 goroutine、增加任务和完成任务收敛到一个调用里。计数归零时，所有 Wait 返回。",
      "适用场景：并发抓取多个 URL；并行处理一批文件；测试中等待后台 goroutine 完成；服务关闭时等待后台 worker 停止；pipeline 中等待所有发送者结束后关闭结果 channel。",
      "特殊情况：WaitGroup 使用后不能复制；计数变成负数会 panic；当计数为 0 时，正数 Add 必须发生在 Wait 之前；复用 WaitGroup 时，下一批 Add 要等上一批 Wait 返回后再开始；WaitGroup.Go 要求函数不要 panic；WaitGroup 本身不收集错误，也不控制取消。",
      "常见误区：在 goroutine 内部 Add 会与 Wait 形成竞态；漏掉 Done 会永久等待；重复 Done 会让计数为负；用 WaitGroup 跑大量任务时没有并发上限；需要错误传播和取消时仍然手写共享 error 会产生竞争。",
      "实战建议：优先使用 WaitGroup.Go；旧代码保持 `Add(1)` 在 `go` 语句之前，函数内 `defer Done()`；需要错误返回、取消和并发上限时用 errgroup；结果 channel 由等待所有发送者结束的一方关闭；配合 context 控制任务退出。",
      "参考来源：本讲解参考 sync.WaitGroup、errgroup、Go Memory Model、Go Blog Pipelines and cancellation 和 Go Documentation，右侧来源支撑计数语义、Go 方法、Add/Done/Wait 边界、错误传播和流水线收尾。",
    ],
    typicalProblems: [
      "WaitGroup 的 Add、Done、Wait 分别做什么",
      "为什么 Add 通常要写在 go 语句之前",
      "WaitGroup.Go 和 Add/Done 写法有什么差异",
      "WaitGroup 计数为负会发生什么",
      "WaitGroup 为什么不适合直接处理错误传播",
      "什么时候应该改用 errgroup",
    ],
    commonIssues: ["Add 时机错误", "漏掉 Done", "重复 Done", "复制 WaitGroup", "复用批次交叉", "错误收集数据竞争"],
    practiceAdvice: ["优先使用 WaitGroup.Go", "Add 写在 go 前面", "goroutine 内 defer Done", "错误传播用 errgroup", "结果 channel 由等待方统一关闭"],
    prerequisites: ["goroutine"],
    related: ["pipeline", "context", "errgroup"],
    order: 37,
  }),
  /* <!-- KG_REVIEWED: sync.Once | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "sync-once",
    zh: "sync.Once",
    en: "sync.Once",
    area: "concurrency",
    difficulty: "medium",
    summary: "sync.Once 保证某段初始化逻辑在并发调用下只执行一次，后续调用等待或直接返回。",
    concept: "保证初始化逻辑只执行一次，用于懒加载、单例准备和一次性注册。",
    focus: "Do、OnceFunc、OnceValue、OnceValues、并发初始化、panic 语义、错误缓存、deadlock、测试重置和全局状态",
    mechanism: "Once.Do 在同一个 Once 实例上只调用第一次传入的函数；其他并发调用会等待该函数返回；函数返回 synchronizes before 任意 Do 返回；Once 使用后不能复制；新的 Once 实例才能再次执行一次。",
    scenarios: ["单例初始化", "配置加载", "连接池准备", "指标注册", "昂贵计算缓存"],
    pitfalls: "初始化错误缓存、panic 后状态、Do 内递归调用死锁、测试复用、全局状态污染、OnceFunc panic 复现和参数捕获混乱",
    sourceRefs: ["go-sync-once", "go-sync-oncefunc", "go-memory-model", "go-effective-go", "go-docs"],
    explanation: [
      "概念定义：sync.Once 是一次性并发初始化工具。多个 goroutine 同时请求同一份资源时，它保证初始化函数只执行一次，其他调用等待结果完成。",
      "工作机制：`once.Do(f)` 只会执行第一次传入的 f，后续 Do 调用即使传入不同函数也直接返回。f 返回之前，其他 Do 会阻塞；f 返回后，后续 Do 能观察到初始化效果。OnceFunc、OnceValue、OnceValues 把 Once 封装成可并发调用的函数，并缓存返回值。",
      "适用场景：懒加载配置、注册全局指标、初始化连接池、构建只读缓存、加载模板、执行昂贵计算并缓存结果。OnceValue 适合只计算一个结果，OnceValues 适合返回值加 error 的场景。",
      "特殊情况：Once.Do 的 f 如果 panic，Once 仍视为已经执行过，后续 Do 不再调用 f；OnceFunc/OnceValue 在 f panic 后，后续调用会用同一个 panic 值继续 panic；f 内部再次调用同一个 Once.Do 会死锁；Once 需要重新执行时只能创建新 Once。",
      "常见误区：把可能失败的初始化直接放入 Once.Do 后，第一次失败会被永久缓存成失败状态或半初始化状态；在测试中复用全局 Once 会让用例相互影响；在 Do 内捕获动态参数会让第一次调用的参数成为唯一生效参数；用 Once 承载可刷新配置会限制后续更新。",
      "实战建议：初始化函数保持短小、确定；需要处理错误时用 OnceValues 缓存 `(T, error)`，并明确是否允许失败后重试；测试中提供重置或注入新实例；避免 Do 内递归调用同一个 Once；可刷新数据用 atomic.Value、mutex 或显式 reload 机制。",
      "参考来源：本讲解参考 sync.Once、OnceFunc/OnceValue、Go Memory Model、Effective Go 和 Go Documentation，右侧来源支撑一次执行语义、并发等待、panic 行为、同步关系和工程使用建议。",
    ],
    typicalProblems: [
      "sync.Once 如何保证并发下只执行一次",
      "Do 传入不同函数时会执行哪一个",
      "Once.Do 中函数 panic 后后续调用会怎样",
      "OnceFunc 和 OnceValue 与 Once.Do 有什么差异",
      "带错误的初始化应该如何设计",
      "测试中如何避免全局 Once 污染",
    ],
    commonIssues: ["失败初始化被缓存", "Do 内递归死锁", "测试全局状态污染", "参数捕获只生效第一次", "Once 复制", "可刷新配置误用 Once"],
    practiceAdvice: ["初始化逻辑保持确定", "失败策略提前设计", "测试中注入新 Once", "避免 Do 内递归调用", "需要刷新时选择显式 reload 机制"],
    prerequisites: ["goroutine", "mutex"],
    related: ["init-function", "sync-map"],
    order: 38,
  }),
  /* <!-- KG_REVIEWED: sync.Map | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "sync-map",
    zh: "sync.Map",
    en: "sync.Map",
    area: "concurrency",
    difficulty: "medium",
    summary: "sync.Map 是并发安全的专用 map，适合只增缓存或多 goroutine 操作互不相交 key 的场景。",
    concept: "提供并发安全的特定场景 map，减少部分读多写少或 key 分片场景的锁竞争。",
    focus: "Load、Store、LoadOrStore、LoadAndDelete、CompareAndSwap、Clear、Range、read/write operation、类型安全和一致性边界",
    mechanism: "sync.Map 内部完成并发同步，Load/Store/Delete 等操作可被多个 goroutine 同时调用；写操作会 synchronizes before 观察到该写入效果的读操作；Range 顺序和快照一致性没有强保证。",
    scenarios: ["只增缓存", "插件注册表", "连接映射", "跨 goroutine 状态表", "互不相交 key 的并发写入"],
    pitfalls: "类型断言分散、写多场景退化、Range 一致性误解、复杂不变量难维护、普通 map 替换成本和 key/value any 带来的错误延迟",
    sourceRefs: ["go-sync-map", "go-sync-mutex", "go-memory-model", "go-docs"],
    explanation: [
      "概念定义：sync.Map 是标准库提供的并发安全 map。它服务于特定高并发访问模式，尤其是一次写入多次读取的缓存，或多个 goroutine 操作互不相交 key 的场景。",
      "工作机制：sync.Map 的零值可直接使用，Load 读取，Store 写入，LoadOrStore 做并发下的存在即读和缺失即写，LoadAndDelete 原子取出并删除，CompareAndSwap/CompareAndDelete 做条件更新，Clear 清空全部条目。它在内存模型上保证被读到的写入先于该读操作。",
      "适用场景：只增长缓存、插件注册表、连接表、按连接 ID 分散更新的状态表，以及读远多于写且不需要维护复杂跨 key 不变量的场景。",
      "特殊情况：Range 不是一致性快照，每个 key 最多访问一次，但并发 Store/Delete 会让 Range 观察到任意时刻的映射；Range 即使回调很快返回 false，也可能是 O(N)；CompareAndSwap 的旧值必须可比较；Map 使用后不能复制。",
      "常见误区：把 sync.Map 当作普通 map+锁的全面替代会丢失类型安全和不变量控制；在写多、需要批量一致性、需要按多个 key 同时更新的场景下，它通常让维护更困难；Range 中做强一致统计会得到不稳定结果；Load 后直接类型断言分散在各处会增加 panic 风险。",
      "实战建议：多数业务代码优先选择 `map[K]V + sync.RWMutex`，类型安全和不变量更清楚；选择 sync.Map 前确认访问模式符合只增读多或 key 集合分散；把类型断言封装在小结构体方法里；Range 只用于近似扫描、清理或调试；复杂一致性需求回到普通 map 加锁。",
      "参考来源：本讲解参考 sync.Map、sync.Mutex、Go Memory Model 和 Go Documentation，右侧来源支撑专用场景、并发安全、方法语义、Range 边界和同步关系。",
    ],
    typicalProblems: [
      "sync.Map 适合哪些访问模式",
      "为什么多数代码仍推荐 map 加锁",
      "LoadOrStore 和 Store 的差异是什么",
      "Range 为什么不是一致性快照",
      "CompareAndSwap 对 old 值有什么要求",
      "sync.Map 如何封装才能减少类型断言分散",
    ],
    commonIssues: ["类型断言分散", "Range 一致性误判", "写多场景性能不佳", "复杂不变量难维护", "CompareAndSwap 旧值不可比较", "使用后复制 Map"],
    practiceAdvice: ["优先评估 map+锁", "封装类型断言", "Range 只做近似扫描", "确认读多写少或 key 分散模式", "复杂一致性用显式锁"],
    prerequisites: ["map", "mutex"],
    related: ["atomic", "race-detector"],
    order: 39,
  }),
  /* <!-- KG_REVIEWED: atomic | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "atomic",
    zh: "atomic",
    en: "Atomic",
    area: "concurrency",
    difficulty: "hard",
    summary: "atomic 提供低层原子内存操作，适合单变量计数、状态位和少量性能敏感同步路径。",
    concept: "用原子操作处理低层共享数值或指针状态，并获得顺序一致的内存同步语义。",
    focus: "Load、Store、Swap、CompareAndSwap、Add、typed atomic、atomic.Value、sequential consistency、happens-before、单变量不变量和可读性",
    mechanism: "sync/atomic 的操作是原子读写/修改，并在 Go 内存模型中提供同步关系；所有原子操作表现得像按某个顺序一致的全局顺序执行；类型化原子如 atomic.Int64、atomic.Bool、atomic.Pointer[T] 提供更安全的 API。",
    scenarios: ["高频计数", "状态标记", "性能敏感路径", "只读配置热更新", "底层库同步"],
    pitfalls: "多变量不变式、可读性下降、内存模型理解、过早优化、atomic.Value 类型一致性、混用普通读写和原子读写以及 32 位平台对齐问题",
    sourceRefs: ["go-sync-atomic", "go-memory-model", "go-sync-mutex", "go-docs"],
    explanation: [
      "概念定义：atomic 是 Go 的低层同步工具，用于对单个数值、指针或值容器执行不可分割的并发操作。它适合简单计数器、开关状态、只读配置发布和底层库实现。",
      "工作机制：Load 读取，Store 写入，Swap 交换，CompareAndSwap 在旧值匹配时更新，Add 做原子加法。若一个原子操作的效果被另一个原子操作观察到，前者在内存模型上先于后者；所有原子操作表现为顺序一致。",
      "适用场景：请求计数、关闭标志、限流状态、读多写少配置热更新、无锁结构中的单节点状态。atomic.Value 适合发布整体不可变配置，atomic.Pointer[T] 适合类型安全指针更新。",
      "特殊情况：atomic 只自然表达单变量或单指针不变量，多字段一致性仍需要 mutex；atomic.Value 首次 Store 后，后续 Store 必须使用相同具体类型；普通读写和原子读写混用同一变量会形成数据竞争；旧式函数 API 对某些 64 位值在 32 位平台有对齐要求，类型化原子更稳妥。",
      "常见误区：为了性能用 atomic 替代所有锁，会让代码可读性和正确性下降；用多个 atomic 变量组合业务状态容易读到中间状态；CAS 循环缺少退避会浪费 CPU；只把写改成 atomic，读仍然普通读取会保留竞争；把 atomic.Value 当成可变 map 容器会破坏发布语义。",
      "实战建议：先用 mutex 写出正确实现，再用基准测试证明 atomic 有收益；优先使用类型化原子；一个 atomic 变量只表达一个清晰状态；发布配置时 Store 不可变副本；多变量不变量用锁；用 race detector 和压测验证并发路径。",
      "参考来源：本讲解参考 sync/atomic、Go Memory Model、sync.Mutex 和 Go Documentation，右侧来源支撑原子操作语义、顺序一致、typed atomic、atomic.Value 和低层使用边界。",
    ],
    typicalProblems: [
      "atomic.Load/Store/Swap/CAS 分别适合什么场景",
      "Go 的原子操作有什么内存顺序保证",
      "为什么多变量不变量通常需要 mutex",
      "atomic.Value 使用时有什么类型限制",
      "类型化原子相比旧函数 API 有什么优势",
      "如何判断 atomic 优化是否真的有收益",
    ],
    commonIssues: ["普通读写和原子读写混用", "多个 atomic 组成不一致状态", "CAS 循环忙等", "atomic.Value 类型不一致", "过早优化", "代码可读性下降"],
    practiceAdvice: ["先用锁保证正确性", "优先使用类型化原子", "单个原子变量表达单个状态", "配置热更新发布不可变副本", "用基准测试证明收益"],
    commands: ["go test -race ./...", "go test -bench=. ./..."],
    prerequisites: ["mutex", "memory-model"],
    related: ["race-detector", "scheduler"],
    order: 40,
  }),
  /* <!-- KG_REVIEWED: race detector | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "race-detector",
    zh: "race detector",
    en: "Race Detector",
    area: "tooling",
    difficulty: "medium",
    summary: "race detector 通过 `-race` 插桩在运行时发现数据竞争，并给出冲突访问和 goroutine 创建栈。",
    concept: "在测试、运行或构建程序时发现数据竞争，帮助定位并发共享变量读写问题。",
    focus: "-race、插桩、冲突访问栈、goroutine 创建栈、测试覆盖、GORACE、运行开销、race build tag 和平台要求",
    mechanism: "race detector 在构建时插入监测代码，运行过程中记录内存访问和同步事件；当两个 goroutine 并发访问同一变量且至少一个是写，并且缺少同步关系时，输出 data race 报告。",
    scenarios: ["并发测试", "CI 检查", "压测前验证", "线上问题复现", "重构并发代码后回归"],
    pitfalls: "覆盖不足、运行开销、竞争报告解读、测试数据规模、cgo/平台要求、长生命周期 defer/recover 开销和 race tag 分支差异",
    sourceRefs: ["go-race-detector", "go-memory-model", "go-sync-atomic", "go-docs"],
    explanation: [
      "概念定义：race detector 是 Go 内置的数据竞争检测工具。它帮助发现多个 goroutine 对同一内存地址并发访问，且至少一个访问是写入，同时没有锁、channel、atomic 等同步关系的情况。",
      "工作机制：加上 `-race` 后，go 命令构建带插桩的程序。程序运行时记录读写、同步事件和 goroutine 创建位置。一旦发现竞争，会打印当前访问栈、之前冲突访问栈，以及相关 goroutine 的创建栈。",
      "适用场景：CI 中跑并发单元测试；重构锁和 channel 后回归；压测前用 `go test -race` 或 `go run -race` 验证；难以复现的并发问题可构建 `-race` 二进制在接近真实负载下运行。",
      "特殊情况：race detector 只能发现实际执行到的竞争路径，覆盖不足时需要用更真实的工作负载补充；`GORACE` 可控制报告路径、退出码、路径裁剪、是否首个错误即停等；启用 `-race` 会定义 race build tag；当前工具有平台和 C 编译器要求，开销通常明显增加。",
      "常见误区：跑一次 `go test -race` 通过就认为没有竞争，会忽略覆盖不足；只看第一段栈会漏掉 goroutine 创建位置；为了让测试过关用 sleep 调度会掩盖问题；把报告中的读写位置当唯一原因，实际需要回到共享状态所有权和同步设计上修复。",
      "实战建议：CI 至少对关键包运行 `go test -race ./...`；对服务用 `go build -race` 构建后压测核心路径；报告解读先看冲突读写，再看 goroutine 创建栈；修复优先选择明确所有权、mutex、channel 或 atomic；大仓库用 GORACE 的 log_path 和 strip_path_prefix 收敛报告。",
      "参考来源：本讲解参考 Data Race Detector、Go Memory Model、sync/atomic 和 Go Documentation，右侧来源支撑数据竞争定义、-race 用法、报告格式、GORACE、覆盖限制和运行开销。",
    ],
    typicalProblems: [
      "race detector 能发现什么类型的问题",
      "为什么 -race 只能发现实际执行到的竞争",
      "race 报告里的两个访问栈和创建栈如何阅读",
      "GORACE 常用配置有哪些",
      "race build tag 有什么用途",
      "发现数据竞争后如何选择 mutex、channel 或 atomic 修复",
    ],
    commonIssues: ["测试覆盖不足", "报告栈解读错误", "运行开销过高", "平台或 cgo 要求不满足", "用 sleep 掩盖竞争", "只修局部未修所有权"],
    practiceAdvice: ["CI 跑 go test -race", "关键服务构建 -race 压测", "先读冲突访问栈再读创建栈", "用 GORACE 收敛报告", "按共享状态所有权修复竞争"],
    commands: ["go test -race ./...", "go run -race .", "go build -race ./cmd/app", "GORACE=\"log_path=/tmp/race strip_path_prefix=$(pwd)\" go test -race ./..."],
    prerequisites: ["goroutine", "mutex"],
    related: ["atomic", "go-test", "memory-model"],
    order: 41,
  }),
  /* <!-- KG_REVIEWED: pipeline | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "pipeline",
    zh: "pipeline",
    en: "Pipeline",
    area: "concurrency",
    difficulty: "medium",
    summary: "pipeline 用多个 channel 连接的阶段组织流式并发处理，核心难点是关闭、取消、错误传播和背压。",
    concept: "用多个阶段和 channel 组织流式并发处理，让数据在 goroutine 阶段之间有序流动。",
    focus: "stage、source、sink、fan-out、fan-in、close outbound、drain inbound、done/context、backpressure、errgroup 和 goroutine leak",
    mechanism: "每个阶段通常从输入 channel 接收值，处理后发送到输出 channel；阶段在完成所有发送后关闭自己的输出；下游持续接收直到输入关闭，或通过 done/context 通知上游停止发送。",
    scenarios: ["日志处理", "批量转码", "数据清洗", "并发爬取", "文件摘要计算"],
    pitfalls: "下游提前退出、发送阻塞、关闭顺序、错误传播、缓冲容量脆弱、fan-in 关闭时机和 goroutine 泄漏",
    sourceRefs: ["go-concurrency-pipelines", "go-context-package", "go-errgroup", "go-diagnostics", "go-docs"],
    explanation: [
      "概念定义：pipeline 是一种并发组织方式：把处理过程拆成多个阶段，阶段之间用 channel 传递数据。每个阶段可以有一个或多个 goroutine，前端是 source，末端是 sink。",
      "工作机制：一个阶段从 inbound channel 读取数据，处理后写入 outbound channel。发送全部结束后，阶段关闭 outbound channel；接收方用 range 消费直到 channel 关闭。fan-out 让多个 worker 从同一输入分担工作，fan-in 把多个输出合并到一个 channel，合并点常用 WaitGroup 控制最终关闭。",
      "适用场景：日志清洗、批量图片转码、爬虫抓取、文件摘要计算、ETL 数据转换和流式压缩。只要任务能拆成多个顺序阶段，并且每个阶段可独立并发，就适合用 pipeline 表达。",
      "特殊情况：下游提前返回时，上游发送可能永久阻塞，形成 goroutine 泄漏；缓冲 channel 只能在元素数量和消费数量都明确时简化问题，通用方案是 done channel 或 context 取消；错误传播需要单独通道、errgroup 或带 error 的结果结构；关闭 channel 的责任应属于发送完成的一方。",
      "常见误区：只关闭输出 channel，却没有通知上游停止，会让上游卡在发送；多个发送者直接 close 同一个 channel 会 panic；fan-in 时在所有发送 goroutine 完成前 close 输出会导致发送到已关闭通道；忽略背压会让某个慢阶段拖住整个链路。",
      "实战建议：每个阶段遵守两条规则：发送完成后关闭自己的输出；接收输入直到关闭或取消。长期 pipeline 使用 context 作为取消信号；fan-in 用 WaitGroup 或 errgroup 统一关闭输出；错误和取消绑定设计；用 goroutine profile 和 block profile 排查卡住的阶段。",
      "参考来源：本讲解参考 Go Blog Pipelines and cancellation、context 包、errgroup、Go Diagnostics 和 Go Documentation，右侧来源支撑 pipeline 定义、fan-out/fan-in、关闭规则、取消模式、错误传播和泄漏排查。",
    ],
    typicalProblems: [
      "Go pipeline 中一个 stage 应该遵守哪些关闭规则",
      "fan-out 和 fan-in 分别解决什么问题",
      "下游提前退出为什么会导致 goroutine 泄漏",
      "done channel 和 context 在 pipeline 中如何使用",
      "fan-in 什么时候可以 close 输出 channel",
      "pipeline 错误传播应该用结果结构、错误通道还是 errgroup",
    ],
    commonIssues: ["下游提前退出", "上游发送阻塞", "多发送者 close panic", "fan-in 关闭过早", "错误传播丢失", "缓冲容量依赖脆弱"],
    practiceAdvice: ["阶段完成发送后关闭输出", "下游退出时取消上游", "fan-in 用 WaitGroup 统一关闭", "错误传播与取消一起设计", "用 profile 定位阻塞阶段"],
    prerequisites: ["channel", "select", "waitgroup"],
    related: ["context", "errgroup", "timer-ticker"],
    order: 42,
  }),
  /* <!-- KG_REVIEWED: timer 与 ticker | 2026-05-29 | source_count=4 --> */
  goPoint({
    id: "timer-ticker",
    zh: "timer 与 ticker",
    en: "Timer and Ticker",
    area: "concurrency",
    difficulty: "medium",
    summary: "timer 触发一次时间事件，ticker 周期性触发时间事件，二者常与 select 组合实现超时、心跳和周期任务。",
    concept: "用时间事件驱动超时、延迟执行、周期任务、心跳检测和重试退避。",
    focus: "time.Timer、time.Ticker、After、AfterFunc、Tick、NewTimer、NewTicker、Stop、Reset、monotonic clock、Go 1.23 GC 和阻塞消费",
    mechanism: "Timer 在指定 duration 后向 C channel 发送一次当前时间，Ticker 按固定间隔向 C channel 发送时间；Stop 停止后续事件，Reset 改变触发时间；time.After 返回 Timer 的 channel，time.AfterFunc 到期后在独立 goroutine 中运行函数。",
    scenarios: ["请求超时", "周期刷新", "心跳检测", "重试退避", "空闲连接清理"],
    pitfalls: "Ticker 停止遗漏、Reset 使用不当、time.After 高频创建、时间漂移、阻塞消费、AfterFunc 并发回调和单调时钟/墙钟混淆",
    sourceRefs: ["go-time-package", "go-context-package", "go-concurrency-pipelines", "go-docs"],
    explanation: [
      "概念定义：Timer 和 Ticker 是 time 包里的时间事件工具。Timer 只触发一次，适合超时和延迟执行；Ticker 按周期触发，适合心跳、轮询和定时刷新。",
      "工作机制：NewTimer 返回带 C channel 的 Timer，到期后发送一个 Time；NewTicker 返回 Ticker，按间隔重复发送 Time；select 可以同时等待业务 channel、ctx.Done 和 timer/ticker channel；AfterFunc 到期后调用函数并返回可 Stop/Reset 的 Timer。",
      "适用场景：请求处理用 context.WithTimeout 或 Timer 做超时；后台任务用 Ticker 周期刷新；连接管理用 Timer 做空闲超时；重试逻辑用 Timer 配合指数退避；pipeline 用 select 超时和取消兜底。",
      "特殊情况：Go 1.23 起，未引用且未 Stop 的 Timer/Ticker 可被 GC 回收，老版本中 time.After 或 time.Tick 的泄漏风险更大；Ticker 会根据接收方速度调整或丢弃 tick；time 包测量间隔优先使用 monotonic clock，墙钟调整对 Since/Sub 等测量影响较小；Stop 后 channel 通常不会被关闭。",
      "常见误区：在高频循环里反复 time.After 会制造大量 timer；Ticker 消费慢时以为每个周期事件都能排队保存；Stop 后 range ticker.C 会一直等待；AfterFunc 回调可能并发运行，修改共享状态需要同步；Reset 已触发或已停止的 timer 时需要遵守当前版本文档语义。",
      "实战建议：请求超时优先用 context.WithTimeout；高频复用场景用 NewTimer 并 Reset；长期 Ticker 用 `defer ticker.Stop()`；select 循环加入 ctx.Done；慢消费场景记录实际执行时间并处理跳 tick；AfterFunc 回调里保护共享状态。",
      "参考来源：本讲解参考 time 包、context 包、Go Blog Pipelines and cancellation 和 Go Documentation，右侧来源支撑 Timer/Ticker API、Stop/Reset、Go 1.23 GC 变化、单调时钟和 select 使用模式。",
    ],
    typicalProblems: [
      "Timer 和 Ticker 的核心区别是什么",
      "time.After、NewTimer 和 AfterFunc 分别适合什么场景",
      "Go 1.23 对 Timer/Ticker GC 行为有什么变化",
      "Ticker 消费慢时会发生什么",
      "Stop 后 channel 为什么不能用 range 等待关闭",
      "高频超时逻辑如何避免创建大量 timer",
    ],
    commonIssues: ["高频 time.After 创建过多", "Ticker 忘记 Stop", "Reset 语义误用", "慢消费丢 tick", "AfterFunc 回调并发修改状态", "Stop 后等待 channel 关闭"],
    practiceAdvice: ["请求超时优先用 context", "长期 ticker defer Stop", "高频场景复用 Timer", "select 中保留取消分支", "记录实际执行时间处理漂移"],
    prerequisites: ["select"],
    related: ["context", "pipeline"],
    order: 43,
  }),
  /* <!-- KG_REVIEWED: errgroup | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "errgroup",
    zh: "errgroup",
    en: "errgroup",
    area: "concurrency",
    difficulty: "medium",
    summary: "errgroup 把一组 goroutine 的等待、错误返回、取消传播和并发上限组合成一个任务组。",
    concept: "把并发任务、错误返回和上下文取消组合起来，简化子任务编排。",
    focus: "Group、WithContext、Go、Wait、first non-nil error、SetLimit、TryGo、取消传播、pipeline 和共享变量安全",
    mechanism: "errgroup.Group 的 Go 启动返回 error 的函数，Wait 等待所有函数返回并返回首个非 nil 错误；WithContext 创建关联 context，首个错误或 Wait 返回会取消派生 context；SetLimit 控制活跃 goroutine 数量，TryGo 在容量不足时直接返回 false。",
    scenarios: ["并发 RPC", "批量处理", "多阶段服务调用", "任务编排", "带错误的 pipeline"],
    pitfalls: "错误覆盖、取消传递缺失、并发数限制误用、共享变量竞争、循环变量捕获、Wait 调用时机和 Group 复用",
    sourceRefs: ["go-errgroup", "go-context-package", "go-sync-waitgroup", "go-concurrency-pipelines", "go-docs"],
    explanation: [
      "概念定义：errgroup 是 x/sync 提供的并发任务组工具。它像 WaitGroup 一样等待一组 goroutine，同时把错误返回和 context 取消整合进同一个模型。",
      "工作机制：`g.Go(func() error { ... })` 启动任务；`g.Wait()` 阻塞直到所有任务结束，并返回第一个非 nil 错误。`errgroup.WithContext(ctx)` 返回 Group 和派生 ctx，任一任务返回非 nil 错误时会取消该 ctx，其他任务可通过 ctx.Done 及时退出。",
      "适用场景：并发调用多个 RPC；批量处理文件；pipeline 中任一阶段失败就取消其他阶段；服务启动时并行准备多个依赖；需要限制并发数的批处理用 SetLimit 或 TryGo。",
      "特殊情况：Group 的零值可用，但没有自动取消 context；WithContext 的派生 context 会在首个错误或 Wait 返回时取消；Wait 只返回首个非 nil 错误，后续错误需要任务内部单独收集；SetLimit 不能在活跃 goroutine 存在时修改；Group 表示同一批任务，不适合跨批复用。",
      "常见误区：任务函数里不监听 ctx.Done，会让取消传播失效；并发写同一个结果 slice/map 仍需保证每个 goroutine 写不同位置或加锁；循环变量捕获会让任务处理错对象；用 errgroup 之后仍手写共享 err 变量会产生竞争；设置并发上限为 0 会阻止新任务加入。",
      "实战建议：需要错误传播和取消时优先用 errgroup；每个任务都接收派生 ctx；结果写入按索引预分配或使用 channel 汇总；批处理设置合理 SetLimit；pipeline 中由 Wait 后统一关闭结果 channel；需要所有错误时用 errors.Join 或独立错误收集机制。",
      "参考来源：本讲解参考 errgroup 包、context 包、sync.WaitGroup、Go Blog Pipelines and cancellation 和 Go Documentation，右侧来源支撑 Group/WithContext/Go/Wait、首个错误、并发限制和 pipeline 示例。",
    ],
    typicalProblems: [
      "errgroup 和 sync.WaitGroup 的核心区别是什么",
      "WithContext 返回的 context 什么时候会被取消",
      "Wait 为什么只返回第一个非 nil 错误",
      "SetLimit 和 TryGo 分别适合什么场景",
      "errgroup 中如何安全收集多个结果",
      "pipeline 中如何用 errgroup 处理错误和取消",
    ],
    commonIssues: ["任务不监听 context", "共享结果数据竞争", "循环变量捕获", "Group 跨批复用", "SetLimit 活跃时修改", "只保留首个错误"],
    practiceAdvice: ["任务函数接收派生 ctx", "用 SetLimit 控制并发", "结果按索引或 channel 汇总", "Wait 后统一关闭结果 channel", "需要全部错误时单独收集"],
    prerequisites: ["goroutine", "context", "waitgroup"],
    related: ["pipeline", "error-handling"],
    order: 44,
  }),
  /* <!-- KG_REVIEWED: Go runtime | 2026-05-29 | source_count=5 --> */
  goPoint({
    id: "go-runtime",
    zh: "Go runtime",
    en: "Go Runtime",
    area: "runtime",
    difficulty: "medium",
    summary: "Go runtime 随程序一起链接，负责 goroutine 调度、内存分配、GC、栈管理、定时器、网络轮询和运行时诊断。",
    concept: "提供调度、GC、网络轮询、栈管理、定时器、反射类型信息和运行时诊断能力。",
    focus: "goroutine、scheduler、GOMAXPROCS、stack growth、GC、allocator、timer、netpoll、runtime/metrics、pprof、trace 和 GODEBUG",
    mechanism: "编译后的 Go 程序携带 runtime，运行时在进程内管理 goroutine、OS 线程、P 执行资源、内存分配、垃圾回收、栈增长与收缩、定时器、网络事件和部分低层类型信息。",
    scenarios: ["性能分析", "并发调度理解", "内存问题定位", "服务稳定性治理", "线上诊断"],
    pitfalls: "只看业务代码、忽略阻塞点、GC 指标误读、调度延迟、GOMAXPROCS 误配、GODEBUG 滥用和 cgo/系统调用影响",
    sourceRefs: ["go-runtime-package", "go-diagnostics", "go-memory-model", "go-std-lib", "go-docs"],
    explanation: [
      "概念定义：Go runtime 是每个 Go 程序自带的运行时系统。它让开发者可以用 goroutine、channel、GC 和标准库网络 I/O 写程序，同时由运行时承担调度、内存和低层系统协作。",
      "工作机制：运行时维护 goroutine 队列，把 goroutine 调度到 OS 线程上执行；分配器管理堆对象；GC 并发标记和清扫；栈按需增长；网络轮询把 socket readiness 转成 goroutine 唤醒；timer 驱动时间事件；runtime 包暴露 NumGoroutine、GOMAXPROCS、ReadMemStats、Stack、GoroutineProfile 等低层入口。",
      "适用场景：排查 CPU 高、内存增长、goroutine 泄漏、锁等待、阻塞、GC 抖动和调度延迟；服务容量评估时结合 pprof、trace、runtime/metrics 和日志观察运行时行为；容器场景关注 GOMAXPROCS 和内存限制。",
      "特殊情况：GOGC 控制 GC 目标比例，GOMEMLIMIT 设置运行时软内存限制，GODEBUG 可打开 gctrace、schedtrace、inittrace 等诊断输出；GOMAXPROCS 影响同时执行 Go 代码的 P 数量；cgo、长系统调用、锁线程和阻塞 syscall 会影响调度与诊断结果。",
      "常见误区：看到内存 RSS 高就直接判断泄漏，会忽略堆、栈、scavenger 和 OS 回收策略；只看 goroutine 数量会漏掉阻塞位置；把 GC pause 当成唯一性能瓶颈会忽略分配速率；随意调 GOGC/GOMEMLIMIT 会在吞吐、延迟和内存之间引入新权衡。",
      "实战建议：CPU 问题先看 CPU profile；内存问题看 heap profile、alloc_space/inuse_space 和 runtime metrics；goroutine 增长看 goroutine profile；锁等待看 mutex/block profile；调度问题看 trace 和 GODEBUG=schedtrace；调参前先建立基准和监控曲线。",
      "参考来源：本讲解参考 runtime 包、Go Diagnostics、Go Memory Model、Go Standard Library 和 Go Documentation，右侧来源支撑运行时职责、环境变量、GC/调度诊断、pprof/trace 和内存同步基础。",
    ],
    typicalProblems: [
      "Go runtime 主要负责哪些事情",
      "G、M、P 和 GOMAXPROCS 分别影响什么",
      "GC、堆、栈和 RSS 之间如何区分",
      "GODEBUG 的 gctrace、schedtrace、inittrace 适合看什么",
      "goroutine 泄漏、锁等待和调度延迟分别用什么工具排查",
      "GOGC 和 GOMEMLIMIT 调整会带来哪些权衡",
    ],
    commonIssues: ["GC 指标误读", "RSS 与堆混淆", "只看 goroutine 数量", "忽略阻塞 profile", "GOMAXPROCS 误配", "无基准调运行时参数"],
    practiceAdvice: ["先用 pprof 和 trace 建立证据", "内存问题区分 alloc/inuse/RSS", "goroutine 增长看栈和阻塞点", "调 GOGC/GOMEMLIMIT 前做压测", "用 GODEBUG 临时辅助诊断"],
    commands: ["GODEBUG=gctrace=1 ./app", "GODEBUG=schedtrace=1000 ./app", "go tool pprof", "go test -trace trace.out ./...", "go test -run=^$ -bench=. -benchmem"],
    prerequisites: ["go-overview"],
    related: ["scheduler", "gc", "pprof"],
    order: 45,
  }),
  /* <!-- KG_REVIEWED: 调度器 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "scheduler", zh: "调度器", en: "Scheduler", area: "runtime", difficulty: "hard", concept: "把 goroutine 映射到系统线程执行。", focus: "G、M、P、抢占、阻塞和 GOMAXPROCS", mechanism: "调度器维护 goroutine 队列，P 提供执行资源，M 表示系统线程；运行时在阻塞、系统调用、抢占和网络事件之间切换执行。", scenarios: ["高并发服务", "CPU 占用分析", "阻塞定位", "吞吐调优"], pitfalls: "GOMAXPROCS 理解、长时间 CPU 循环、系统调用阻塞和调度延迟指标", commands: ["GODEBUG=schedtrace=1000 go run ."], prerequisites: ["goroutine", "go-runtime"], related: ["gc", "pprof", "atomic"], order: 46 }),
  /* <!-- KG_REVIEWED: GC | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "gc", zh: "GC", en: "Garbage Collection", area: "runtime", difficulty: "hard", concept: "自动回收不可达对象并控制堆内存增长。", focus: "并发标记清扫、GOGC、堆大小、暂停时间和分配速率", mechanism: "Go GC 根据堆增长目标触发并发标记，识别仍可达对象，清理可回收内存，并用写屏障维护并发阶段正确性。", scenarios: ["内存泄漏排查", "延迟优化", "大对象分配治理", "容量规划"], pitfalls: "把 GC 暂停当作唯一指标、忽略分配速率、保留引用和池化过度", commands: ["GODEBUG=gctrace=1 go test ./...", "go test -bench=. -benchmem"], prerequisites: ["go-runtime"], related: ["escape-analysis", "pprof", "array-slice"], order: 47 }),
  /* <!-- KG_REVIEWED: 逃逸分析 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "escape-analysis", zh: "逃逸分析", en: "Escape Analysis", area: "runtime", difficulty: "hard", concept: "判断变量放在栈上还是堆上，影响分配和 GC 压力。", focus: "指针返回、接口装箱、闭包捕获和编译器报告", mechanism: "编译器分析变量生命周期和引用路径，发现变量可能在当前栈帧外继续存活时，把它分配到堆上。", scenarios: ["性能优化", "分配次数分析", "热点函数调优", "大对象传递"], pitfalls: "盲目追求零逃逸、接口转换影响、闭包捕获和可读性牺牲", commands: ["go build -gcflags=-m ./..."], prerequisites: ["pointer", "closure"], related: ["gc", "benchmark", "pprof"], order: 48 }),
  /* <!-- KG_REVIEWED: Go 内存模型 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "memory-model", zh: "Go 内存模型", en: "Go Memory Model", area: "runtime", difficulty: "hard", concept: "定义 goroutine 之间读写可见性和同步关系。", focus: "happens-before、channel、锁、atomic 和数据竞争", mechanism: "内存模型描述同步操作建立的顺序关系，程序通过锁、channel、atomic 等同步手段让一个 goroutine 的写入对另一个 goroutine 可见。", scenarios: ["并发库设计", "竞态排查", "底层优化", "代码审查"], pitfalls: "依赖调度时序、普通变量通信、双重检查和 atomic 混用", prerequisites: ["goroutine", "mutex", "channel"], related: ["atomic", "race-detector"], order: 49 }),
  /* <!-- KG_REVIEWED: go test | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "go-test", zh: "go test", en: "go test", area: "testing", difficulty: "easy", concept: "运行单元测试、示例、基准测试和覆盖率统计。", focus: "测试文件、testing 包、包级运行、缓存和覆盖率", mechanism: "go test 编译待测包和测试文件，执行 Test、Benchmark、Example 等入口，并根据参数输出覆盖率、竞态或性能结果。", scenarios: ["单元测试", "回归测试", "CI 验证", "覆盖率统计"], pitfalls: "测试缓存、并行测试隔离、临时目录清理和外部依赖波动", commands: ["go test ./...", "go test -cover ./..."], prerequisites: ["go-toolchain"], related: ["table-driven-test", "benchmark", "race-detector"], order: 50 }),
  /* <!-- KG_REVIEWED: 表驱动测试 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "table-driven-test", zh: "表驱动测试", en: "Table-driven Test", area: "testing", difficulty: "easy", concept: "用测试用例表覆盖多组输入、输出和边界条件。", focus: "用例结构、t.Run、子测试、边界覆盖和可读性", mechanism: "把输入、期望结果和说明放入切片表中，循环调用 t.Run 执行每个用例，统一断言主流程和边界行为。", scenarios: ["纯函数测试", "解析器测试", "错误分支覆盖", "兼容性测试"], pitfalls: "用例命名含糊、共享可变状态、并行子测试和断言信息缺失", prerequisites: ["go-test", "struct"], related: ["benchmark", "go-vet"], order: 51 }),
  /* <!-- KG_REVIEWED: benchmark | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "benchmark", zh: "benchmark", en: "Benchmark", area: "testing", difficulty: "medium", concept: "用基准测试评估代码吞吐、延迟和分配。", focus: "Benchmark 函数、b.N、分配统计和稳定输入", mechanism: "go test 按 b.N 反复执行基准函数，自动调整迭代次数，并可输出 ns/op、B/op 和 allocs/op 等指标。", scenarios: ["热点函数优化", "算法对比", "分配治理", "回归检测"], pitfalls: "输入规模失真、编译器优化影响、环境抖动和只看单次结果", commands: ["go test -bench=. -benchmem ./..."], prerequisites: ["go-test"], related: ["pprof", "escape-analysis", "gc"], order: 52 }),
  /* <!-- KG_REVIEWED: pprof | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "pprof", zh: "pprof", en: "pprof", area: "performance", difficulty: "hard", concept: "采集和分析 CPU、内存、阻塞和互斥锁等性能剖面。", focus: "CPU profile、heap profile、block profile、mutex profile 和火焰图", mechanism: "pprof 从运行时或测试中采集采样数据，把函数调用栈聚合成报告，帮助定位 CPU 热点、分配来源和阻塞位置。", scenarios: ["CPU 飙高排查", "内存增长分析", "锁竞争定位", "性能回归"], pitfalls: "采样窗口过短、压测流量失真、profile 类型混淆和优化前后缺少对照", commands: ["go test -cpuprofile cpu.out -bench=.", "go tool pprof cpu.out"], prerequisites: ["benchmark", "go-runtime"], related: ["gc", "scheduler", "http-server"], order: 53 }),
  /* <!-- KG_REVIEWED: go vet | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "go-vet", zh: "go vet", en: "go vet", area: "tooling", difficulty: "easy", concept: "用静态检查发现可疑代码模式。", focus: "格式化字符串、不可达代码、复制锁、结构体标签和常见误用", mechanism: "go vet 基于语法和类型信息执行一组官方分析器，报告编译器可接受但高风险的代码。", scenarios: ["CI 质量检查", "提交前扫描", "重构验证", "库发布前检查"], pitfalls: "只依赖编译通过、忽略检查报告、生成代码噪音和自定义分析缺口", commands: ["go vet ./..."], prerequisites: ["go-toolchain"], related: ["gofmt", "go-test", "staticcheck"], order: 54 }),
  /* <!-- KG_REVIEWED: build tags | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "build-tags", zh: "build tags", en: "Build Tags", area: "tooling", difficulty: "medium", concept: "按平台、环境或自定义条件选择参与构建的文件。", focus: "go:build、文件后缀、平台条件和测试变体", mechanism: "构建系统读取文件顶部的 go:build 条件和文件名后缀，选择符合当前 GOOS、GOARCH 和 tags 的源码参与编译。", scenarios: ["平台适配", "企业版功能", "集成测试开关", "实验特性"], pitfalls: "条件表达式复杂、CI 标签遗漏、文件组合失衡和测试覆盖缺口", commands: ["go test -tags=integration ./...", "GOOS=linux GOARCH=amd64 go build"], prerequisites: ["go-toolchain"], related: ["go-module", "go-test"], order: 55 }),
  /* <!-- KG_REVIEWED: HTTP Server | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "http-server", zh: "HTTP Server", en: "HTTP Server", area: "web", difficulty: "medium", concept: "用 net/http 暴露 HTTP 服务入口。", focus: "Handler、ServeMux、Server、超时、优雅关闭和中间件", mechanism: "net/http 接收连接后解析请求，把请求交给匹配的 Handler；Server 配置读写超时、TLS、连接状态和关闭流程。", scenarios: ["REST API", "内部服务", "健康检查", "Webhook"], pitfalls: "超时缺失、请求体关闭、上下文取消、优雅退出和 panic 保护", commands: ["go test ./...", "curl localhost:8080/health"], prerequisites: ["main-function", "context"], related: ["http-middleware", "json", "http-client"], order: 56 }),
  /* <!-- KG_REVIEWED: HTTP Middleware | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "http-middleware", zh: "HTTP Middleware", en: "HTTP Middleware", area: "web", difficulty: "medium", concept: "在 Handler 链路中统一处理横切逻辑。", focus: "Handler 包装、日志、鉴权、恢复、超时和链路追踪", mechanism: "中间件接收并返回 http.Handler，通过闭包包裹 next，在请求进入业务处理前后执行统一逻辑。", scenarios: ["认证授权", "访问日志", "panic 恢复", "限流超时"], pitfalls: "顺序依赖、ResponseWriter 包装、上下文值滥用和重复写响应", prerequisites: ["http-server", "closure"], related: ["panic-recover", "context", "error-handling"], order: 57 }),
  /* <!-- KG_REVIEWED: HTTP Client | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "http-client", zh: "HTTP Client", en: "HTTP Client", area: "web", difficulty: "medium", concept: "用 net/http 发起外部 HTTP 调用并管理连接复用。", focus: "Client、Transport、超时、连接池、请求上下文和响应体", mechanism: "http.Client 通过 Transport 复用连接并发送请求，调用方通过 context 控制取消，通过关闭响应体归还连接资源。", scenarios: ["调用下游服务", "Webhook 推送", "外部 API 集成", "健康探测"], pitfalls: "超时配置缺失、响应体未关闭、连接池耗尽和重试放大", prerequisites: ["context", "error-handling"], related: ["http-server", "json"], order: 58 }),
  /* <!-- KG_REVIEWED: JSON | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "json", zh: "JSON", en: "JSON", area: "web", difficulty: "easy", concept: "用 encoding/json 在结构体和 JSON 文本之间转换。", focus: "Marshal、Unmarshal、结构体标签、零值和流式解码", mechanism: "encoding/json 通过反射读取导出字段和 json 标签，把 Go 值编码为 JSON，或把 JSON 文本解码到结构体、map 或接口值中。", scenarios: ["REST API", "配置文件", "消息体", "日志字段"], pitfalls: "未导出字段、标签拼写、数字精度、空值语义和未知字段处理", prerequisites: ["struct", "interface"], related: ["http-server", "http-client"], order: 59 }),
  /* <!-- KG_REVIEWED: database/sql | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "database-sql", zh: "database/sql", en: "database/sql", area: "web", difficulty: "medium", concept: "用统一接口访问关系型数据库并管理连接池。", focus: "DB、Tx、Rows、Context、连接池和驱动注册", mechanism: "database/sql 通过驱动提供具体数据库协议实现，DB 管理连接池，Query、Exec、Tx 和 Context 共同控制语句执行、事务和取消。", scenarios: ["业务读写", "事务处理", "连接池调优", "慢查询定位"], pitfalls: "Rows 关闭、事务边界、连接泄漏、上下文超时和 NULL 扫描", prerequisites: ["context", "error-handling", "init-function"], related: ["http-server", "sql-transaction"], order: 60 }),
  /* <!-- KG_REVIEWED: SQL 事务 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "sql-transaction", zh: "SQL 事务", en: "SQL Transaction", area: "web", difficulty: "medium", concept: "在 Go 代码中用 Tx 明确数据库原子操作边界。", focus: "BeginTx、Commit、Rollback、隔离级别和上下文取消", mechanism: "BeginTx 从连接池中绑定连接并创建事务，业务在 Tx 上执行语句，成功后 Commit，失败路径通过 Rollback 释放事务状态。", scenarios: ["订单创建", "余额变更", "批量写入", "一致性保护"], pitfalls: "混用 DB 与 Tx、Rollback 缺失、长事务、上下文取消和连接占用", prerequisites: ["database-sql", "defer"], related: ["error-handling", "context"], order: 61 }),
  /* <!-- KG_REVIEWED: 配置与环境变量 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "config-env", zh: "配置与环境变量", en: "Configuration and Environment", area: "tooling", difficulty: "easy", concept: "用环境变量、参数和配置文件把运行差异从代码中分离。", focus: "os.Getenv、flag、配置结构体、默认值和启动校验", mechanism: "程序启动时读取环境变量、命令行参数或配置文件，解析到结构体并做校验，再把配置注入依赖组件。", scenarios: ["服务启动", "多环境部署", "本地调试", "CI 任务"], pitfalls: "默认值含糊、敏感信息泄露、启动后动态变更和配置校验缺失", prerequisites: ["main-function", "struct"], related: ["http-server", "go-test"], order: 62 }),
  /* <!-- KG_REVIEWED: 日志 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "logging", zh: "日志", en: "Logging", area: "tooling", difficulty: "easy", concept: "记录请求、错误、状态和诊断上下文。", focus: "结构化日志、上下文关联、级别、字段和采样", mechanism: "日志库把时间、级别、消息和字段写入标准输出或文件，服务通过请求 ID、trace ID 和业务字段关联链路。", scenarios: ["接口排查", "任务审计", "错误定位", "性能分析"], pitfalls: "字段缺失、敏感信息、重复日志、阻塞输出和级别混乱", prerequisites: ["error-handling", "context"], related: ["http-middleware", "pprof"], order: 63 }),
  /* <!-- KG_REVIEWED: 静态检查 | 2026-05-24 | source_count=7 --> */
  goPoint({ id: "staticcheck", zh: "静态检查", en: "Static Analysis", area: "tooling", difficulty: "medium", concept: "用额外分析器发现可维护性和正确性风险。", focus: "vet、staticcheck、ineffassign、unused 和 CI 集成", mechanism: "静态检查工具基于语法树、类型信息和控制流分析代码模式，报告冗余、误用、潜在 bug 和风格问题。", scenarios: ["质量门禁", "重构验证", "开源库维护", "团队规范"], pitfalls: "规则噪音、版本漂移、误报处理和本地 CI 配置差异", commands: ["go vet ./...", "staticcheck ./..."], prerequisites: ["go-vet"], related: ["gofmt", "go-test"], order: 64 }),
] satisfies GraphKnowledgePoint[];
